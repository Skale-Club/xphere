// Empty stub for the `server-only` marker module under Vitest.
// Next.js provides `server-only` via its bundler; in the Node test runner it is
// not a real package, so we alias it here to a no-op (see vitest.config.ts).
export {}
