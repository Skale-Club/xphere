// Test-only stub for the `server-only` package, which throws/!resolves outside
// a React Server Components bundler. Aliased in vitest.config.ts so server
// modules (e.g. src/lib/rbac/server.ts) can be imported by unit tests.
export {}
