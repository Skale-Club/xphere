---
phase: 05-admin-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/013_org_widget_config.sql
  - src/types/database.ts
  - src/app/api/widget/[token]/config/route.ts
  - tests/widget-config-route.test.ts
autonomous: true
requirements: [ADMIN-01]

must_haves:
  truths:
    - "organizations gains widget_display_name, widget_primary_color, and widget_welcome_message columns without rewriting prior migrations"
    - "GET /api/widget/[token]/config resolves orgs by widget_token and returns only public-safe widget fields"
    - "Invalid or inactive tokens return 401 from the public config endpoint"
    - "Null or blank org config values are normalized to the existing Phase 4 widget defaults so public widget boot still works"
    - "Database types reflect the new organization columns"
  artifacts:
    - path: "supabase/migrations/013_org_widget_config.sql"
      provides: "Append-only schema change for per-org widget appearance settings"
    - path: "src/types/database.ts"
      provides: "Updated TypeScript row/insert/update types for organizations"
    - path: "src/app/api/widget/[token]/config/route.ts"
      provides: "Public widget config endpoint for displayName, primaryColor, welcomeMessage"
    - path: "tests/widget-config-route.test.ts"
      provides: "Coverage for valid token, invalid token, inactive org, and fallback/default behavior"
  key_links:
    - from: "src/app/api/widget/[token]/config/route.ts"
      to: "organizations.widget_token"
      via: "public token lookup"
      pattern: ".eq('widget_token', token)"
    - from: "src/app/api/widget/[token]/config/route.ts"
      to: "src/widget/index.ts"
      via: "runtime boot config fetch"
      pattern: "displayName|primaryColor|welcomeMessage"
---

<objective>
Wave 1: backend foundations for org-scoped widget configuration.

Purpose: Add the minimal schema and public read endpoint needed so the widget can fetch safe per-org UI settings at startup without coupling this concern to the chat route.

Output: migration 013, updated database types, public config route, automated coverage for token handling and defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-admin-configuration/05-CONTEXT.md
@src/app/api/chat/[token]/route.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration 013 for organization widget config fields</name>
  <files>supabase/migrations/013_org_widget_config.sql, src/types/database.ts</files>
  <read_first>
    - supabase/migrations/012_org_widget_token.sql
    - src/types/database.ts
    - .planning/phases/05-admin-configuration/05-CONTEXT.md (D-01, D-02, D-13)
  </read_first>
  <action>
Add a new append-only migration that extends `organizations` with:

- `widget_display_name`
- `widget_primary_color`
- `widget_welcome_message`

Use nullable text columns unless a stronger constraint is already justified by the code path. Do not introduce a new widget table.

Update `src/types/database.ts` so the organizations row/insert/update types include the new fields.
  </action>
  <acceptance_criteria>
    - Migration 013 is additive only and does not edit previous migration files
    - `Database['public']['Tables']['organizations']` includes the 3 new widget config fields
    - No `system_prompt` or extra widget settings are introduced in this plan
  </acceptance_criteria>
  <done>Schema and generated types can represent per-org widget UI settings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GET /api/widget/[token]/config</name>
  <files>src/app/api/widget/[token]/config/route.ts</files>
  <read_first>
    - src/app/api/chat/[token]/route.ts
    - .planning/phases/05-admin-configuration/05-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
Create a Node.js route handler at `src/app/api/widget/[token]/config/route.ts`.

Use the service-role Supabase client because the request is public and token-based. Resolve the org by `widget_token`, require `is_active = true`, and return only these public fields:

```json
{
  "displayName": "...",
  "primaryColor": "#18181B",
  "welcomeMessage": "..."
}
```

Normalize null/blank values to the same hardcoded defaults currently used by the widget so boot-time config fetch failures or partially configured orgs remain safe.
  </action>
  <acceptance_criteria>
    - Valid active token returns 200 with only public-safe widget config fields
    - Invalid or inactive token returns 401
    - Route stays lean and does not return org IDs, secrets, or chat/session data
    - Default/fallback values match the current widget UI contract
  </acceptance_criteria>
  <done>Widget runtime has a dedicated public endpoint for org-specific UI config.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for config route behavior</name>
  <files>tests/widget-config-route.test.ts</files>
  <read_first>
    - tests/*.test.ts (match existing Vitest style)
    - src/app/api/widget/[token]/config/route.ts
  </read_first>
  <action>
Add route-level tests that cover:

1. valid active token returns configured values
2. invalid token returns 401
3. inactive org returns 401
4. null/blank DB values normalize to widget defaults

Mock only the smallest necessary surface of the service-role client.
  </action>
  <verify>
    <automated>npx vitest run tests/widget-config-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All new route tests pass
    - Tests prove the endpoint never exposes non-public org fields
  </acceptance_criteria>
  <done>Public config contract is pinned by automated tests.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/widget-config-route.test.ts
npm run build
```
</verification>

<success_criteria>
- ADMIN-01 backend storage foundation exists
- Public config endpoint is safe, token-scoped, and default-tolerant
- Migration history remains append-only
- Build and focused tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-configuration/05-01-SUMMARY.md`
</output>
