---
phase: 05-admin-configuration
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/layout/app-sidebar.tsx
  - src/app/(dashboard)/widget/page.tsx
  - src/app/(dashboard)/widget/actions.ts
  - src/components/widget/widget-settings-form.tsx
  - src/components/widget/widget-preview.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "A new top-level /widget dashboard page exists and is reachable from the sidebar"
    - "Admins can edit display name, primary color, and welcome message for the active org and save them via server action"
    - "The page shows a live preview that reflects unsaved edits immediately"
    - "The page shows the canonical embed script tag using https://voiceops.skale.club/widget.js and the current widget token"
    - "Admins can regenerate widget_token with an explicit warning that old installs are invalidated immediately"
    - "Hex color validation is enforced in both client schema and server-side persistence path"
  artifacts:
    - path: "src/app/(dashboard)/widget/page.tsx"
      provides: "Authenticated widget admin route for the active org"
    - path: "src/app/(dashboard)/widget/actions.ts"
      provides: "Server actions for save + token regeneration"
    - path: "src/components/widget/widget-settings-form.tsx"
      provides: "Interactive form with live preview state and save UX"
    - path: "src/components/widget/widget-preview.tsx"
      provides: "Local preview mirror of widget UI"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Widget nav entry"
  key_links:
    - from: "src/app/(dashboard)/widget/actions.ts"
      to: "organizations"
      via: "active-org scoped update + token rotation"
      pattern: "widget_display_name|widget_primary_color|widget_welcome_message|widget_token"
    - from: "src/components/widget/widget-settings-form.tsx"
      to: "src/components/widget/widget-preview.tsx"
      via: "unsaved form state passed into preview props"
      pattern: "displayName|primaryColor|welcomeMessage"
---

<objective>
Wave 2: deliver the admin-facing widget configuration surface.

Purpose: Give the active organization a first-class dashboard page to customize widget appearance, preview the result before saving, copy the install snippet, and rotate its public token safely.

Output: `/widget` page, sidebar entry, save/token server actions, preview component, embed code panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-configuration/05-CONTEXT.md
@src/components/layout/app-sidebar.tsx
@src/app/(dashboard)/organizations/actions.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dashboard route and sidebar entry</name>
  <files>src/components/layout/app-sidebar.tsx, src/app/(dashboard)/widget/page.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx
    - src/app/(dashboard)/organizations/page.tsx
    - .planning/phases/05-admin-configuration/05-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
Add `Widget` as a top-level nav item in the sidebar and create `src/app/(dashboard)/widget/page.tsx`.

The page should be an authenticated server component that reads the active org using existing auth/org patterns, loads the org's current widget config values plus `widget_token`, and renders the admin UI.
  </action>
  <acceptance_criteria>
    - `/widget` appears in the sidebar with the same active-state behavior as other top-level routes
    - Unauthenticated users are redirected using existing dashboard patterns
    - Page data is scoped to the active org only
  </acceptance_criteria>
  <done>Admins can navigate to a dedicated widget configuration page.</done>
</task>

<task type="auto">
  <name>Task 2: Implement widget settings save and token regeneration actions</name>
  <files>src/app/(dashboard)/widget/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/organizations/actions.ts
    - .planning/phases/05-admin-configuration/05-CONTEXT.md (D-04, D-10, D-11, D-13)
  </read_first>
  <action>
Create server actions for:

1. saving `widget_display_name`, `widget_primary_color`, and `widget_welcome_message`
2. regenerating `widget_token` with `crypto.randomUUID()`

Use `getUser()` and `createClient()` helpers, update only the active org, and revalidate `/widget` after success. Enforce `#RRGGBB` validation server-side before persistence.
  </action>
  <acceptance_criteria>
    - Save action updates only the active organization row
    - Regenerate action replaces the previous token entirely
    - Both actions return user-facing errors cleanly when validation fails
    - No direct `supabase.auth.getUser()` calls are introduced
  </acceptance_criteria>
  <done>Authenticated mutations exist for all admin configuration actions in scope.</done>
</task>

<task type="auto">
  <name>Task 3: Build the form, preview, embed code, and danger zone UI</name>
  <files>src/components/widget/widget-settings-form.tsx, src/components/widget/widget-preview.tsx, src/app/(dashboard)/widget/page.tsx</files>
  <read_first>
    - src/components/organizations/organization-form.tsx
    - src/components/tools/tool-config-form.tsx
    - src/widget/index.ts
    - .planning/phases/05-admin-configuration/05-CONTEXT.md (D-05, D-06, D-07, D-11)
  </read_first>
  <action>
Implement a client form component using the repo's existing `react-hook-form` + `zod` + `sonner` pattern.

The page should include:

- editable fields for display name, primary color, welcome message
- a live local preview that updates before save
- an embed code panel showing:

```html
<script src="https://voiceops.skale.club/widget.js" data-token="<widget_token>"></script>
```

- a token regeneration control in a clearly labeled warning/danger section

Do not mount the real widget script inside the dashboard page.
  </action>
  <acceptance_criteria>
    - Unsaved edits update the preview immediately
    - Embed code always uses the canonical production host
    - Token regeneration warning makes immediate invalidation explicit
    - UI remains limited to Phase 5 scope; no system prompt editing appears
  </acceptance_criteria>
  <done>The dashboard surface satisfies ADMIN-01 through ADMIN-04 from the admin side.</done>
</task>

</tasks>

<verification>
```bash
npm run build
```
</verification>

<success_criteria>
- `/widget` is a first-class dashboard area
- Save, preview, embed code, and token regeneration all work for the active org
- Validation is enforced on both client and server
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-configuration/05-02-SUMMARY.md`
</output>
