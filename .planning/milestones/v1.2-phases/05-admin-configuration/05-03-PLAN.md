---
phase: 05-admin-configuration
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/widget/index.ts
  - public/widget.js
  - tests/widget.test.ts
autonomous: true
requirements: [ADMIN-01]

must_haves:
  truths:
    - "The widget fetches /api/widget/[token]/config during startup and applies returned displayName, primaryColor, and welcomeMessage"
    - "If the config fetch fails, the widget falls back to the same Phase 4 defaults instead of failing to boot"
    - "The fetch does not break the existing token capture, double-init guard, session persistence, or chat send flow"
    - "Automated widget tests cover successful config hydration and failure fallback behavior"
    - "npm run build:widget regenerates public/widget.js from the updated widget source"
  artifacts:
    - path: "src/widget/index.ts"
      provides: "Widget startup config fetch + runtime UI overrides"
    - path: "tests/widget.test.ts"
      provides: "Coverage for config fetch success/failure without regressing prior widget contracts"
    - path: "public/widget.js"
      provides: "Built IIFE bundle with config-fetch behavior"
  key_links:
    - from: "src/widget/index.ts"
      to: "src/app/api/widget/[token]/config/route.ts"
      via: "boot-time GET fetch"
      pattern: "/api/widget/${token}/config"
    - from: "src/widget/index.ts"
      to: "tests/widget.test.ts"
      via: "jsdom-evaluated runtime behavior"
      pattern: "displayName|primaryColor|welcomeMessage"
---

<objective>
Wave 3: wire the public widget runtime to the new config endpoint.

Purpose: Make the shipped embed artifact actually use the new admin-configured values while preserving the Phase 4 widget contract and fallback safety.

Output: updated widget source, rebuilt public/widget.js, expanded widget tests for config hydration/fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-admin-configuration/05-CONTEXT.md
@src/widget/index.ts
@src/app/api/widget/[token]/config/route.ts
@tests/widget.test.ts
@.planning/phases/04-widget-embed-script/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch public widget config during widget boot</name>
  <files>src/widget/index.ts</files>
  <read_first>
    - src/widget/index.ts
    - .planning/phases/05-admin-configuration/05-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
Update the standalone widget so startup includes a GET request to `/api/widget/{token}/config` using the existing `apiBase` origin derived from the script URL.

Apply returned values to the widget UI for:

- header/display name
- primary color usage in the widget shell
- welcome/empty-state copy

Retain the current hardcoded defaults as the fallback when the fetch fails, returns non-200, or returns malformed data.

Do not regress the existing document.currentScript synchronous capture requirement.
  </action>
  <acceptance_criteria>
    - Widget still boots when config fetch fails
    - Runtime config affects visible widget UI state
    - Existing session/chat behavior remains intact
  </acceptance_criteria>
  <done>The public widget now reflects admin-managed org configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Extend widget tests for config hydration and fallback</name>
  <files>tests/widget.test.ts</files>
  <read_first>
    - tests/widget.test.ts
    - src/widget/index.ts
  </read_first>
  <action>
Expand the jsdom widget tests to cover at least:

1. successful config fetch updates displayed widget values
2. failed config fetch preserves existing defaults
3. prior widget behaviors (double-init guard, localStorage session handling, invalid-token error flow) still pass

Prefer extending the existing widget test suite instead of creating a second overlapping widget runtime test file.
  </action>
  <verify>
    <automated>npx vitest run tests/widget.test.ts tests/widget-asset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New config-related widget tests pass
    - Pre-existing widget behavior stays green
  </acceptance_criteria>
  <done>Runtime config behavior is protected by automated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the distributable widget asset</name>
  <files>public/widget.js</files>
  <read_first>
    - package.json (existing build:widget script)
  </read_first>
  <action>
Run the repo's widget build pipeline so `public/widget.js` reflects the updated TypeScript source.

Do not hand-edit the built artifact.
  </action>
  <verify>
    <automated>npm run build:widget</automated>
  </verify>
  <acceptance_criteria>
    - public/widget.js is regenerated from src/widget/index.ts
    - widget asset tests remain green after rebuild
  </acceptance_criteria>
  <done>The shipped widget bundle includes the new config-fetch behavior.</done>
</task>

</tasks>

<verification>
```bash
npm run build:widget
npx vitest run tests/widget.test.ts tests/widget-asset.test.ts
npm run build
```
</verification>

<success_criteria>
- Public widget consumes admin-managed config safely
- Fallback defaults preserve boot reliability
- Widget build output is regenerated and tested
- No Phase 4 regressions are introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-configuration/05-03-SUMMARY.md`
</output>
