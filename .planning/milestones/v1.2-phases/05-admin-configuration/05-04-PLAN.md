---
phase: 05-admin-configuration
plan: 04
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified: []
autonomous: false
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "A human verifies the /widget page end-to-end in the browser"
    - "Saved settings appear in both the dashboard preview and the real embedded widget"
    - "The embed script uses the current token and old tokens stop working after regeneration"
    - "All automated verification from Plans 01-03 remains green"
  artifacts:
    - path: "src/app/(dashboard)/widget/page.tsx"
      provides: "Interactive admin UI under manual review"
    - path: "public/widget.js"
      provides: "Real embed artifact under manual review"
  key_links:
    - from: "src/app/(dashboard)/widget/page.tsx"
      to: "public/widget.js"
      via: "admin saves config, embedded widget consumes it"
      pattern: "widget_token"
---

<objective>
Wave 4: browser verification checkpoint for admin configuration.

Purpose: Confirm the admin UI, public config endpoint, and real widget artifact work together end-to-end, including the operationally sensitive token-rotation flow.

Output: Human confirmation that all ADMIN requirements are satisfied in practice, not just by isolated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-admin-configuration/05-01-SUMMARY.md
@.planning/phases/05-admin-configuration/05-02-SUMMARY.md
@.planning/phases/05-admin-configuration/05-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human browser verification of the widget admin flow</name>
  <files>src/app/(dashboard)/widget/page.tsx, public/widget.js</files>
  <action>Open the dashboard and verify the checklist below against a real organization with a valid widget token.</action>
  <what-built>
Phase 5 adds per-org widget configuration stored on organizations, a dedicated `/widget` admin page, a public config endpoint, runtime widget config fetch, and token regeneration. This checkpoint verifies that those pieces work together in a real browser.
  </what-built>
  <how-to-verify>
1. Start the app and sign in as an admin with an active org selected.
2. Open `/widget`.
3. Confirm the page shows current values for display name, primary color, welcome message, and the current embed script.
4. Change all 3 fields without saving and confirm the live preview updates immediately.
5. Save changes and reload `/widget`; confirm persisted values remain.
6. Paste the shown script tag into a simple HTML page (or reuse `public/widget-test.html` with the current token).
7. Load that page and confirm the real widget header/name, visible accent color, and welcome message reflect the saved config.
8. Back in `/widget`, regenerate the token.
9. Confirm the page shows a different embed script token after regeneration.
10. Reload the old installed widget page using the old token and confirm chat/config no longer resolve.
11. Update the embed script to the new token and confirm the widget works again.
12. Confirm no system prompt editor or out-of-scope settings were introduced.

Type `approved` if all checks pass, or describe any issues that need fixing.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Human has confirmed ADMIN-01 through ADMIN-04 end-to-end in a real browser.</done>
  <resume-signal>Type "approved" to proceed, or describe any issues for Claude to fix before continuing.</resume-signal>
</task>

</tasks>

<verification>
```bash
npm run build
npx vitest
```
</verification>

<success_criteria>
- All 4 ADMIN requirements are confirmed by human verification
- Token regeneration behavior is operationally correct
- Real widget reflects saved admin settings
- Automated verification remains green
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-configuration/05-04-SUMMARY.md`
</output>
