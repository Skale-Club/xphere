---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/app/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(dashboard)/tools/page.tsx
  - src/app/(dashboard)/tools/[toolConfigId]/page.tsx
  - src/components/assistants/assistant-mapping-form.tsx
  - src/components/assistants/assistant-mappings-table.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/tools/tools-table.tsx
  - src/lib/knowledge/extract-text.ts
  - src/types/database.ts
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02

must_haves:
  truths:
    - "The string 'VoiceOps' does not appear in any file under src/ in user-facing strings, metadata, or UI labels"
    - "The string 'Voice AI Operations Platform' does not appear anywhere in src/"
    - "Every page title, sidebar brand, login heading, and tool description displays Leaidear"
    - "layout.tsx metadata title is 'Leaidear' and description is 'AI Operations Platform'"
    - "app-sidebar.tsx brand span text is 'Leaidear'"
    - "login/page.tsx h1 text is 'Leaidear' and subtitle is 'AI Operations Platform'"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root metadata with title=Leaidear, description=AI Operations Platform"
      contains: "title: 'Leaidear'"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Sidebar brand display showing Leaidear"
      contains: "Leaidear"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page h1 and subtitle showing Leaidear"
      contains: "Leaidear"
  key_links:
    - from: "src/app/layout.tsx"
      to: "browser tab title"
      via: "Next.js metadata export"
      pattern: "title.*Leaidear"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "sidebar brand span"
      via: "JSX text content"
      pattern: ">Leaidear<"
---

<objective>
Rename all user-visible "VoiceOps" strings to "Leaidear" across 10 source files and 2 documentation files. This is a pure text substitution pass — no logic changes, no new imports, no structural modifications.

Purpose: Fulfill BRAND-01 and BRAND-02 — the running app must display "Leaidear" on every page, nav element, login screen, and page title.
Output: 12 modified files with VoiceOps → Leaidear substitutions applied per the exact replacement table from research.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Exact replacement table from 01-RESEARCH.md (confirmed by live grep 2026-04-04): -->

| File | Line | Current string | Replace with |
|------|------|----------------|--------------|
| src/app/layout.tsx | 10 | `title: 'VoiceOps'` | `title: 'Leaidear'` |
| src/app/layout.tsx | 11 | `description: 'Voice AI Operations Platform'` | `description: 'AI Operations Platform'` |
| src/app/(auth)/login/page.tsx | 88 | `VoiceOps` (h1 text) | `Leaidear` |
| src/app/(auth)/login/page.tsx | 91 | `Voice AI Operations Platform` (subtitle) | `AI Operations Platform` |
| src/app/(dashboard)/tools/page.tsx | 16 | `VoiceOps actions and integrations` | `Leaidear actions and integrations` |
| src/app/(dashboard)/tools/[toolConfigId]/page.tsx | 125 | `VoiceOps mapping` | `Leaidear mapping` |
| src/components/assistants/assistant-mapping-form.tsx | 140 | `read in VoiceOps` | `read in Leaidear` |
| src/components/assistants/assistant-mappings-table.tsx | 232 | `inside VoiceOps` | `inside Leaidear` |
| src/components/layout/app-sidebar.tsx | 95 | `VoiceOps` (brand span) | `Leaidear` |
| src/components/tools/tools-table.tsx | 218 | `through VoiceOps actions` | `through Leaidear actions` |
| src/lib/knowledge/extract-text.ts | 31 | `VoiceOps-KnowledgeBot/1.0` | `Leaidear-KnowledgeBot/1.0` |
| src/types/database.ts | 1 | `// Database type definitions for VoiceOps` | `// Database type definitions for Leaidear` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename VoiceOps to Leaidear in all src/ files</name>
  <files>
    src/app/layout.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(dashboard)/tools/page.tsx,
    src/app/(dashboard)/tools/[toolConfigId]/page.tsx,
    src/components/assistants/assistant-mapping-form.tsx,
    src/components/assistants/assistant-mappings-table.tsx,
    src/components/layout/app-sidebar.tsx,
    src/components/tools/tools-table.tsx,
    src/lib/knowledge/extract-text.ts,
    src/types/database.ts
  </files>
  <read_first>
    - src/app/layout.tsx (read full file before editing)
    - src/app/(auth)/login/page.tsx (read full file before editing)
    - src/app/(dashboard)/tools/page.tsx (read full file before editing)
    - src/app/(dashboard)/tools/[toolConfigId]/page.tsx (read full file before editing)
    - src/components/assistants/assistant-mapping-form.tsx (read full file before editing)
    - src/components/assistants/assistant-mappings-table.tsx (read full file before editing)
    - src/components/layout/app-sidebar.tsx (read full file before editing)
    - src/components/tools/tools-table.tsx (read full file before editing)
    - src/lib/knowledge/extract-text.ts (read full file before editing)
    - src/types/database.ts (read full file before editing)
  </read_first>
  <action>
Apply exactly these substitutions to each file. Read each file first, then make the targeted edit. Do NOT change any other content (imports, logic, types, URLs, cookie names, webhook URLs).

**src/app/layout.tsx:**
- Line 10: change `title: 'VoiceOps'` → `title: 'Leaidear'`
- Line 11: change `description: 'Voice AI Operations Platform'` → `description: 'AI Operations Platform'`

**src/app/(auth)/login/page.tsx:**
- Find the h1 element containing the text "VoiceOps" and change its text content to "Leaidear"
- Find the subtitle/paragraph containing "Voice AI Operations Platform" and change to "AI Operations Platform"

**src/app/(dashboard)/tools/page.tsx:**
- Find "VoiceOps actions and integrations" and change to "Leaidear actions and integrations"

**src/app/(dashboard)/tools/[toolConfigId]/page.tsx:**
- Find "VoiceOps mapping" and change to "Leaidear mapping"

**src/components/assistants/assistant-mapping-form.tsx:**
- Find "read in VoiceOps" and change to "read in Leaidear"

**src/components/assistants/assistant-mappings-table.tsx:**
- Find "inside VoiceOps" and change to "inside Leaidear"

**src/components/layout/app-sidebar.tsx:**
- Find the brand span at line ~95 containing "VoiceOps" and change to "Leaidear"

**src/components/tools/tools-table.tsx:**
- Find "through VoiceOps actions" and change to "through Leaidear actions"

**src/lib/knowledge/extract-text.ts:**
- Find "VoiceOps-KnowledgeBot/1.0" (User-Agent header value) and change to "Leaidear-KnowledgeBot/1.0"

**src/types/database.ts:**
- Find the file comment on line 1: `// Database type definitions for VoiceOps` and change to `// Database type definitions for Leaidear`

**IMPORTANT — Do NOT change:**
- Any URL containing `voiceops.skale.club` (canonical production origin — intentionally preserved)
- The `vo_active_org` cookie name (`vo_` is an internal prefix, not the brand name)
- `package.json` `"name": "voiceops"` (internal, never user-visible)
- Anything in `supabase/migrations/` (never edit existing migrations)
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/voiceops && grep -r "VoiceOps\|Voice AI" src/ --include="*.ts" --include="*.tsx" 2>/dev/null || echo "CLEAN — no VoiceOps strings found in src/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "VoiceOps" src/` returns no output (exit 1 = no matches = CLEAN)
    - `grep -r "Voice AI" src/` returns no output
    - src/app/layout.tsx contains: `title: 'Leaidear'`
    - src/app/layout.tsx contains: `description: 'AI Operations Platform'`
    - src/components/layout/app-sidebar.tsx contains: `Leaidear`
    - src/lib/knowledge/extract-text.ts contains: `Leaidear-KnowledgeBot/1.0`
    - src/types/database.ts line 1 contains: `Leaidear`
    - `npm run build` exits 0 (no TypeScript errors from the edits)
  </acceptance_criteria>
  <done>All 10 src/ files updated; grep confirms zero VoiceOps occurrences remaining in src/; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation files and run brand test</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - CLAUDE.md (read full file before editing — must not change voiceops.skale.club URLs)
    - README.md (read full file before editing)
    - tests/brand.test.ts (understand what assertions will be verified)
  </read_first>
  <action>
Update documentation files to use "Leaidear" instead of "VoiceOps" for consistency (per D-03).

**CLAUDE.md:**
- Change the heading `# VoiceOps - Claude Code Instructions` → `# Leaidear - Claude Code Instructions`
- Change the product framing description "VoiceOps is a tenant-aware integration..." → "Leaidear is a tenant-aware integration..."
- **DO NOT change**: `https://voiceops.skale.club` (canonical production origin — must remain unchanged, per Pitfall 3 in RESEARCH.md)
- **DO NOT change**: webhook URL paths like `/api/vapi/tools`, `/api/vapi/calls`, `/api/vapi/campaigns`
- **DO NOT change**: any technical identifiers (cookie names, env vars, package names)

**README.md:**
- Replace occurrences of "VoiceOps" with "Leaidear" in the project title/description
- Do not change technical identifiers, URLs, or CLI commands

After updating documentation files, run the brand test and the full build to verify Plan 02 completion:
- `npx vitest run tests/brand.test.ts` — must PASS (GREEN)
- `npm run build` — must exit 0
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/voiceops && npx vitest run tests/brand.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md line 1 contains: `# Leaidear - Claude Code Instructions`
    - CLAUDE.md contains: `Leaidear is a tenant-aware`
    - CLAUDE.md still contains: `https://voiceops.skale.club` (URL unchanged)
    - `npx vitest run tests/brand.test.ts` exits 0 (both assertions GREEN)
    - `npm run build` exits 0
    - `grep "VoiceOps" CLAUDE.md` returns only the URL line or the product-framing section — no heading or brand label occurrences
  </acceptance_criteria>
  <done>CLAUDE.md and README.md updated to Leaidear; brand test passes GREEN; build clean</done>
</task>

</tasks>

<verification>
- `grep -r "VoiceOps\|Voice AI" src/` returns no matches
- `npx vitest run tests/brand.test.ts` exits 0 (GREEN)
- `npm run build` exits 0
- `grep "voiceops.skale.club" CLAUDE.md` still returns the canonical URL line (unchanged)
</verification>

<success_criteria>
Zero "VoiceOps" or "Voice AI Operations Platform" strings remain in src/. The brand test is GREEN. The build is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
