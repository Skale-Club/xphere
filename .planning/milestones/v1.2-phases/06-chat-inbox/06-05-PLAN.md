---
phase: 06-chat-inbox
plan: "05"
type: execute
wave: 5
depends_on:
  - "06-04"
files_modified: []
autonomous: false
requirements:
  - INBOX-01
  - INBOX-02
  - INBOX-03
  - INBOX-04
  - INBOX-05
  - INBOX-06
  - INBOX-07

must_haves:
  truths:
    - "Admin can navigate to /chat and see the conversation inbox"
    - "The sidebar Chat group appears with Inbox and Settings items; Widget is no longer standalone"
    - "Selecting a conversation loads the message thread"
    - "Admin can send a message and see it appear in the thread"
    - "Archive/Reopen and Delete actions work on conversations"
    - "Desktop shows ResizablePanelGroup layout"
    - "Mobile shows CSS slide animation when switching between list and detail"
  artifacts:
    - path: "src/app/(dashboard)/chat/page.tsx"
      provides: "Working /chat page"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Updated sidebar with Chat group"
  key_links: []
---

<objective>
Human verification of the complete Phase 6 chat inbox feature — all four automated plans have run, now confirm the inbox works end-to-end in the browser.

Purpose: Plans 01-04 were fully automated. This checkpoint verifies the integration in a live browser: database migration is live, API endpoints respond correctly, UI renders and interacts properly, and sidebar navigation is correct.

Output: Confirmed or issues reported for gap-closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-inbox/06-CONTEXT.md
@.planning/phases/06-chat-inbox/06-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 6 Chat Inbox — complete feature:
- Migration 015: `conversations` and `conversation_messages` tables in Supabase (renamed from chat_sessions/chat_messages)
- Admin API: GET /api/chat/conversations, GET+DELETE /api/chat/conversations/[id], GET+POST /api/chat/conversations/[id]/messages, POST /api/chat/conversations/[id]/status, GET /api/chat/settings
- Frontend: ConversationList (tabs, search, relative times), ChatArea (message bubbles, debug toggle, send form), AdminChatLayout (polling, responsive layout)
- /chat page wired to AdminChatLayout
- Sidebar Chat group: Inbox (/chat) + Settings (/widget), standalone Widget item removed
  </what-built>
  <how-to-verify>
Start the dev server if not already running:
```bash
npm run dev
```
Visit http://localhost:4267 and verify each item:

**Sidebar navigation:**
1. Confirm the sidebar shows a "Chat" group with "Inbox" and "Settings" sub-items
2. Confirm there is NO standalone "Widget" item at the top level
3. Click "Inbox" — confirm it navigates to /chat
4. Click "Settings" — confirm it navigates to /widget (widget config page still works)

**Chat inbox (requires at least one conversation — send a test message to the widget first):**
5. If no conversations exist, open the widget test page (public/widget-test.html) in a browser, send a message via the widget, then refresh /chat
6. Confirm conversations appear in the list with visitor info, relative time, and last message preview
7. Confirm the tabs (Open / Archived / All) filter conversations by status
8. Confirm the search field filters by name or message content

**Conversation detail:**
9. Click a conversation — confirm the message thread loads
10. Confirm visitor messages appear right-aligned (blue bubble) and assistant messages left-aligned (white/border bubble)
11. Type a message in the textarea and press Enter — confirm it sends and appears in the thread
12. Shift+Enter should add a newline (not send)

**Actions:**
13. On a selected conversation in the list, click "Archive" — confirm status changes to closed and conversation moves to Archived tab
14. Click "Reopen" on an archived conversation — confirm it moves back to Open tab
15. Try deleting a conversation via the Delete button (requires AlertDialog confirmation) — confirm it's removed

**Desktop layout:**
16. Confirm the conversation list and message thread are side-by-side (ResizablePanelGroup) on a wide viewport
17. Confirm the divider handle is draggable

**Mobile layout (resize browser to mobile width ~375px):**
18. Confirm only the conversation list is shown initially
19. Click a conversation — confirm the list slides out (left) and message thread slides in (right) with 300ms animation
20. Confirm the back button (arrow) in the chat header is visible and returns to the list

**Debug messages:**
21. If any conversations exist from Phase 3 (AI with tool calls), open one and check the "Show debug" checkbox — internal messages should become visible
  </how-to-verify>
  <resume-signal>
Type "approved" if all 21 checks pass.
Or describe which checks failed so gap-closure plans can be created.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirms all 21 checklist items pass.
</verification>

<success_criteria>
- All 21 browser verification items confirmed
- No TypeScript errors (npm run build was run in Plan 04)
- Sidebar navigation correct
- Conversations visible and interactive
- Admin can send messages
- Responsive layout works on desktop and mobile
</success_criteria>

<output>
After approval, create `.planning/phases/06-chat-inbox/06-05-SUMMARY.md` and update ROADMAP.md to mark Phase 6 complete.
</output>
