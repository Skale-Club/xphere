# v2.9 — UX Polish & Workflows Unification

**Started:** 2026-05-19
**Theme:** Cohesive polish pass + major Workflows refactor.

## Phases

### Phase 97 — Landing Page Polish
- 97.1 Reduce hero padding
- 97.2 Favicon + larger text in header
- 97.3 Favicon + larger text in footer
- 97.4 Footer solid bg 90% alpha

### Phase 98 — Header & Layout Reorganization
- 98.1 Move dial pad button to header (persistent)
- 98.2 Remove Copilot from sidebar (already floating bubble)
- 98.3 Super Admin shield in header (admin-only toggle)
- 98.4 Move org switcher from sidebar to header

### Phase 99 — Flow Canvas UX Overhaul
- 99.1 Remove yellow selection outline
- 99.2 Fix zoom controls (invisible + broken)
- 99.3 Minimap colors match real node types
- 99.4 Toolbar responsive (no overflow on small windows)
- 99.5 Friendly labels + colored icons for trigger/action types
- 99.6 Hide technical fields (Config JSON, Credential ref, Node ID) — build proper UI
- 99.7 Filter trigger/action options by active integrations
- 99.8 Node panel hierarchy: friendly name as title, type as subtitle
- 99.9 Breadcrumb shows editable flow name; remove duplicates

### Phase 100 — Companies Standardization
- 100.1 DB + code naming alignment (Companies everywhere)
- 100.2 Export CSV button moved to header line

### Phase 101 — Deal/Opportunity Panel Redesign
- Better hierarchy, typography, spacing, design system consistency

### Phase 102 — Workflows Unification (BIG)
- Merge "Automations" (Action Engine tool registry) + "Visual Flows"
- Single system named **Workflows**
- Preserve folders, logs, integrations, all existing capabilities
- Unified sidebar nav, unified UX

### Phase 103 — In-App Notification System
- Notification bell in header with unread count badge
- Notification panel/dropdown with list of events
- Notification types: new conversation, missed call, new contact, campaign finished, flow run failed
- Mark as read (individual + mark all read)
- Supabase Realtime for live delivery
- Persistence in DB (notifications table, per-org, per-user)

### Phase 104 — Light Theme
- Full light mode implementation via Tailwind / CSS variables
- Theme toggle (dark/light) persisted per user
- All dashboard pages, sidebar, components adapted
- Landing page already light-compatible (verify)
- System preference detection on first visit

## Success Criteria

- All visual polish items shipped
- Header is the unified action surface (org, dial, admin, copilot all accessible)
- Flow canvas usable by non-technical operators (no JSON exposed by default)
- Workflows is one cohesive system with full feature parity to old Automations + Flows
- Companies naming consistent across DB, slugs, code, UI
