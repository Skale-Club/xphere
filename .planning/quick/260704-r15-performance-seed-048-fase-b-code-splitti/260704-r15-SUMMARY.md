---
phase: 260704-r15-performance-seed-048-fase-b-code-splitti
plan: 01
subsystem: performance
tags: [next-dynamic, code-splitting, twilio, wavesurfer, calls, bundle-size]

# Dependency graph
requires:
  - phase: SEED-048 Phase A+C (quick task 260704-pr3)
    provides: Prior Calls page performance work; this plan continues Phase B (code-splitting)
provides:
  - Dynamic import() of @twilio/voice-sdk's Device class, gated behind enabled + token-fetch success
  - Four new *-lazy.tsx client wrapper components using next/dynamic({ ssr: false })
  - Calls page (list view) initial JS payload no longer includes wavesurfer.js, the call-detail/voice-settings dialog trees, or RoutingChainEditor unconditionally
affects: [calls, performance, seed-048]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "'use client' + next/dynamic(..., { ssr: false }) wrapper file pattern for code-splitting heavy client components imported from Server Components (already established by confetti-burst.tsx, now reused 4x for calls components)"
    - "Dynamic import() inside an effect body for SDK classes only needed conditionally (Device from @twilio/voice-sdk), keeping the type import static and zero-cost"

key-files:
  created:
    - src/components/calls/call-waveform-player-lazy.tsx
    - src/components/calls/call-detail-dialog-lazy.tsx
    - src/components/calls/voice-settings-dialog-lazy.tsx
    - src/components/calls/routing-chain-editor-lazy.tsx
  modified:
    - src/components/calls/twilio-device-provider.tsx
    - src/components/calls/call-detail-human.tsx
    - src/app/(dashboard)/calls/(hub)/page.tsx

key-decisions:
  - "Followed the exact confetti-burst.tsx pattern (use client wrapper + next/dynamic ssr:false) for all four new lazy wrappers, rather than inlining dynamic() calls in Server Components (which Next.js 16 disallows)"
  - "Kept Call as a type-only import in twilio-device-provider.tsx (already zero-cost) and moved only the Device runtime import to a dynamic import() inside the effect"

patterns-established:
  - "*-lazy.tsx naming convention for next/dynamic client wrappers around heavy components imported by Server Components"

requirements-completed: [SEED-048-B]

# Metrics
duration: 12min
completed: 2026-07-04
---

# Phase 260704-r15: Performance — SEED-048 Phase B Code-Splitting Summary

**Lazy-loaded the Twilio Voice SDK and dynamic-imported wavesurfer.js plus the calls-page dialog/routing-editor trees, so the Calls list view's initial JS payload no longer unconditionally ships ~200KB (voice SDK) + ~80KB (wavesurfer) + ~150KB (routing editor) of code most visitors never execute.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-04T23:36:00Z
- **Completed:** 2026-07-04T23:48:07Z
- **Tasks:** 2 completed
- **Files modified:** 7 (3 modified, 4 created)

## Accomplishments
- `@twilio/voice-sdk`'s `Device` class is now dynamically imported only inside `TwilioDeviceProvider`'s effect, reached only when `enabled` is true and the `/api/twilio/token` fetch succeeds — phone_forward/sip users never trigger this import.
- `wavesurfer.js`, `CallDetailDialog`, `VoiceSettingsDialog`, and `RoutingChainEditor` are now behind `next/dynamic(..., { ssr: false })` client wrappers, following the codebase's existing `confetti-burst.tsx` pattern.
- No visible behavior, UI, or prop-contract changes — all four lazy-wrapped components forward identical props to their originals.
- `npm run build` passes cleanly (verified after each task and once more as the authoritative final check) with no TypeScript errors and no Server-Component/`ssr:false` build error.

## Task Commits

Each task was committed atomically:

1. **Task 1: Lazy-load the Twilio Voice SDK inside TwilioDeviceProvider** - `6bf909e2` (feat)
2. **Task 2: Dynamic-import the waveform player, call-detail dialog, voice-settings dialog, and routing-chain editor** - `2ed200f5` (feat)

**Plan metadata:** (this commit, see below)

## Files Created/Modified
- `src/components/calls/twilio-device-provider.tsx` - `Device` moved from a static import to `const { Device } = await import('@twilio/voice-sdk')` inside the effect's `init()`, gated by the existing `enabled` check; `Call`/`Device` remain type-only imports at module scope
- `src/components/calls/call-waveform-player-lazy.tsx` (new) - `'use client'` wrapper: `dynamic(() => import('./call-waveform-player').then(m => m.CallWaveformPlayer), { ssr: false })`
- `src/components/calls/call-detail-dialog-lazy.tsx` (new) - `'use client'` wrapper around `CallDetailDialog`, forwards `children`
- `src/components/calls/voice-settings-dialog-lazy.tsx` (new) - `'use client'` wrapper around `VoiceSettingsDialog`, forwards `tab` + `children`
- `src/components/calls/routing-chain-editor-lazy.tsx` (new) - `'use client'` wrapper around `RoutingChainEditor`, forwards `initial`/`members`/`destinations`
- `src/components/calls/call-detail-human.tsx` - imports and renders `CallWaveformPlayerLazy` instead of `CallWaveformPlayer` directly
- `src/app/(dashboard)/calls/(hub)/page.tsx` - imports and renders `CallDetailDialogLazy`, `VoiceSettingsDialogLazy`, `RoutingChainEditorLazy` instead of the direct components in the `CallDetail`, `VoiceSettings`, and `RoutingTab` inline Server Component functions; conditional-render logic (`callId && ...`, `settingsTab && canManage && ...`) unchanged

## Decisions Made
None beyond what's documented above — plan's prescribed pattern (confetti-burst.tsx-style wrappers) followed exactly.

## Deviations from Plan

None - plan executed exactly as written. One incidental note: the live `page.tsx`'s `CallDetail` function signature (`{ id }`, calling `getUnifiedCall(id)` directly) differs slightly from the plan's `<interfaces>` excerpt (`{ id, rows }`), but this is a documentation artifact of the plan's context snapshot, not something the task actions depended on — only import paths and JSX component names were swapped, and those matched exactly.

## Self-Check: PASSED

Verified created files exist and commits exist (see below).
