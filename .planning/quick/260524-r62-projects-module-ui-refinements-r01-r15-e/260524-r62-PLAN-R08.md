---
phase: 260524-r62-projects-module-ui-refinements
plan: R08
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1044_project_folders.sql
  - src/types/database.ts
  - src/app/(dashboard)/projects/_actions/folders.ts
  - src/app/(dashboard)/projects/actions.ts
  - src/components/projects/new-folder-button.tsx
  - src/components/projects/projects-list-folders.tsx
  - src/app/(dashboard)/projects/page.tsx
  - src/app/(dashboard)/projects/trash/page.tsx
  - src/app/(dashboard)/projects/trash/trash-actions.tsx
autonomous: true
requirements:
  - R08

must_haves:
  truths:
    - "Users can create named folders on /projects and drag-and-drop project rows between folders (and within a folder to reorder)"
    - "Each project row is a single-line list item with NAME / STATUS / UPDATED columns (no card grid)"
    - "The toolbar shows + Project (LEFT), Folder (LEFT), Show archived toggle (RIGHT), Trash link (RIGHT) — preserving R01 left placement of + Project"
    - "Soft-archive and soft-delete columns exist on projects and are wired so /projects/trash lists soft-deleted projects with restore + hard-delete + empty-trash"
    - "All folder + project lifecycle mutations are server actions, RLS-scoped via get_current_org_id()"
    - "Each concern lands as its own atomic git commit named feat(projects-ui): R08-<n> - <desc>"
    - "supabase/migrations/1044_project_folders.sql is applied to the remote DB via a one-shot .tmp-apply-XXXX.cjs psql script (DATABASE_URL from .env.local) and tracked in supabase_migrations.schema_migrations; the temp script is deleted after success"
    - "npm run build exits 0 after the final task"
  artifacts:
    - path: "supabase/migrations/1044_project_folders.sql"
      provides: "project_folders table (id, org_id, name, color, icon, parent_id self-FK, position, created_by, created_at, updated_at, UNIQUE(org_id, parent_id, name)) + RLS + index + updated_at trigger; ALTER projects ADD folder_id (FK SET NULL) + position + archived_at + deleted_at; partial indexes for folder, archived, deleted"
      contains: "CREATE TABLE project_folders"
    - path: "src/types/database.ts"
      provides: "ProjectFolderRow interface + ProjectRow extended with folder_id, position, archived_at, deleted_at fields"
      contains: "ProjectFolderRow"
    - path: "src/app/(dashboard)/projects/_actions/folders.ts"
      provides: "Server actions: listFolders, createFolder, renameFolder, updateFolderMeta, reorderFolders, moveFolder, archiveFolder, deleteFolder — direct port of workflows/_actions/folders.ts with table name swap"
      contains: "project_folders"
    - path: "src/app/(dashboard)/projects/actions.ts"
      provides: "Adds moveProjectToFolder, reorderProjectsInFolder, archiveProject, unarchiveProject, softDeleteProject, restoreProjectFromTrash, hardDeleteProject, emptyProjectsTrash actions; getProjects updated to filter out deleted_at IS NOT NULL by default and respect includeArchived flag"
      contains: "softDeleteProject"
    - path: "src/components/projects/new-folder-button.tsx"
      provides: "Client dialog button that calls createFolder server action and router.refresh() — adapted copy of workflows/new-folder-button.tsx"
      contains: "createFolder"
    - path: "src/components/projects/projects-list-folders.tsx"
      provides: "Main DnD list component: DndContext + PointerSensor + TouchSensor, Unfiled group + sortable folder groups, sortable project rows, three-column row layout (NAME / STATUS / UPDATED), row [...] menu with Move to folder / Archive / Delete, folder header [...] menu with Rename / Delete, optimistic local state, DragOverlay portal"
      contains: "DndContext"
    - path: "src/app/(dashboard)/projects/page.tsx"
      provides: "Replaces card grid with ProjectsListFolders; toolbar = + Project + Folder (LEFT) and Show archived + Trash (RIGHT); preserves R01 left placement; loads projects + folders + trash count in parallel; supports ?archived=1 query param"
      contains: "ProjectsListFolders"
    - path: "src/app/(dashboard)/projects/trash/page.tsx"
      provides: "Trash listing page: lists soft-deleted projects with restore + hard-delete + empty-trash buttons; mirrors workflows/trash/page.tsx"
      contains: "deleted_at"
    - path: "src/app/(dashboard)/projects/trash/trash-actions.tsx"
      provides: "Client buttons for restore / hard-delete / empty-trash that call the new project trash server actions"
      contains: "restoreProjectFromTrash"
  key_links:
    - from: "git log feat/projects-ui-refinements"
      to: "5 atomic commits matching ^(feat|fix|chore)\\(projects-ui\\): R08-[1-5] -"
      via: "one commit per task, never squashed"
      pattern: "R08-\\d"
    - from: "src/components/projects/projects-list-folders.tsx"
      to: "src/app/(dashboard)/projects/_actions/folders.ts AND src/app/(dashboard)/projects/actions.ts"
      via: "named imports of createFolder, renameFolder, deleteFolder, reorderFolders, moveProjectToFolder, reorderProjectsInFolder, archiveProject, softDeleteProject"
      pattern: "from ['\"]@/app/\\(dashboard\\)/projects/(_actions/folders|actions)['\"]"
    - from: "src/app/(dashboard)/projects/page.tsx"
      to: "src/components/projects/projects-list-folders.tsx"
      via: "<ProjectsListFolders projects={...} folders={...} /> replaces the old card grid"
      pattern: "ProjectsListFolders"
    - from: "remote Supabase schema"
      to: "supabase/migrations/1044_project_folders.sql"
      via: "psql connection (DATABASE_URL) confirms public.project_folders exists AND public.projects has columns folder_id, position, archived_at, deleted_at"
      pattern: "project_folders"
---

<objective>
R08 — Add a Workflows-style folder + drag-and-drop listing to the Projects module, plus the underlying soft-lifecycle (archive + trash) schema. Replace the current `/projects` card grid with a single-line list grouped by collapsible folders. Toolbar mirrors `/workflows` (no Logs button). Implement as 5 atomic commits so each concern can be reviewed and reverted independently.

Purpose: bring Projects to parity with the Workflows module so users can organize many projects, hide stale ones, and recover deleted ones — without inventing a new UX pattern.

Output: a new migration (`1044_project_folders.sql`), `_actions/folders.ts`, extended `actions.ts`, two new components, a rewritten `/projects` page, a `/projects/trash` page, and updated `database.ts` types. Migration is pushed to remote via the same `.tmp-apply-XXXX.cjs` psql-script pattern used in this branch's earlier work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@supabase/migrations/100_workflow_folders.sql
@src/app/(dashboard)/workflows/page.tsx
@src/components/workflows/workflows-list.tsx
@src/components/workflows/new-folder-button.tsx
@src/app/(dashboard)/workflows/_actions/folders.ts
@src/app/(dashboard)/workflows/_actions/workflows.ts
@src/app/(dashboard)/workflows/trash/page.tsx
@src/app/(dashboard)/projects/page.tsx
@src/app/(dashboard)/projects/actions.ts
@src/types/database.ts
</context>

<interfaces>
<!-- Key existing types the executor will touch. Extract from src/types/database.ts -->

From src/types/database.ts (currently):
```ts
export interface ProjectRow {
  id: string
  org_id: string
  name: string
  description: string | null
  color: string | null
  created_by: string | null
  created_at: string
  updated_at: string
}
```

After Task 2 it MUST become:
```ts
export interface ProjectRow {
  id: string
  org_id: string
  name: string
  description: string | null
  color: string | null
  folder_id: string | null         // NEW (R08)
  position: number                 // NEW (R08)
  archived_at: string | null       // NEW (R08)
  deleted_at: string | null        // NEW (R08)
  created_by: string | null
  created_at: string
  updated_at: string
}

export interface ProjectFolderRow {  // NEW (R08)
  id: string
  org_id: string
  name: string
  color: string | null
  icon: string | null
  parent_id: string | null
  position: number
  created_by: string | null
  created_at: string
  updated_at: string
}
```

Server action result shape (port from workflows/_actions):
```ts
type ActionResult<T = void> =
  | { ok: true; data: T }
  | { ok: false; error: string }
```

Server action signatures the executor MUST create (folders.ts):
```ts
listFolders(): Promise<ActionResult<ProjectFolderRow[]>>
createFolder(input: { name: string; color?: string|null; icon?: string|null; parent_id?: string|null }): Promise<ActionResult<ProjectFolderRow>>
renameFolder(id: string, input: { name: string }): Promise<ActionResult<void>>
updateFolderMeta(id: string, input: { color?: string|null; icon?: string|null }): Promise<ActionResult<void>>
reorderFolders(orderedIds: string[]): Promise<ActionResult<void>>
moveFolder(id: string, parent_id: string|null): Promise<ActionResult<void>>
archiveFolder(id: string): Promise<ActionResult<void>>
deleteFolder(id: string, opts?: { cascadeChildren?: boolean }): Promise<ActionResult<void>>
```

Server action signatures to add to actions.ts:
```ts
moveProjectToFolder(projectId: string, folderId: string|null): Promise<ActionResult<void>>
reorderProjectsInFolder(folderId: string|null, orderedIds: string[]): Promise<ActionResult<void>>
archiveProject(projectId: string): Promise<ActionResult<void>>
unarchiveProject(projectId: string): Promise<ActionResult<void>>
softDeleteProject(projectId: string): Promise<ActionResult<void>>
restoreProjectFromTrash(projectId: string): Promise<ActionResult<void>>
hardDeleteProject(projectId: string): Promise<ActionResult<void>>
emptyProjectsTrash(): Promise<ActionResult<{ count: number }>>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task R08-1: Migration 1044 — project_folders table + projects lifecycle columns</name>
  <files>supabase/migrations/1044_project_folders.sql</files>
  <read_first>
    - supabase/migrations/100_workflow_folders.sql (the reference migration to PORT — copy structure, swap workflow→project)
    - supabase/migrations/1043_project_task_assignee.sql (last project migration; confirm style/syntax used in this codebase)
    - CLAUDE.md "Database" section (never edit old migrations; new files only)
  </read_first>
  <action>
    Create `supabase/migrations/1044_project_folders.sql` as a direct port of `supabase/migrations/100_workflow_folders.sql` with EVERY identifier `workflow*` replaced by `project*`. Specifically:

    1. CREATE TABLE `project_folders` with columns: id (UUID PK gen_random_uuid()), org_id (UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE), name TEXT NOT NULL, color TEXT, icon TEXT, parent_id UUID REFERENCES project_folders(id) ON DELETE CASCADE, position INTEGER NOT NULL DEFAULT 0, created_by UUID REFERENCES auth.users(id), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE(org_id, parent_id, name).

    2. ALTER TABLE project_folders ENABLE ROW LEVEL SECURITY.

    3. CREATE POLICY "project_folders org members" ON project_folders USING (org_id = get_current_org_id()) WITH CHECK (org_id = get_current_org_id()).

    4. CREATE INDEX project_folders_org_parent_idx ON project_folders(org_id, parent_id).

    5. ALTER TABLE projects ADD COLUMN IF NOT EXISTS folder_id UUID REFERENCES project_folders(id) ON DELETE SET NULL, ADD COLUMN IF NOT EXISTS position INTEGER NOT NULL DEFAULT 0, ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ, ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ.

    6. CREATE INDEX IF NOT EXISTS projects_folder_idx ON projects(folder_id) WHERE deleted_at IS NULL.
       CREATE INDEX IF NOT EXISTS projects_archived_idx ON projects(org_id, archived_at) WHERE deleted_at IS NULL.
       CREATE INDEX IF NOT EXISTS projects_deleted_idx ON projects(org_id, deleted_at) WHERE deleted_at IS NOT NULL.

    7. CREATE OR REPLACE FUNCTION touch_project_folder_updated_at() returning TRIGGER (NEW.updated_at = now(); RETURN NEW;).
       DROP TRIGGER IF EXISTS trg_project_folders_touch ON project_folders.
       CREATE TRIGGER trg_project_folders_touch BEFORE UPDATE ON project_folders FOR EACH ROW EXECUTE FUNCTION touch_project_folder_updated_at().

    Header comment: "-- R08: Project folders, archive, and trash. Port of migration 100 (workflow folders) for the projects module."

    THEN apply to remote using the same `.tmp-apply-XXXX.cjs` pattern used by prior tasks on this branch:
    - Create `.tmp-apply-1044.cjs` at repo root: read DATABASE_URL from `.env.local`, connect with `pg` Client, run the entire SQL file (`fs.readFileSync('supabase/migrations/1044_project_folders.sql','utf8')`), then INSERT into `supabase_migrations.schema_migrations` (version='1044', name='project_folders', statements=[]) ON CONFLICT DO NOTHING, then log success and exit.
    - Run with `node .tmp-apply-1044.cjs`.
    - Delete `.tmp-apply-1044.cjs` after success.
    - Do NOT add `.tmp-apply-1044.cjs` to git.

    Commit:
      git add supabase/migrations/1044_project_folders.sql
      git commit -m "feat(projects-ui): R08-1 - project_folders table + projects lifecycle columns (migration 1044)"
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/1044_project_folders.sql` exists.
    - grep `CREATE TABLE project_folders` returns 1 match in the file.
    - grep `ALTER TABLE projects` AND `folder_id` AND `archived_at` AND `deleted_at` AND `position` all return matches in the file.
    - grep `get_current_org_id` returns at least one match (RLS policy).
    - grep `touch_project_folder_updated_at` returns 2 matches (function + trigger EXECUTE).
    - psql query `SELECT to_regclass('public.project_folders')` returns NOT NULL.
    - psql query `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='projects' AND column_name IN ('folder_id','position','archived_at','deleted_at')` returns 4 rows.
    - `.tmp-apply-1044.cjs` does NOT exist after the task (deleted).
    - git log -1 --pretty=%s matches `^feat\(projects-ui\): R08-1 -`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {Client}=require('pg');require('dotenv').config({path:'.env.local'});(async()=>{const c=new Client({connectionString:process.env.DATABASE_URL});await c.connect();const t=await c.query(\"SELECT to_regclass('public.project_folders') AS t\");const cols=await c.query(\"SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='projects' AND column_name IN ('folder_id','position','archived_at','deleted_at') ORDER BY column_name\");console.log({table:t.rows[0].t,cols:cols.rows.map(r=>r.column_name)});await c.end();})().catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>
    Migration file committed; remote DB has `project_folders` table and the 4 new `projects` columns; tracking row present in `supabase_migrations.schema_migrations`; temp psql script deleted.
  </done>
</task>

<task type="auto">
  <name>Task R08-2: Extend database.ts types + add folder/lifecycle server actions</name>
  <files>
    src/types/database.ts
    src/app/(dashboard)/projects/_actions/folders.ts
    src/app/(dashboard)/projects/actions.ts
  </files>
  <read_first>
    - src/app/(dashboard)/workflows/_actions/folders.ts (THE template to port — copy verbatim with `workflow_folders` → `project_folders`, `/workflows` → `/projects`)
    - src/app/(dashboard)/workflows/_actions/workflows.ts (THE template for the move/reorder/archive/trash actions — port the 8 actions into projects/actions.ts)
    - src/app/(dashboard)/projects/actions.ts (CURRENT — to know where to insert the new actions and how the existing `db(supabase)` cast helper works)
    - src/types/database.ts lines 73-82 (current ProjectRow shape)
  </read_first>
  <action>
    PART A — `src/types/database.ts`:
    1. Extend `ProjectRow` interface by adding (in order, before `created_by`): `folder_id: string | null`, `position: number`, `archived_at: string | null`, `deleted_at: string | null`.
    2. Add a NEW interface immediately after `ProjectRow`:
       ```ts
       export interface ProjectFolderRow {
         id: string
         org_id: string
         name: string
         color: string | null
         icon: string | null
         parent_id: string | null
         position: number
         created_by: string | null
         created_at: string
         updated_at: string
       }
       ```

    PART B — `src/app/(dashboard)/projects/_actions/folders.ts` (NEW file, NEW directory):
    Port `src/app/(dashboard)/workflows/_actions/folders.ts` verbatim with these substitutions ONLY:
      - `workflow_folders` → `project_folders`
      - `workflows` (when used as a table name inside cascade helpers) → `projects`
      - `WorkflowFolderRow` → `ProjectFolderRow`
      - `revalidatePath('/workflows')` → `revalidatePath('/projects')`
      - Import path `import type { Database } from '@/types/database'` → `import type { ProjectFolderRow } from '@/types/database'` (use the named interface directly; the projects tables aren't in the Supabase Database type — same pattern as existing actions.ts)
      - Use the same `db(supabase)` cast helper from existing actions.ts (copy a local copy at the top of folders.ts: `function db(s:any){return s as any}` with the same eslint-disable comments; OR import from a shared helper if you prefer, but do NOT change the rest of actions.ts).
    Keep ALL 8 exported functions: listFolders, createFolder, renameFolder, updateFolderMeta, reorderFolders, moveFolder, archiveFolder, deleteFolder. Keep the `collectDescendantFolderIds` private helper.

    PART C — `src/app/(dashboard)/projects/actions.ts` (APPEND, do not split):
    Add a new section "// ─── Project folders + lifecycle (R08) ─────────────────────" at the end of the file with 8 new exported async functions that are direct ports of `workflows/_actions/workflows.ts`:
      - `moveProjectToFolder(projectId, folderId)` — port of moveWorkflowToFolder; uses table `projects`, column `folder_id`, append at tail by position; revalidatePath('/projects').
      - `reorderProjectsInFolder(folderId, orderedIds)` — port of reorderWorkflowsInFolder; updates `position` in batch.
      - `archiveProject(projectId)` — port of archiveWorkflow MINUS the `is_active` guard (projects have no is_active column); set `archived_at = now()`.
      - `unarchiveProject(projectId)` — set archived_at = null.
      - `softDeleteProject(projectId)` — set `deleted_at = now()`; revalidatePath('/projects') and '/projects/trash'.
      - `restoreProjectFromTrash(projectId)` — set deleted_at = null.
      - `hardDeleteProject(projectId)` — defense-in-depth: SELECT deleted_at first; refuse if NULL with `error: 'must_be_in_trash'`; then DELETE with `.not('deleted_at','is',null)`.
      - `emptyProjectsTrash()` — fetch org_id from get_current_org_id RPC; SELECT id FROM projects WHERE deleted_at IS NOT NULL; DELETE IN those ids with the same `.not('deleted_at','is',null)` guard; return `{ count }`.
    Use the existing `db(supabase)` helper. Use the existing `ActionResult<T>` style (DEFINE it at the top of the new section if not already present in the file — check first; if absent, add `type ActionResult<T = void> = { ok: true; data: T } | { ok: false; error: string }` near the top of the file in the imports area).

    ALSO modify the existing `getProjects()` function (top of actions.ts) to accept an optional `opts?: { includeArchived?: boolean; includeDeleted?: boolean }` argument and:
      - Always filter `.is('deleted_at', null)` unless `includeDeleted` is true.
      - When `includeArchived` is false (default), filter `.is('archived_at', null)`.
      - Order by `position` ascending, then `created_at` descending (so manual ordering wins; new projects without manual order sort newest first).

    Commit:
      git add src/types/database.ts src/app/(dashboard)/projects/_actions/folders.ts src/app/(dashboard)/projects/actions.ts
      git commit -m "feat(projects-ui): R08-2 - project folder + lifecycle server actions"
  </action>
  <acceptance_criteria>
    - grep `ProjectFolderRow` in src/types/database.ts returns >= 1 match.
    - grep `folder_id` AND `archived_at` AND `deleted_at` AND `position` in the ProjectRow interface block of src/types/database.ts all return matches.
    - File `src/app/(dashboard)/projects/_actions/folders.ts` exists.
    - grep `^export async function createFolder` AND `renameFolder` AND `reorderFolders` AND `archiveFolder` AND `deleteFolder` in folders.ts return matches.
    - grep `project_folders` in folders.ts returns >= 5 matches.
    - grep `workflow_folders` in folders.ts returns 0 matches.
    - grep `softDeleteProject` AND `hardDeleteProject` AND `emptyProjectsTrash` AND `moveProjectToFolder` AND `reorderProjectsInFolder` AND `archiveProject` in src/app/(dashboard)/projects/actions.ts all return matches.
    - `npm run build` exits 0 (type-check passes — the type extension is the key risk).
    - git log -1 --pretty=%s matches `^feat\(projects-ui\): R08-2 -`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Types extended, 16 new server actions present and type-check clean, atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task R08-3: NewFolderButton component + ProjectsListFolders (DnD list)</name>
  <files>
    src/components/projects/new-folder-button.tsx
    src/components/projects/projects-list-folders.tsx
  </files>
  <read_first>
    - src/components/workflows/new-folder-button.tsx (port verbatim)
    - src/components/workflows/workflows-list.tsx (THE big port — read in full; this is ~1100 lines)
    - src/components/projects/project-board.tsx (to confirm the codebase's existing @dnd-kit + TouchSensor patterns; preserve TouchSensor for P11 mobile polish)
    - src/components/projects/project-list.tsx (current list view — borrow row layout idioms if useful)
  </read_first>
  <action>
    PART A — `src/components/projects/new-folder-button.tsx` (NEW):
    Port `src/components/workflows/new-folder-button.tsx` verbatim with substitutions:
      - import `createFolder` from `@/app/(dashboard)/projects/_actions/folders` (NOT workflows).
      - Dialog description: change "Group related workflows together. You can move workflows in from each row's menu." → "Group related projects together. You can move projects in from each row's menu."
      - Component name stays `NewFolderButton`.

    PART B — `src/components/projects/projects-list-folders.tsx` (NEW):
    Adapt `src/components/workflows/workflows-list.tsx` to the project domain. Strategy: copy-paste then trim. Concretely:

    1. Imports:
       - Same @dnd-kit imports.
       - Same lucide + phosphor icons EXCEPT REMOVE the trigger-specific icons (CalendarBlank, ClockCountdown, CursorClick, FlowArrow, Lightning, WebhooksLogo) — projects don't have triggers. Keep ChevronRight, Folder/FolderOpen, MoreHorizontal, Archive, Trash2, FolderInput, Pencil, GripVertical.
       - Server actions: import `archiveProject, moveProjectToFolder, reorderProjectsInFolder, softDeleteProject` from `@/app/(dashboard)/projects/actions` (NOT _actions/projects) and `deleteFolder, renameFolder, reorderFolders` from `@/app/(dashboard)/projects/_actions/folders`.
       - ADD: import `useSensor, TouchSensor` from @dnd-kit/core (preserve P11 mobile polish) and include in the sensors array alongside PointerSensor with `activationConstraint: { delay: 250, tolerance: 8 }`.

    2. Types:
       ```ts
       interface ProjectSummary {
         id: string
         name: string
         description: string | null
         color: string | null
         folder_id: string | null
         archived_at: string | null
         updated_at: string
       }
       interface ProjectFolder {
         id: string
         org_id: string
         name: string
         color: string | null
         icon: string | null
         parent_id: string | null
         position: number
         created_by: string | null
         created_at: string
         updated_at: string
       }
       interface Props { projects: ProjectSummary[]; folders?: ProjectFolder[] }
       ```
       DELETE the `TRIGGER_META` map and the `triggerLabel()` helper — not applicable to projects.

    3. Main component: rename `WorkflowsList` → `ProjectsListFolders`. Keep DndContext, sensors, collisionDetectionStrategy, groups memo, handleDragStart, handleDragOver, handleDragEnd EXACTLY as the workflows version with these symbol swaps:
       - `WorkflowSummary` → `ProjectSummary`, `WorkflowFolder` → `ProjectFolder`
       - `localWorkflows` → `localProjects`, `workflows` → `projects` (prop and state)
       - `moveWorkflowToFolder` → `moveProjectToFolder`
       - `reorderWorkflowsInFolder` → `reorderProjectsInFolder`
       - `archiveWorkflow` → `archiveProject`
       - `softDeleteWorkflow` → `softDeleteProject`
       - All `revalidatePath`/route refs '/workflows' → '/projects'.
       - DragData type variant `"workflow"` → `"project"`.

    4. Row rendering: REPLACE the workflow row (which has Trigger column + WorkflowToggle + kind badge) with a Project row that has THREE columns:
       - LEFT (flex-1 min-w-0): drag-handle (GripVertical) + color dot (12px circle, background = project.color ?? '#6366f1') + name (font-medium truncate) + description excerpt (text-xs text-text-tertiary truncate, optional).
       - MIDDLE (w-24 shrink-0): STATUS — show "Archived" pill with Archive icon if `archived_at` is set, otherwise "Active" (subtle, text-text-tertiary).
       - RIGHT (w-28 text-right shrink-0): UPDATED — `formatDistanceToNow(parseISO(project.updated_at), { addSuffix: true })`.
       - FAR RIGHT: row [...] dropdown menu (DropdownMenu) with items: "Move to folder" (DropdownMenuSub listing localFolders + "Unfiled"), Archive/Unarchive (toggle based on archived_at), DropdownMenuSeparator, "Move to trash" (destructive).
       - The whole row is wrapped in `<Link href={\`/projects/\${project.id}\`}>` for navigation, EXCEPT the drag handle and the [...] menu which use stopPropagation. Use the same `<SortableProjectRow>` pattern as `<SortableWorkflowRow>` in the reference.

    5. KEEP the UnfiledGroup, FolderGroup, GroupBody, FolderMenu, RenameFolderDialog, DeleteFolderDialog, WorkflowDragPreview (rename to ProjectDragPreview), FolderDragPreview, MoveToFolderSubmenu structures from workflows-list.tsx, adjusting copy ("workflow"→"project") and removing trigger-specific UI.

    6. Empty state: when both folders.length === 0 AND projects.length === 0, render the same Card-based empty state as the current `projects/page.tsx` ("You don't have any projects yet" + a "+ Project" button). Use the EXISTING `NewProjectDialog` component (`@/components/projects/new-project-dialog`) — do NOT duplicate it.

    DO NOT touch any other project component files. DO NOT touch workflows files. DO NOT add new dependencies.

    Commit:
      git add src/components/projects/new-folder-button.tsx src/components/projects/projects-list-folders.tsx
      git commit -m "feat(projects-ui): R08-3 - NewFolderButton + ProjectsListFolders (DnD + folders)"
  </action>
  <acceptance_criteria>
    - Files `src/components/projects/new-folder-button.tsx` and `src/components/projects/projects-list-folders.tsx` exist.
    - grep `export function NewFolderButton` in new-folder-button.tsx returns 1 match.
    - grep `createFolder` in new-folder-button.tsx returns >= 1 match and the import path includes `projects/_actions/folders`.
    - grep `export function ProjectsListFolders` in projects-list-folders.tsx returns 1 match.
    - grep `DndContext` AND `SortableContext` AND `useSortable` AND `DragOverlay` AND `TouchSensor` AND `PointerSensor` in projects-list-folders.tsx all return matches.
    - grep `moveProjectToFolder` AND `reorderProjectsInFolder` AND `archiveProject` AND `softDeleteProject` in projects-list-folders.tsx all return matches.
    - grep `TRIGGER_META` AND `triggerLabel` AND `WorkflowToggle` in projects-list-folders.tsx all return ZERO matches (trigger UI deliberately removed).
    - grep `formatDistanceToNow` in projects-list-folders.tsx returns >= 1 match (UPDATED column).
    - `npm run build` exits 0.
    - git log -1 --pretty=%s matches `^feat\(projects-ui\): R08-3 -`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both client components compile clean, ProjectsListFolders matches the workflows-list shape (folders + DnD + row menu), no trigger-specific UI bled over.
  </done>
</task>

<task type="auto">
  <name>Task R08-4: Rewrite /projects page (toolbar + folder-aware list, preserve R01)</name>
  <files>
    src/app/(dashboard)/projects/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/workflows/page.tsx (THE toolbar layout to mirror, MINUS the Logs button)
    - src/app/(dashboard)/projects/page.tsx (CURRENT — confirm + Project is currently LEFT-aligned per R01)
    - src/components/projects/new-project-dialog.tsx (existing dialog — keep using it for the + Project button)
    - src/components/projects/projects-list-folders.tsx (from Task R08-3 — the new list)
    - src/components/projects/new-folder-button.tsx (from Task R08-3 — the new folder button)
  </read_first>
  <action>
    REPLACE the body of `src/app/(dashboard)/projects/page.tsx` end-to-end with a folder-aware page modeled on `src/app/(dashboard)/workflows/page.tsx`. Specifically:

    1. Make the component `async` and accept `PageProps { searchParams: Promise<{ archived?: string }> }`. Destructure `archived` from awaited searchParams; `includeArchived = archived === '1'`.

    2. In parallel (Promise.all):
       - Fetch projects via the existing `getProjects({ includeArchived })` server action (now accepts the opts arg per Task R08-2). DO NOT add a new fetcher.
       - Fetch folders directly: `await createClient()` then `supabase.from('project_folders').select('*').order('position').order('created_at')`. Use the same `db(s as any)` cast as actions.ts uses if TS complains.
       - Fetch trash count: `supabase.from('projects').select('id', { count: 'exact', head: true }).not('deleted_at','is',null)`.

    3. Render with `PageContainer` (from `@/components/layout/page-header`). Toolbar div (responsive — copy the sm:flex / sm:hidden split from workflows page) MUST have:
       - LEFT cluster: `<NewProjectDialog><Button size="sm" className="h-8"><Plus className="h-4 w-4 mr-1.5" />Project</Button></NewProjectDialog>` immediately followed by `<NewFolderButton className="h-8" />`. PRESERVE the LEFT alignment of the + Project button — this is the R01 fix and MUST NOT regress.
       - RIGHT cluster (hidden lg:flex items-center gap-2): "Show archived" / "Hide archived" toggle Button (asChild Link to `/projects` or `/projects?archived=1`, with Archive icon), then Trash link Button (asChild Link to `/projects/trash`, with Trash2 icon and the trashCount badge). NO Logs button.
       - Mobile (lg:hidden) DropdownMenu with the same two right-cluster items (Show archived, Trash), mirroring the workflows mobile menu. NO Logs item.

    4. Below the toolbar, in the existing px-4 sm:px-6 lg:px-8 pb-2 wrapper, render:
       `<ProjectsListFolders projects={projects} folders={folders} />`

    5. KEEP the existing empty-state behavior INSIDE ProjectsListFolders (already added in R08-3). Do NOT duplicate it on the page.

    6. REMOVE: the old `<Card>` grid loop, the `<Suspense>` wrapper with the 6-skeleton card array, the standalone `ProjectsList` helper function, and the `FolderKanban` import (no longer used here).

    7. Imports the rewritten page needs:
       - `import Link from 'next/link'`
       - `import { Plus, Archive, Trash2, MoreHorizontal } from 'lucide-react'`
       - `import { createClient } from '@/lib/supabase/server'`
       - `import { Button } from '@/components/ui/button'`
       - `import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from '@/components/ui/dropdown-menu'`
       - `import { Tooltip, TooltipContent, TooltipTrigger } from '@/components/ui/tooltip'`
       - `import { PageContainer } from '@/components/layout/page-header'`
       - `import { getProjects } from './actions'`
       - `import { NewProjectDialog } from '@/components/projects/new-project-dialog'`
       - `import { NewFolderButton } from '@/components/projects/new-folder-button'`
       - `import { ProjectsListFolders } from '@/components/projects/projects-list-folders'`

    Commit:
      git add src/app/(dashboard)/projects/page.tsx
      git commit -m "feat(projects-ui): R08-4 - folder-aware /projects page with Show archived + Trash toolbar"
  </action>
  <acceptance_criteria>
    - grep `ProjectsListFolders` in src/app/(dashboard)/projects/page.tsx returns >= 1 match.
    - grep `NewFolderButton` in src/app/(dashboard)/projects/page.tsx returns >= 1 match.
    - grep `Show archived` AND `Trash` in src/app/(dashboard)/projects/page.tsx return matches.
    - grep `FolderKanban` AND `Suspense` AND `<Card` in src/app/(dashboard)/projects/page.tsx return ZERO matches (the old grid is gone).
    - grep `NewProjectDialog` in src/app/(dashboard)/projects/page.tsx returns >= 1 match AND it appears BEFORE `NewFolderButton` in the file (preserves R01 left placement). One way to check: line number of first `NewProjectDialog` < line number of first `NewFolderButton`.
    - grep `Logs` in src/app/(dashboard)/projects/page.tsx returns ZERO matches.
    - grep `includeArchived` in src/app/(dashboard)/projects/page.tsx returns >= 1 match (Show archived wired through).
    - `npm run build` exits 0.
    - `npm run dev` (manual sanity, not automated): /projects renders folders + projects, + Folder button creates a folder, dragging a project into the folder persists across refresh. (Spot-check in next task or skip — atomic commit gate is build green.)
    - git log -1 --pretty=%s matches `^feat\(projects-ui\): R08-4 -`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && node -e "const fs=require('fs');const s=fs.readFileSync('src/app/(dashboard)/projects/page.tsx','utf8');const a=s.indexOf('NewProjectDialog');const b=s.indexOf('NewFolderButton');if(a<0||b<0||a>b){console.error('R01 LEFT placement regressed: NewProjectDialog must appear before NewFolderButton');process.exit(1)}console.log('R01 left placement preserved')"</automated>
  </verify>
  <done>
    /projects page replaced; toolbar matches Workflows pattern minus Logs; R01 left placement preserved; build green; atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task R08-5: /projects/trash route + final build verification</name>
  <files>
    src/app/(dashboard)/projects/trash/page.tsx
    src/app/(dashboard)/projects/trash/trash-actions.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/workflows/trash/page.tsx (the template — mirror exactly with project terminology)
    - src/app/(dashboard)/workflows/trash/trash-actions.tsx (read with the Read tool — port to projects; the file exists at this path per ls)
    - src/app/(dashboard)/projects/actions.ts (confirm restoreProjectFromTrash, hardDeleteProject, emptyProjectsTrash exist from R08-2)
  </read_first>
  <action>
    PART A — `src/app/(dashboard)/projects/trash/page.tsx` (NEW):
    Port `src/app/(dashboard)/workflows/trash/page.tsx` verbatim with substitutions:
      - Title: "Workflows" → "Projects"; description: "Workflows here are hidden from triggers and agents." → "Projects here are hidden from the main list."
      - Table query: `.from('workflows').select('id, name, kind, trigger_type, deleted_at')` → `.from('projects').select('id, name, color, deleted_at')`.
      - Type `TrashedWorkflow` → `TrashedProject` with fields `{ id: string; name: string; color: string | null; deleted_at: string }`.
      - Back link: `/workflows` → `/projects`.
      - Row sub-line: "w.kind · w.trigger_type" → render a small color dot using `w.color ?? '#6366f1'`.
      - `<TrashRowActions workflowId={w.id} name={w.name} />` → `<TrashRowActions projectId={w.id} name={w.name} />`.

    PART B — `src/app/(dashboard)/projects/trash/trash-actions.tsx` (NEW):
    Port `src/app/(dashboard)/workflows/trash/trash-actions.tsx` verbatim with substitutions:
      - Server action imports → `restoreProjectFromTrash, hardDeleteProject, emptyProjectsTrash` from `@/app/(dashboard)/projects/actions`.
      - Prop name `workflowId` → `projectId`.
      - Function name `restoreWorkflowFromTrash` → `restoreProjectFromTrash` (in action calls).
      - Function name `hardDeleteWorkflow` → `hardDeleteProject`.
      - Function name `emptyTrash` → `emptyProjectsTrash`.
      - Toast copy: "workflow" → "project" everywhere.
      - Component names: `TrashRowActions` stays the same name; `EmptyTrashButton` stays the same name.

    PART C — Final build + commit:
    Run `npm run build` and confirm exit 0.

    Commit:
      git add src/app/(dashboard)/projects/trash/page.tsx src/app/(dashboard)/projects/trash/trash-actions.tsx
      git commit -m "feat(projects-ui): R08-5 - /projects/trash route with restore + hard delete + empty"
  </action>
  <acceptance_criteria>
    - Files `src/app/(dashboard)/projects/trash/page.tsx` and `src/app/(dashboard)/projects/trash/trash-actions.tsx` exist.
    - grep `TrashRowActions` AND `EmptyTrashButton` in trash-actions.tsx return matches.
    - grep `restoreProjectFromTrash` AND `hardDeleteProject` AND `emptyProjectsTrash` in trash-actions.tsx all return matches.
    - grep `from ['\"]@/app/\\(dashboard\\)/projects/actions['\"]` in trash-actions.tsx returns 1 match (server actions live in the existing actions.ts, not _actions/).
    - grep `workflows` in trash-actions.tsx returns ZERO matches (no leaks from the port).
    - grep `from('projects')` in page.tsx returns >= 1 match.
    - grep `Back to projects` in page.tsx returns 1 match.
    - `npm run build` exits 0.
    - `git log --oneline -5` shows 5 commits, all matching `^[a-f0-9]+ feat\(projects-ui\): R08-[1-5] -` in reverse order (R08-5 newest).
    - Final remote DB check (re-run R08-1's verify command): table + 4 columns still present.
    - git log -1 --pretty=%s matches `^feat\(projects-ui\): R08-5 -`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && git log --oneline -5 | grep -E "feat\(projects-ui\): R08-[1-5] -" | wc -l</automated>
  </verify>
  <done>
    /projects/trash route lives; restore / hard delete / empty trash all wire to the new server actions; `npm run build` exits 0; the branch has exactly 5 new commits R08-1 through R08-5; remote DB still shows project_folders + the 4 lifecycle columns.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks land:

1. **Schema (remote):**
   - `psql $DATABASE_URL -c "\d project_folders"` shows the table with all columns including the FK to organizations and self-FK on parent_id.
   - `psql $DATABASE_URL -c "\d projects"` shows folder_id, position, archived_at, deleted_at columns.
   - `psql $DATABASE_URL -c "SELECT version FROM supabase_migrations.schema_migrations WHERE version='1044'"` returns 1 row.

2. **Build:** `npm run build` exits 0 with no new type errors.

3. **Commits (local, NOT pushed):**
   `git log --oneline feat/projects-ui-refinements -10` shows 5 new commits at the top, each matching `^feat\(projects-ui\): R08-[1-5] -`. No squash, no amend.

4. **UI smoke (manual, optional but recommended before merging):**
   - `npm run dev`, visit http://localhost:4267/projects.
   - + Project button is on the LEFT (R01 preserved).
   - Click "Folder" → create folder named "Test" → folder appears as a collapsible group.
   - Drag a project row into the folder → it relocates; refresh page → still there.
   - Row [...] menu → "Move to trash" → project disappears from main list.
   - Click "Trash" link → trashed project listed; "Restore" brings it back; "Delete forever" removes it permanently.
   - "Show archived" toggle: archive a project via row menu, then toggle → archived appears greyed out.
   - No console errors. Mobile drag (Chrome devtools touch sim) still works thanks to TouchSensor.

5. **Scope guard:** `git diff --name-only main...feat/projects-ui-refinements` should show only files under `src/app/(dashboard)/projects/`, `src/components/projects/`, `supabase/migrations/`, `src/types/database.ts` (plus prior R01-R07/R09-R15 work). No workflows/ files touched.
</verification>

<success_criteria>
- 5 atomic commits on `feat/projects-ui-refinements`, none pushed.
- Migration 1044 applied to remote DB and tracked in `supabase_migrations.schema_migrations`.
- `npm run build` exits 0.
- `/projects` shows the new folder + DnD list with + Project on the LEFT and Folder next to it (R01 preserved); Show archived + Trash on the RIGHT; no Logs button.
- `/projects/trash` route exists and restores/permanently deletes soft-deleted projects.
- Drag-and-drop reorders within folders and moves between folders, with optimistic UI and server persistence.
- Zero edits to the Workflows module (`git diff --name-only` excludes `src/app/(dashboard)/workflows/**` and `src/components/workflows/**`).
- The `.tmp-apply-1044.cjs` script does not exist in the working tree or in any commit.
</success_criteria>

<output>
After completion, append a brief section to the existing `260524-r62-SUMMARY.md` (do NOT create a new SUMMARY file) under the heading `## R08 — Project listing folder structure with drag-and-drop` listing:
  - The 5 commit SHAs (short) and subjects
  - Remote DB verification result (table + columns present)
  - A note that the migration was applied via a one-shot psql script (deleted after success)
  - Any deviations from this plan (none expected)
</output>
