---
phase: 260704-pr3-performance-seed-048-fases-a-c-eliminar-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/calls/(hub)/page.tsx
  - src/app/(dashboard)/calls/actions.ts
  - src/components/calls/call-detail-ai.tsx
  - src/components/calls/call-detail-human.tsx
  - src/components/calls/call-detail-dialog.tsx
  - src/components/calls/unified-call-timeline.tsx
  - src/app/(dashboard)/calls/loading.tsx
  - src/app/(dashboard)/layout.tsx
  - src/lib/org/settings.ts
  - src/lib/rbac/server.ts
  - src/hooks/use-unread-count.ts
  - src/components/layout/sidebar.tsx
  - src/components/layout/org-switcher.tsx
  - src/components/layout/top-bar.tsx
autonomous: true
requirements: [SEED-048-A, SEED-048-C]
must_haves:
  truths:
    - "Clicking a call row opens the detail dialog without re-fetching data the hub already loaded (client-side lookup by id, server fetch only as fallback)"
    - "CallDetailAi and CallDetailHuman no longer block on a redundant full-row re-fetch before rendering"
    - "The dialog subtree does not re-render when unrelated search params change (e.g. timeline filters)"
    - "Call rows are memoized so unrelated parent re-renders don't cascade into every row"
    - "The dialog body shows a skeleton while its data resolves instead of a blank flash"
    - "Every dashboard page navigation resolves call_routing_chains, org settings, and RBAC context with no avoidable sequential round-trip"
    - "The org switcher dropdown opens instantly using a server-preloaded org list instead of a fresh fetch on first click"
    - "The sidebar's unread chat badge paints with a server-computed initial value instead of waiting on a client round-trip"
  artifacts:
    - path: "src/app/(dashboard)/calls/(hub)/page.tsx"
      provides: "CallDetail dialog reusing hub-fetched rows via client-side lookup, falling back to getUnifiedCall(id) only when not found"
    - path: "src/components/calls/call-detail-ai.tsx"
      provides: "Narrowed calls select + action_logs query parallelized using call.external_id (already the vapi_call_id) instead of sequential dependency"
    - path: "src/components/calls/call-detail-human.tsx"
      provides: "No redundant call_logs re-fetch — all rendered fields sourced from the already-fetched unified call row"
    - path: "src/components/calls/call-detail-dialog.tsx"
      provides: "close() and re-render dependency narrowed to the call search param only"
    - path: "src/components/calls/unified-call-timeline.tsx"
      provides: "UnifiedCallRow wrapped in React.memo keyed on id/updated_at-equivalent fields"
    - path: "src/app/(dashboard)/calls/loading.tsx"
      provides: "Loading state covers dialog body skeleton in addition to timeline skeleton"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "call_routing_chains folded into the existing Promise.all batch; org id/row reused instead of re-derived for getOrgSettings"
    - path: "src/lib/rbac/server.ts"
      provides: "getRbacContext() runs the platform_admins lookup and org_members lookup in parallel once org_id is resolved"
    - path: "src/hooks/use-unread-count.ts"
      provides: "Accepts an optional server-computed initialCount to seed state, keeping realtime subscription behavior unchanged"
    - path: "src/components/layout/org-switcher.tsx"
      provides: "Accepts a preloaded orgs list prop, falling back to lazy fetch only if not provided"
  key_links:
    - from: "src/app/(dashboard)/calls/(hub)/page.tsx"
      to: "src/components/calls/call-detail-ai.tsx / call-detail-human.tsx"
      via: "already-fetched UnifiedCallWithContact row passed through, no re-fetch of the same row"
      pattern: "getUnifiedCall"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/lib/org/settings.ts"
      via: "getOrgSettings receiving/reusing the org id or row already resolved by getActiveOrg()"
      pattern: "getOrgSettings"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/sidebar.tsx / top-bar.tsx"
      via: "server-fetched initialUnreadCount and orgs props threaded down to useUnreadCount/OrgSwitcher"
      pattern: "initialUnreadCount|preloadedOrgs"
---

<objective>
Execute SEED-048 Phase A (Calls detail duplicate-fetch waterfall) and Phase C (dashboard layout/auth parallelization) as a pure performance refactor. No visible behavior change, no schema changes, no UI/UX changes.

Purpose: The operator reported slow click-to-render, most visible on the Calls page but present dashboard-wide. Phase A eliminates 2-3 serialized round-trips per call-detail-dialog open by reusing data the hub already fetched. Phase C removes sequential auth/org/RBAC round-trips paid on every single page navigation.

Output: Calls detail dialog opens from already-loaded data (fallback fetch only if the row isn't in the current page), CallDetailAi/CallDetailHuman stop re-fetching what's already available, dialog re-renders are scoped to the relevant search param, call rows are memoized, a dialog-body loading skeleton exists, and the dashboard layout resolves org/RBAC/routing-chain state in parallel batches with server-preloaded initial values for the org switcher and unread-count badge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-048-system-performance-calls-page-latency.md
@CLAUDE.md

<interfaces>
<!-- unified_calls view (migration 063_unified_calls_view.sql) — confirms which
     fields on UnifiedCallWithContact already satisfy CallDetailAi/CallDetailHuman
     without re-fetching from `calls` / `call_logs`. -->

For AI calls (call_type === 'ai'):
- unified_calls.id            = calls.id
- unified_calls.external_id   = calls.vapi_call_id   (already available — no need to re-fetch `calls` to get vapi_call_id)
- unified_calls.notes         = calls.summary
- unified_calls.substatus     = calls.ended_reason
- unified_calls.started_at    = COALESCE(calls.started_at, calls.created_at)
- NOT in the view: calls.transcript_turns (must still be fetched from `calls` for the transcript timeline)

For human calls (call_type === 'human'):
- unified_calls.id                 = call_logs.id
- unified_calls.external_id        = call_logs.call_sid    (already available)
- unified_calls.recording_duration = call_logs.recording_duration (already available)
- unified_calls.ended_at           = call_logs.ended_at    (already available — call-detail-human.tsx's re-fetch of call_logs is fully redundant)

From src/app/(dashboard)/calls/actions.ts:
```typescript
export interface UnifiedCallContact {
  id: string
  first_name: string | null
  last_name: string | null
  name: string | null
  phone: string | null
  email: string | null
}
export interface UnifiedCallWithContact extends UnifiedCall {
  contact: UnifiedCallContact | null
}
export async function getUnifiedCall(id: string): Promise<UnifiedCallWithContact | null>
export async function getUnifiedCalls(filters: UnifiedCallFilters): Promise<UnifiedCallsResult>
// UnifiedCallsResult.rows: UnifiedCallWithContact[]
```

From src/lib/org/active-org.ts:
```typescript
export const getActiveOrg = cache(async (): Promise<{ id: string; name: string } | null>)
```

From src/lib/org/settings.ts:
```typescript
export interface OrgSettings { id, timezone, currency, legalName, taxId, address }
export const getOrgSettings = cache(async (): Promise<OrgSettings>)
```

From src/lib/rbac/server.ts:
```typescript
export interface RbacContext { userId: string | null; orgId: string | null; role: OrgRole | null; isPlatformAdmin: boolean }
export async function getRbacContext(): Promise<RbacContext>
```

From src/app/(dashboard)/organizations/actions.ts:
```typescript
export async function getUserOrgs(): Promise<{ id: string; name: string; logo_url: string | null }[]>
```

From src/app/api/chat/unread-count/route.ts (server-side count source to mirror for the initial value):
```typescript
// supabase.rpc('inbox_unread_count') -> number
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Calls detail dialog — eliminate the duplicate-fetch waterfall (Phase A)</name>
  <files>src/app/(dashboard)/calls/(hub)/page.tsx, src/app/(dashboard)/calls/actions.ts, src/components/calls/call-detail-ai.tsx, src/components/calls/call-detail-human.tsx, src/components/calls/call-detail-dialog.tsx, src/components/calls/unified-call-timeline.tsx, src/app/(dashboard)/calls/loading.tsx</files>
  <action>
    Current state verified live (post v3.5 overhaul commits ebf096d4/6bd9e0d6) — `CallDetailDialog` is already the centered-dialog version; line numbers in the seed are approximate but the described waterfall is still present exactly as documented.

    1. `src/app/(dashboard)/calls/(hub)/page.tsx`:
       - `CallDetail({ id })` currently always calls `getUnifiedCall(id)` even though `result.rows` (already fetched in the parent `CallsPage` for `UnifiedCallTimeline`) very likely already contains that row. Change `CallDetail` to accept the already-fetched `rows` array, do `rows.find(r => r.id === id)`, and only fall back to `getUnifiedCall(id)` when not found (e.g. row is on a different page than the one currently rendered — deep link via URL, or stale pagination). Thread `result.rows` from `CallsPage` into `CallDetail({ id, rows })`.
       - Keep the `hasAnyNumber` branch structure unchanged — `CallDetail` is only invoked when `callId` is set, independent of that branch, so pass `result.rows` (empty array is fine when the onboarding gate renders instead of the timeline).
    2. `src/app/(dashboard)/calls/actions.ts` (`getUnifiedCall`): keep as the fallback-path function — no column narrowing needed here since every `unified_calls` column is genuinely consumed somewhere in `CallDetailAi`/`CallDetailHuman` (verified: id, call_type, direction, counterpart_name/number, contact, notes, duration_seconds, cost, assistant_id, external_id, substatus, started_at, ended_at, recording_url, recording_duration, routing_mode all render). Do not change its `select('*')` — narrowing here would require re-deriving a manual column list identical to the view's full shape, providing no real savings while risking drift. Leave this function's body unchanged; it is now only called on the fallback path (row not in current page), which is rare.
    3. `src/components/calls/call-detail-ai.tsx`: Per confirmed view mapping, `call.external_id` already equals `calls.vapi_call_id` for AI rows — remove the sequential dependency where `action_logs` only fires after the `calls` row resolves. Restructure to:
       - Fire `supabase.from('calls').select('transcript_turns, started_at').eq('id', call.id).maybeSingle()` (narrowed to just the two columns actually used: `transcript_turns` for the timeline and `started_at` for `buildTimeline`'s anchor — both NOT present on `call`) and `supabase.from('action_logs').select('*').eq('vapi_call_id', call.external_id).order('created_at', { ascending: true })` inside a single `Promise.all([...])`, since the `action_logs` query no longer needs to wait on `vapiCall.vapi_call_id` — `call.external_id` already has it.
       - Keep the `vapiCall?.started_at` gate on `buildTimeline` the same as today (falls back to `[]` if null).
    4. `src/components/calls/call-detail-human.tsx`: Per confirmed view mapping, `call_logs.ended_at` is already `unified_calls.ended_at`, `call_sid` is already `call.external_id`, and `recording_duration` is already `call.recording_duration`. The entire `call_logs` re-fetch is redundant — delete it. Replace `logRow?.ended_at ?? null` with `call.ended_at` directly. Remove the now-unused `createClient` import and the `supabase` re-fetch block. This function no longer needs to be `async` for the fetch (it can stay `async` since it's a server component, that's fine either way — do not change its exported signature/props).
    5. `src/components/calls/call-detail-dialog.tsx`: The `close()` callback currently rebuilds `URLSearchParams` from the full `sp` (searchParams) object and is memoized on `[router, pathname, sp]` — meaning the callback identity changes on ANY search-param change (e.g. a timeline `type`/`direction`/`q`/`page` filter click while the dialog happens to be open), which forces `Dialog`'s `onOpenChange` prop to change identity and can cascade unnecessary child re-renders. Narrow the dependency to just the `call` param value: read `const callParam = sp.get('call')` and depend on `[router, pathname, callParam, sp]` is not enough since `sp` itself is unstable — instead capture only what's needed to delete the "call" key. Since `close()` needs to preserve all *other* params (only removing `call`), keep using `sp.entries()` inside the callback body (that's necessary for correctness — it must not drop other filters), but change the `useCallback` dependency array to `[router, pathname, sp.get('call')]` so the memoized function identity is stable across unrelated param changes (filter/page changes) and only changes when the `call` param itself changes. Reference: this addresses the SEED-048 Phase A item on `call-detail-dialog.tsx:13-32`.
    6. `src/components/calls/unified-call-timeline.tsx`: Wrap the internal `UnifiedCallRow` function component in `React.memo`. Add a custom comparator (or rely on shallow prop equality on the single `row` prop) keyed on `row.id` and any field that can change without the row object being replaced — since `rows` comes fresh from the server on every navigation (new array/objects each request), default shallow memo on the `row` object reference is sufficient here (a new server render always produces new row objects when data changes, and identical unchanged data produces structurally-recreated-but-value-equal objects on re-renders triggered by sibling state like dialog open/close). Implement as:
       ```tsx
       const UnifiedCallRow = React.memo(function UnifiedCallRow({ row }: { row: UnifiedCallWithContact }) {
         /* existing body unchanged */
       }, (prev, next) => prev.row.id === next.row.id && prev.row.updated_at === next.row.updated_at)
       ```
       Note: `unified_calls` does not have an `updated_at` column (confirmed via migration 063 — only `created_at`). Use a comparator based on the fields that actually vary per re-render relevance: `prev.row.id === next.row.id && prev.row.status === next.row.status && prev.row.notes === next.row.notes` (status and notes are the only fields that can change after initial load, e.g. call ending or notes edited). If in doubt, prefer comparing `JSON.stringify(prev.row) === JSON.stringify(next.row)` only as a last resort — prefer the explicit field comparator above since it's cheaper and this list can be long.
    7. `src/app/(dashboard)/calls/loading.tsx`: This is the Next.js route-level `loading.tsx`, which only covers the initial page shell (before `CallsPage` itself resolves) — it does NOT cover the dialog body, which is a nested async server component (`CallDetail` → `CallDetailAi`/`CallDetailHuman`) rendered inside the same page after the outer `Promise.all` in `CallsPage` already resolved. Wrap the `CallDetailAi`/`CallDetailHuman` invocation inside `CallDetail` (in `page.tsx`) with `<Suspense fallback={<CallDetailSkeleton />}>` where `CallDetailSkeleton` is a small new local skeleton (simple pulse blocks matching the two-column detail layout: header row + `lg:col-span-2` content blocks + aside meta rows) — add it either as a small inline function in `page.tsx` or a new tiny component. Do not modify `loading.tsx` to try to cover this — route-level loading only fires on hard navigation, not on client-side `?call=` param changes, so `Suspense` inside the page is the correct mechanism. (If a case exists where `loading.tsx` needs a note, add a one-line comment there pointing to the in-page Suspense boundary — do not restructure `loading.tsx` itself.)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Clicking a call row that's part of the currently-loaded page renders the detail dialog with zero additional `getUnifiedCall`/`calls`/`call_logs` round-trips; `action_logs` fetch no longer waits on a redundant `calls` re-fetch; dialog re-render is not triggered by unrelated searchParams changes; `UnifiedCallRow` is memoized; dialog body shows a skeleton while resolving; `npm run build` passes with no type errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dashboard layout, org, and RBAC — parallelize the per-navigation tax (Phase C)</name>
  <files>src/app/(dashboard)/layout.tsx, src/lib/org/settings.ts, src/lib/rbac/server.ts, src/hooks/use-unread-count.ts, src/components/layout/sidebar.tsx, src/components/layout/top-bar.tsx, src/components/layout/org-switcher.tsx</files>
  <action>
    All auth/org-scoping semantics must remain identical — `get_current_org_id()` stays the single source of truth for RLS scoping. Do not introduce a path that bypasses it or duplicates its result inconsistently.

    1. `src/app/(dashboard)/layout.tsx` — fold the sequential `call_routing_chains` query into the existing `Promise.all` batch (currently lines ~119-132: `call_settings` + `twilio_phone_numbers` count). The `call_routing_chains` query only needs `activeOrgId`, which is already resolved by `getActiveOrg()` earlier in the function (before this block) — it has no dependency on `settings`/`numberCount` from that same batch, so it can run alongside them. Restructure to:
       ```ts
       const [{ data: settings }, { count: numberCount }, chainResult] = await Promise.all([
         supabase.from('call_settings').select('routing_mode, twilio_client_identity').eq('user_id', user.id).maybeSingle(),
         supabase.from('twilio_phone_numbers').select('id', { count: 'exact', head: true }).eq('is_active', true).eq('capability_voice', true),
         activeOrgId
           ? supabase.from('call_routing_chains').select('is_active, stages').eq('org_id', activeOrgId).maybeSingle()
           : Promise.resolve({ data: null }),
       ])
       const chain = chainResult.data
       ```
       Then use `chain` in place of the old `const { data: chain } = await supabase.from('call_routing_chains')...` call below — remove the old sequential call, keep the `isChainVoiceTarget` derivation logic exactly as-is (it already null-checks `chain?.is_active`).
    2. `src/lib/org/settings.ts` (`getOrgSettings`) currently independently calls `supabase.rpc('get_current_org_id')` and re-fetches `organizations` — duplicating work `getActiveOrg()` (in `src/lib/org/active-org.ts`) already did moments earlier in the same request. Both are already wrapped in React `cache()`, but they don't share the org id lookup. Change `getOrgSettings` to accept an optional pre-resolved org id parameter: `export const getOrgSettings = cache(async (orgId?: string | null): Promise<OrgSettings> => {...})`. Inside, if `orgId` is passed (not `undefined`), skip the `supabase.rpc('get_current_org_id')` call and use the passed value directly (still handling `null` as "no active org" → return `DEFAULTS`, matching current behavior). If `orgId` is `undefined` (param omitted), preserve the exact current behavior (self-resolve via RPC) so any other caller of `getOrgSettings()` elsewhere in the codebase (grep to confirm — if none, still keep this fallback for safety/back-compat) continues to work unchanged.
       In `src/app/(dashboard)/layout.tsx`, change the call site from `const orgSettings = await getOrgSettings()` to `const orgSettings = await getOrgSettings(activeOrgId)`, reusing the `activeOrgId` already resolved via `getActiveOrg()` at the top of the function. This removes one redundant `get_current_org_id()` RPC + does not change the `organizations` row re-fetch (that fetch pulls different columns than `getActiveOrg` — timezone/currency/legal fields vs id/name — so it must stay, only the org-id resolution step is deduplicated).
    3. `src/lib/rbac/server.ts` (`getRbacContext`) — the `get_current_org_id()` RPC result (`orgId`) is a genuine dependency for the `org_members` lookup (can't query membership without knowing which org), but the `platform_admins` lookup does NOT depend on `orgId` at all. Currently these two queries run one after another. Restructure so `platform_admins` and `org_members` run in parallel with each other (both only depend on `orgId`/`user.id`, which are already known once the RPC resolves) rather than the RPC being followed by two more sequential awaits:
       ```ts
       const { data: orgId } = await supabase.rpc('get_current_org_id')

       const [{ data: pa }, membershipResult] = await Promise.all([
         supabase.from('platform_admins').select('user_id').eq('user_id', user.id).maybeSingle(),
         orgId
           ? supabase.from('org_members').select('role').eq('user_id', user.id).eq('organization_id', orgId as string).maybeSingle()
           : Promise.resolve({ data: null }),
       ])
       const isTableAdmin = !!pa
       const role = (membershipResult.data?.role as OrgRole) ?? null
       ```
       Do NOT parallelize the initial `get_current_org_id()` RPC itself with anything else — `org_members` genuinely needs its result, and `platform_admins` doesn't care about ordering, so this Promise.all is safe and doesn't fake a dependency-free parallelization. Preserve the exact return shape of `RbacContext`.
    4. `src/hooks/use-unread-count.ts` — add an optional `initialCount` parameter so callers can seed state from a server-computed value instead of always starting at 0 and waiting for the client fetch. Change signature to `export function useUnreadCount(userId: string | null | undefined, initialCount = 0): number` and `useState(initialCount)`. Keep every other line (the `useEffect`, realtime channel subscription, event listeners, cleanup) completely unchanged — this is purely a seeding mechanism; the mount-time `fetchUnreadCount().then(setCount)` call inside the effect should stay as-is (it reconciles to the true live count shortly after paint, same as today), it just no longer needs to carry the visual weight of the very first paint since `initialCount` fills that gap.
    5. `src/components/layout/sidebar.tsx` — `SidebarBody` currently calls `useUnreadCount(userId)` with no seed. Add an `initialUnreadCount?: number` prop to both `SidebarProps` and `SidebarBodyProps`, thread it through `bodyProps` in `Sidebar`, and call `useUnreadCount(userId, initialUnreadCount ?? 0)` inside `SidebarBody`.
    6. `src/app/(dashboard)/layout.tsx` — compute the initial unread count server-side using the same RPC the `/api/chat/unread-count` route already calls (`supabase.rpc('inbox_unread_count')`), fail-open to `0` on any error (mirroring the route's `console.error` + `{ count: 0 }` fallback), and pass it to `<Sidebar initialUnreadCount={...} ... />`. Fetch this inside the existing `Promise.all` batch used for `settings`/`numberCount`/`call_routing_chains` from step 1 if it fits cleanly (it needs `user.id`, already available, and does not depend on any of that batch's other results) — add it as a fourth parallel entry in that same `Promise.all` to avoid introducing a new sequential await:
       ```ts
       const [{ data: settings }, { count: numberCount }, chainResult, { data: unreadCountRaw }] = await Promise.all([
         /* ...existing two... */,
         /* ...chain from step 1... */,
         supabase.rpc('inbox_unread_count'),
       ])
       const initialUnreadCount = typeof unreadCountRaw === 'number' ? unreadCountRaw : 0
       ```
       This whole block is already inside a `try {} catch { browserVoiceEnabled = false; hasPhoneNumber = false }` — add `let initialUnreadCount = 0` to the outer scope (declared before the `try`, alongside `browserVoiceEnabled`/`hasPhoneNumber`) so it defaults to `0` on any error in that block too, matching existing fail-open conventions in this file.
    7. `src/components/layout/org-switcher.tsx` — add an optional `initialOrgs?: Org[]` prop. When provided, initialize `const [orgs, setOrgs] = useState<Org[] | null>(initialOrgs ?? null)` instead of always starting `null`, and skip the lazy fetch in `handleDropdownOpen` when `orgs` is already non-null (existing `orgs === null` check already handles this correctly — no change needed there beyond the initial state). Keep the "Add organization" / "Manage organizations" behavior and the `handleSwitch` logic completely unchanged.
    8. `src/app/(dashboard)/layout.tsx` — fetch the org list server-side via the already-imported pattern (import `getUserOrgs` from `@/app/(dashboard)/organizations/actions`) and pass it to both `TopBar` call sites' `OrgSwitcher` usages. Since `OrgSwitcher` is rendered inside `TopBar` (desktop) and `MobileMenu` (mobile, also inside `top-bar.tsx`), thread `initialOrgs` through `TopBarProps` (add `initialOrgs: Org[]` — reuse or duplicate the minimal `{id, name, logo_url}` shape already defined in `org-switcher.tsx`, importing the type or inlining it in `top-bar.tsx` — prefer importing to avoid duplication) down to both `<OrgSwitcher initialOrgs={initialOrgs} .../>` call sites in `top-bar.tsx` (desktop actions block and `MobileMenu`). Fetch `getUserOrgs()` in `layout.tsx` — add it to the same `Promise.all` batch from step 1/6 only if it doesn't depend on anything in that block (it only needs the authenticated user, so it qualifies) — extend the same array with a fifth parallel entry, or fetch it independently in parallel via a separate `Promise.all` positioned right after `getActiveOrg()` resolves if merging into the call-settings try/catch block would awkwardly couple unrelated concerns. Prefer whichever keeps the diff smallest while eliminating the sequential/lazy-on-click fetch — if the call-settings `Promise.all` block already has 4 entries from steps 1+6, adding `getUserOrgs()` as a 5th is fine since none of the five depend on each other's results.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>call_routing_chains is fetched inside the same parallel batch as call_settings/twilio_phone_numbers; getOrgSettings reuses the org id already resolved by getActiveOrg instead of re-invoking get_current_org_id; getRbacContext runs the platform_admins and org_members lookups in parallel once org_id is known; useUnreadCount accepts and honors an initialCount seed with realtime behavior unchanged; the org switcher accepts a preloaded orgs list and skips its lazy fetch when present; layout.tsx supplies both the initial unread count and the preloaded org list server-side; npm run build passes with no type errors.</done>
</task>

</tasks>

<verification>
Run `npm run build` after both tasks (each task also runs it individually as its verify step — the final run after Task 2 is the authoritative one covering both phases together). Confirm no new TypeScript errors, no changed RLS/org-scoping behavior (grep for any remaining direct `supabase.auth.getUser()` calls introduced by this work — there should be none, only the cached `getUser()` helper), and that `get_current_org_id()` remains the sole resolver of org scoping (no new cookie-based or client-only org resolution path was introduced).

Manually confirm (no automated UI test exists in this repo for these flows):
- Calls page: click a row in the timeline — dialog opens, Network tab shows no `getUnifiedCall`/duplicate `calls`/`call_logs` request for a row already on the current page.
- Calls page: apply a timeline filter (type/direction) while unrelated — dialog should not spuriously reopen/re-render if not already open.
- Any dashboard page navigation: sidebar unread badge and org switcher name should paint immediately without a visible flash-then-populate.
- Org switcher: click to open dropdown — list appears instantly (no loading spinner on first open when preloaded).
</verification>

<success_criteria>
- Phase A: Calls detail dialog renders from already-loaded hub data with a fallback fetch only when the row isn't present in the current page's rows; CallDetailAi/CallDetailHuman no longer perform redundant full-row re-fetches; call_logs re-fetch in CallDetailHuman is fully removed; action_logs fetch in CallDetailAi no longer sequentially waits on an avoidable prior fetch; dialog re-render is scoped to the `call` param; UnifiedCallRow is memoized; dialog body has a loading skeleton.
- Phase C: call_routing_chains query runs inside the layout's existing parallel batch; getOrgSettings reuses the already-resolved org id instead of re-deriving it; getRbacContext parallelizes the platform-admin and org-membership lookups; the unread-count badge and org switcher list are seeded server-side to avoid a client-side round-trip on first paint/interaction.
- No visible behavior, schema, or RLS/org-scoping change of any kind.
- `npm run build` passes with zero type errors after both tasks are complete.
</success_criteria>

<output>
After completion, create `.planning/quick/260704-pr3-performance-seed-048-fases-a-c-eliminar-/260704-pr3-SUMMARY.md`
</output>
