---
phase: 33-schema-foundation-legacy-default-agent-backfill
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/038_tool_idempotency_and_pricing.sql
autonomous: true
requirements: [OBS-03]
requirements_addressed: [OBS-03]
must_haves:
  truths:
    - "Migration 038 creates public.tool_idempotency_keys (per-org idempotency cache for Phase 38 multi-agent delegation) with RLS"
    - "Migration 038 creates public.agent_model_pricing as a flat reference table (PRIMARY KEY = model TEXT) with NO RLS (public read, service-role write) and seeds the 7 launch models per .planning Specifics"
    - "Both tables are idempotent (CREATE TABLE IF NOT EXISTS); seeded rows use ON CONFLICT (model) DO NOTHING so re-running the migration is safe"
    - "Pricing seed comment flags executor to verify rates are current as of execution time (D-33 Specifics)"
  artifacts:
    - path: "supabase/migrations/038_tool_idempotency_and_pricing.sql"
      provides: "public.tool_idempotency_keys (id, organization_id, agent_invocation_id, idempotency_key TEXT, response JSONB, created_at, expires_at) with UNIQUE (organization_id, idempotency_key); public.agent_model_pricing (model PK, input_per_million_usd NUMERIC, output_per_million_usd NUMERIC, provider TEXT, notes TEXT, updated_at); seed rows for 7 launch models"
      contains: "CREATE TABLE IF NOT EXISTS public.tool_idempotency_keys"
  key_links:
    - from: "supabase/migrations/038_tool_idempotency_and_pricing.sql"
      to: "supabase/migrations/037_agent_invocations.sql"
      via: "tool_idempotency_keys.agent_invocation_id is a nullable FK to agent_invocations(id) ON DELETE SET NULL — the row survives the invocation that wrote it (reads from later invocations need the cached response)"
      pattern: "REFERENCES public\\.agent_invocations\\(id\\)"
    - from: "supabase/migrations/038_tool_idempotency_and_pricing.sql"
      to: "src/lib/agents/runtime (Phase 34)"
      via: "agent_model_pricing.model is queried by cost calculator after each LLM response (input_tokens * input_per_million_usd / 1_000_000 + same for output)"
      pattern: "model TEXT PRIMARY KEY"
---

<objective>
Write migration 038, which lands two reference tables that the Phase 34 runtime depends on but that don't fit the agent-entity slice (034/035) or the observability slice (036/037):

1. `tool_idempotency_keys` — per-org cache so a tool re-call within ~24h returns the cached result rather than re-executing (Phase 38 multi-agent delegation depends on this; Phase 34 starts writing to it for cost protection).
2. `agent_model_pricing` — flat reference table mapping `model` -> `(input_per_million_usd, output_per_million_usd)`. Phase 34's cost calculator joins on `model` to compute `cost_usd` for every `agent_invocations` row.

Purpose: Without `agent_model_pricing` rows, every Phase 34 invocation logs `cost_usd = NULL`, breaking the OBS-04 daily cost cap and the Phase 40 dashboard cost column. This plan ships those rows in the same migration as the table itself for atomicity.

Output: 1 migration file with 2 table creations + 7 seeded pricing rows. Not pushed in this plan — Plan 07 runs `npx supabase db push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md
@./CLAUDE.md
@supabase/migrations/001_foundation.sql
@supabase/migrations/032_ghl_reengagement_sent.sql
@supabase/migrations/033_automation_schedules.sql

<interfaces>
Reusable building blocks from earlier migrations:

From supabase/migrations/001_foundation.sql:
- public.organizations(id UUID PK, ...)
- public.get_current_org_id() RETURNS UUID — SECURITY DEFINER
- public.update_updated_at() RETURNS TRIGGER

From supabase/migrations/037_agent_invocations.sql (Plan 03):
- public.agent_invocations(id UUID PK, organization_id, agent_id, ...)

From supabase/migrations/033_automation_schedules.sql (style reference for seeded-rows-in-migration pattern):
```
INSERT INTO public.automation_schedules (automation_key, is_active, next_run_at, interval_minutes)
VALUES (...)
ON CONFLICT (automation_key) DO NOTHING;
```

REQ OBS-03 (verbatim from REQUIREMENTS.md):
- agent_model_pricing(model TEXT PRIMARY KEY, input_per_million_usd NUMERIC, output_per_million_usd NUMERIC, ...)
- Used by Phase 34 cost calculator to populate agent_invocations.cost_usd

D-33 Specifics (from 33-CONTEXT.md):
- agent_model_pricing seed should include at minimum: Claude Opus 4.7, Claude Sonnet 4.6, Claude Haiku 4.5, GPT-4o, GPT-4o-mini, Gemini 2.5 Pro/Flash
- Source rates as of 2026-05-16 — flag in plan for executor to verify at execution time
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 038_tool_idempotency_and_pricing.sql</name>
  <files>supabase/migrations/038_tool_idempotency_and_pricing.sql</files>
  <read_first>
    - supabase/migrations/032_ghl_reengagement_sent.sql (style reference — header comment, RLS pattern, COMMENT ON TABLE)
    - supabase/migrations/033_automation_schedules.sql (seeded-row-in-migration pattern with ON CONFLICT DO NOTHING)
    - supabase/migrations/001_foundation.sql (RLS template + get_current_org_id() + update_updated_at())
    - supabase/migrations/037_agent_invocations.sql (just-created — confirm public.agent_invocations.id exists for the FK)
    - .planning/REQUIREMENTS.md (search for OBS-03 — confirm agent_model_pricing column shape)
    - .planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md (D-33-01 migration 5 of 6; D-33-15 model is PK; Specifics — 7 launch models)
  </read_first>
  <action>
**EXECUTOR NOTE — verify pricing rates at execution time.** The pricing seeds below reflect published rates as of 2026-05-16. Before pushing, confirm against current vendor pricing pages (anthropic.com/pricing, openai.com/api/pricing, ai.google.dev/pricing). If rates have shifted by more than 5%, update the literals in the INSERT block before the operator runs `npx supabase db push` in Plan 07.

Create `supabase/migrations/038_tool_idempotency_and_pricing.sql` with EXACTLY this SQL content (copy-paste):

```
-- =============================================================================
-- Migration: 038_tool_idempotency_and_pricing
-- Phase: v2.0 Multi-Bot Platform — Phase 33 Schema Foundation
-- Creates: public.tool_idempotency_keys (per-org tool-call cache, ~24h TTL)
--          public.agent_model_pricing (flat model -> price reference, OBS-03)
-- RLS:     tool_idempotency_keys org-scoped (canonical pattern);
--          agent_model_pricing read-public (no RLS) — write via service-role only
-- Seed:    7 launch model pricing rows (rates as of 2026-05-16 — verify at execute time)
-- Decisions: D-33-01 (migration 5 of 6)
--            D-33-15 (agent_model_pricing.model is PRIMARY KEY)
--            Specifics — minimum 7 models seeded
-- TTL cleanup for tool_idempotency_keys is deferred to Phase 38 (D-33 deferred).
-- =============================================================================

-- ---------------------------------------------------------------------------
-- tool_idempotency_keys: cache tool-call responses for ~24h to dedupe replay
-- ---------------------------------------------------------------------------

CREATE TABLE IF NOT EXISTS public.tool_idempotency_keys (
  id                   UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id      UUID         NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE,
  agent_invocation_id  UUID         REFERENCES public.agent_invocations(id) ON DELETE SET NULL,
  idempotency_key      TEXT         NOT NULL,
  tool_name            TEXT         NOT NULL,
  request_hash         TEXT         NOT NULL,
  response             JSONB        NOT NULL,
  created_at           TIMESTAMPTZ  NOT NULL DEFAULT now(),
  expires_at           TIMESTAMPTZ  NOT NULL DEFAULT (now() + interval '24 hours'),
  CONSTRAINT uniq_tool_idem_org_key UNIQUE (organization_id, idempotency_key)
);

ALTER TABLE public.tool_idempotency_keys ENABLE ROW LEVEL SECURITY;

CREATE INDEX IF NOT EXISTS idx_tool_idem_org_key
  ON public.tool_idempotency_keys(organization_id, idempotency_key);

CREATE INDEX IF NOT EXISTS idx_tool_idem_expires
  ON public.tool_idempotency_keys(expires_at);

DROP POLICY IF EXISTS "tool_idempotency_keys_all" ON public.tool_idempotency_keys;
CREATE POLICY "tool_idempotency_keys_all" ON public.tool_idempotency_keys
  FOR ALL TO authenticated
  USING      (organization_id = (SELECT public.get_current_org_id()))
  WITH CHECK (organization_id = (SELECT public.get_current_org_id()));

COMMENT ON TABLE public.tool_idempotency_keys IS
  'Phase 33 (v2.0): per-org tool-call response cache. Phase 38 dispatcher checks (org, idempotency_key) before re-executing a tool. Rows expire after 24h; cleanup job lands in Phase 38.';

-- ---------------------------------------------------------------------------
-- agent_model_pricing: flat reference table (OBS-03)
-- No RLS — read-public reference data; writes via service-role only
-- model is the PRIMARY KEY (D-33-15)
-- ---------------------------------------------------------------------------

CREATE TABLE IF NOT EXISTS public.agent_model_pricing (
  model                    TEXT         PRIMARY KEY,
  source                   TEXT         NOT NULL,
  input_per_1m_usd         NUMERIC(10,4) NOT NULL CHECK (input_per_1m_usd >= 0),
  output_per_1m_usd        NUMERIC(10,4) NOT NULL CHECK (output_per_1m_usd >= 0),
  notes                    TEXT,
  updated_at               TIMESTAMPTZ  NOT NULL DEFAULT now()
);

-- Intentionally no RLS — global reference table. To restrict writes, only the
-- service-role key has INSERT/UPDATE/DELETE because anon and authenticated
-- have no DML grants by default in this project.
-- (No ENABLE ROW LEVEL SECURITY; no policy needed.)

DROP TRIGGER IF EXISTS trg_agent_model_pricing_updated_at ON public.agent_model_pricing;
CREATE TRIGGER trg_agent_model_pricing_updated_at
  BEFORE UPDATE ON public.agent_model_pricing
  FOR EACH ROW EXECUTE FUNCTION public.update_updated_at();

COMMENT ON TABLE public.agent_model_pricing IS
  'Phase 33 (v2.0): OBS-03 flat model->price reference. Phase 34 cost calculator joins on model to compute agent_invocations.cost_usd. Pricing as of 2026-05-16 — service-role updates as vendor pricing changes.';

-- ---------------------------------------------------------------------------
-- Seed: 7 launch models. Rates current as of 2026-05-16. VERIFY AT EXECUTE TIME.
-- Anthropic: anthropic.com/pricing
-- OpenAI:    openai.com/api/pricing
-- Google:    ai.google.dev/pricing
-- Format:    USD per 1,000,000 tokens (input / output)
-- ---------------------------------------------------------------------------

INSERT INTO public.agent_model_pricing (model, source, input_per_1m_usd, output_per_1m_usd, notes) VALUES
  ('anthropic/claude-opus-4-7',     'anthropic', 15.0000, 75.0000, 'Most capable Anthropic model — high cost, deep reasoning'),
  ('anthropic/claude-sonnet-4-6',   'anthropic',  3.0000, 15.0000, 'Balanced Anthropic — default chat model'),
  ('anthropic/claude-haiku-4-5',    'anthropic',  0.8000,  4.0000, 'Fastest/cheapest Anthropic — agents.model default'),
  ('openai/gpt-4o',                 'openai',     2.5000, 10.0000, 'OpenAI flagship multimodal'),
  ('openai/gpt-4o-mini',            'openai',     0.1500,  0.6000, 'OpenAI fast/cheap tier'),
  ('google/gemini-2.5-pro',         'google',     1.2500,  5.0000, 'Google long-context flagship (1M context)'),
  ('google/gemini-2.5-flash',       'google',     0.0750,  0.3000, 'Google fast/cheap tier')
ON CONFLICT (model) DO NOTHING;
```

Use the Write tool. Do NOT push the migration in this task — that happens in Plan 07.
  </action>
  <verify>
    <automated>test -f supabase/migrations/038_tool_idempotency_and_pricing.sql && grep -c "CREATE TABLE IF NOT EXISTS public.tool_idempotency_keys" supabase/migrations/038_tool_idempotency_and_pricing.sql && grep -c "CREATE TABLE IF NOT EXISTS public.agent_model_pricing" supabase/migrations/038_tool_idempotency_and_pricing.sql && grep -c "ON CONFLICT (model) DO NOTHING" supabase/migrations/038_tool_idempotency_and_pricing.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f supabase/migrations/038_tool_idempotency_and_pricing.sql`
    - Creates both tables: `grep -c "CREATE TABLE IF NOT EXISTS public.tool_idempotency_keys" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1 AND `grep -c "CREATE TABLE IF NOT EXISTS public.agent_model_pricing" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - tool_idempotency_keys has UNIQUE (organization_id, idempotency_key): `grep -c "UNIQUE (organization_id, idempotency_key)" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - tool_idempotency_keys has RLS: `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1 (only the one table — pricing has none by design)
    - tool_idempotency_keys has canonical RLS policy: `grep -cE "USING\s+\(organization_id = \(SELECT public\.get_current_org_id\(\)\)\)" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - tool_idempotency_keys.agent_invocation_id FK with SET NULL: `grep -c "REFERENCES public.agent_invocations(id) ON DELETE SET NULL" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - agent_model_pricing.model is PRIMARY KEY (D-33-15): `grep -cE "model\s+TEXT\s+PRIMARY KEY" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - agent_model_pricing has NO ENABLE ROW LEVEL SECURITY for it (only one ENABLE total, on tool_idempotency_keys)
    - agent_model_pricing has NUMERIC(10,4) cost columns: `grep -c "NUMERIC(10,4)" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 2
    - All 7 models present: each grep returns 1: `grep -c "claude-opus-4-7" supabase/migrations/038_tool_idempotency_and_pricing.sql`, `grep -c "claude-sonnet-4-6" supabase/migrations/038_tool_idempotency_and_pricing.sql`, `grep -c "claude-haiku-4-5" supabase/migrations/038_tool_idempotency_and_pricing.sql`, `grep -c "gpt-4o'" supabase/migrations/038_tool_idempotency_and_pricing.sql`, `grep -c "gpt-4o-mini" supabase/migrations/038_tool_idempotency_and_pricing.sql`, `grep -c "gemini-2.5-pro" supabase/migrations/038_tool_idempotency_and_pricing.sql`, `grep -c "gemini-2.5-flash" supabase/migrations/038_tool_idempotency_and_pricing.sql`
    - Seed insert is idempotent: `grep -c "ON CONFLICT (model) DO NOTHING" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - 24h TTL default on tool_idempotency_keys: `grep -c "interval '24 hours'" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
    - No DROP / no RENAME: `grep -cE "DROP TABLE|RENAME" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 0
    - Verification flag comment present: `grep -c "VERIFY AT EXECUTE TIME" supabase/migrations/038_tool_idempotency_and_pricing.sql` returns 1
  </acceptance_criteria>
  <done>038 migration file written; both tables created; 7 seed pricing rows present with VERIFY-AT-EXECUTE-TIME comment so the executor knows to refresh rates before push.</done>
</task>

</tasks>

<verification>
After this task: 1 migration file exists. Combined with Plans 02 + 03, 5 migration files now sit in `supabase/migrations/` ready for Plan 07 push.

Cross-check: `tool_idempotency_keys.agent_invocation_id` references `agent_invocations(id)` which lives in 037 — confirm migration order at push time (Postgres applies in alphanumeric filename order: 034, 035, 036, 037, 038 — correct).
</verification>

<success_criteria>
- Migration file exists and passes the verify command
- File order: 034, 035, 036, 037, 038 (no number conflicts)
- File ready to be committed to git in batch with the rest
- Plan 05 (migration 039) can be written in parallel with this plan since 039 only depends on Plan 02's `agents` table
- Plan 06 (seed migration 040) depends on this plan because the seed indirectly relies on the pricing table existing for downstream Phase 34 cost calc — though no row in 040 itself touches `agent_model_pricing` or `tool_idempotency_keys`
- Plan 07 (push checkpoint) depends on this plan
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-04-SUMMARY.md`
</output>
