---
phase: 10-meta-oauth
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - src/app/(dashboard)/integrations/meta/page.tsx
  - src/components/integrations/meta-settings.tsx
  - src/app/(dashboard)/integrations/page.tsx
autonomous: false
requirements:
  - META-01
  - META-03
  - META-04
  - META-05
  - META-06

must_haves:
  truths:
    - "Admin has a dedicated /integrations/meta settings page with a clear Connect with Facebook entry point"
    - "Connected Messenger and Instagram rows are listed independently, with page/account labels, active status, and last sync timestamps"
    - "A revoked token state surfaces as a reconnect prompt instead of a healthy connection badge"
    - "Admin can disconnect one channel row and independently bind automations to messenger and instagram rows for the same page"
    - "The main /integrations page links cleanly into the Meta settings page without disturbing the existing provider table"
    - "npm run build passes with the new page and client component mounted"
  artifacts:
    - path: "src/app/(dashboard)/integrations/meta/page.tsx"
      provides: "Server-rendered Meta integrations page with channel query and tool config data"
      min_lines: 110
    - path: "src/components/integrations/meta-settings.tsx"
      provides: "Client UI for connect CTA, channel cards, reconnect prompts, disconnect, and automation selectors"
      min_lines: 220
    - path: "src/app/(dashboard)/integrations/page.tsx"
      provides: "Entry point from the integrations index to the dedicated Meta settings page"
      min_lines: 20
  key_links:
    - from: "src/app/(dashboard)/integrations/meta/page.tsx"
      to: "src/components/integrations/meta-settings.tsx"
      via: "channel data + tool config props"
      pattern: "MetaSettings"
    - from: "src/components/integrations/meta-settings.tsx"
      to: "src/app/(dashboard)/integrations/meta/actions.ts"
      via: "server actions for connect/disconnect/automation binding"
      pattern: "connectMeta"
    - from: "src/app/(dashboard)/integrations/page.tsx"
      to: "src/app/(dashboard)/integrations/meta/page.tsx"
      via: "dashboard navigation link"
      pattern: "/integrations/meta"
---

<objective>
Add the admin-facing Meta channel settings UI.

Purpose: Finish Phase 10 by exposing the OAuth connect flow, connection status, reconnect state, disconnect controls, and per-channel automation binding in the dashboard.
Output: One new dashboard page, one new client component, and one small integrations index update.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/FEATURES.md
@.planning/phases/10-meta-oauth/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the server-rendered /integrations/meta page with channel and automation data</name>
  <files>src/app/(dashboard)/integrations/meta/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/integrations/page.tsx
    - src/app/(dashboard)/tools/page.tsx
    - src/lib/supabase/server.ts
    - src/types/database.ts (meta_channels, tool_configs)
  </read_first>
  <action>
    Create `src/app/(dashboard)/integrations/meta/page.tsx` as a server component.

    Required behavior:
    1. Query the current org's `meta_channels` rows ordered by `page_name`, then `channel_type`.
    2. Query active `tool_configs` so the UI can offer automation bindings.
    3. Normalize channel rows into props that include:
       - page id and page name
       - channel type (`messenger` or `instagram`)
       - Instagram username when present
       - `is_active`, `last_synced_at`, `connection_error`, and current `automation_id`
    4. Render a concise page header explaining that Messenger and Instagram share the same Facebook Login flow.
    5. Pass everything to a dedicated client component rather than placing interactive logic in the page itself.
  </action>
  <acceptance_criteria>
    - `src/app/(dashboard)/integrations/meta/page.tsx` exists
    - page stays server-rendered
    - page passes channel rows and automation options into a single Meta settings client component
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/meta-settings.test.tsx --reporter=verbose</automated>
  </verify>
  <done>The dashboard has a tenant-scoped server page ready to host the new Meta settings experience.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Meta settings UI with connect CTA, reconnect state, disconnect, and automation binding</name>
  <files>src/components/integrations/meta-settings.tsx</files>
  <read_first>
    - src/components/integrations/integrations-table.tsx
    - src/components/widget/widget-settings-form.tsx
    - src/components/ui/*
    - .planning/ROADMAP.md (Phase 10 success criteria)
  </read_first>
  <action>
    Create `src/components/integrations/meta-settings.tsx` as a client component.

    Required UX:
    1. When no channels exist, show a primary `Connect with Facebook` button that invokes the `connectMeta` server action.
    2. Render one card/row per `meta_channels` row.
    3. Show:
       - channel label/icon (`Messenger` or `Instagram`)
       - page name
       - linked Instagram username when present
       - active vs reconnect-needed status
       - last sync timestamp
    4. Treat `connection_error` containing Meta error code `190` as a reconnect state; surface a clear prompt and reconnect CTA instead of a healthy badge.
    5. Provide a disconnect button per channel row.
    6. Provide an automation selector per row using the current org's `tool_configs`, allowing different automation ids for messenger and instagram rows from the same page.
    7. Use `sonner` to show success/error toasts for disconnect and automation updates.

    Keep the component aligned with the existing dashboard tone and do not replace the old generic integrations table.
  </action>
  <acceptance_criteria>
    - `src/components/integrations/meta-settings.tsx` exists
    - component contains `use client`
    - component renders connect, disconnect, reconnect, and automation-binding controls
    - component allows separate automation selection per channel row
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/meta-settings.test.tsx --reporter=verbose && npm run build</automated>
  </verify>
  <done>Admins can manage Meta channel connections and automation mappings from one focused dashboard surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add a clear entry point from the integrations index to the dedicated Meta page</name>
  <files>src/app/(dashboard)/integrations/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/integrations/page.tsx
    - src/components/integrations/integrations-table.tsx
  </read_first>
  <action>
    Update `src/app/(dashboard)/integrations/page.tsx`:
    1. Keep the existing integrations table intact.
    2. Add a small callout, card, or inline section linking to `/integrations/meta`.
    3. Explain that Meta Messaging is managed on a dedicated settings page because it connects multiple channels and automations per organization.
  </action>
  <acceptance_criteria>
    - `/integrations` still renders the existing provider table
    - page includes a visible link to `/integrations/meta`
    - the Meta entry point does not disrupt current integration workflows
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The broader integrations area exposes the new Meta module without regressing the existing provider experience.</done>
</task>

</tasks>

<verification>
Phase 10 full verification:
```bash
npx vitest run tests/meta-oauth-actions.test.ts tests/meta-callback-route.test.ts tests/meta-settings.test.tsx --reporter=verbose
npm run build
```
</verification>

<success_criteria>
- `/integrations/meta` exists with a working OAuth connect entry point
- Connected channels show status, reconnect prompts, disconnect, and last sync details
- Messenger and Instagram rows can bind different automations for the same page
- The main integrations page links into the Meta module cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-meta-oauth/10-03-SUMMARY.md`.
</output>
