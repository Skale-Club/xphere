---
phase: 135-wishlist-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/medusa/agent-sig.ts
  - src/lib/medusa/client.ts
  - src/lib/medusa/wishlist-owner.ts
  - src/lib/medusa/actions/wishlist-add.ts
  - src/lib/medusa/actions/wishlist-remove.ts
  - src/lib/medusa/actions/wishlist-list.ts
  - tests/medusa-agent-fetch.test.ts
  - tests/medusa-wishlist.test.ts
autonomous: true
requirements: [WSL-01, WSL-02]
must_haves:
  truths:
    - "signAgentBody(secret, ts, rawBody) returns the exact bare-hex HMAC-SHA256 that stuscle's verify-hmac.ts recomputes over `${ts}.${rawStr}` — byte agreement, locked by the committed CONTEXT vector."
    - "medusaAgentFetch stringifies the body ONCE, signs THAT string, and sends THAT identical string; the X-Xphere-Timestamp header equals the signed ts; X-Xphere-Signature is `v1=`+bare-hex with the prefix applied exactly once."
    - "medusaAgentFetch enforces R11 (medusa:org:{orgId}, 120/60, memory) BEFORE the network call and aborts after 8s, throwing MedusaApiError on non-2xx."
    - "The agent can save/list/remove a product on the visitor's wishlist; the owner comes ONLY from pinned commerce.cus (→ customer_id) else commerce.wishlist_ref (→ guest_ref)."
    - "When neither cus nor wishlist_ref is pinned, every executor returns a friendly 'nothing saved yet' string and makes NO store call."
    - "409 wishlist_full → 'wishlist is full (100 items)' string; every expected failure resolves to a friendly string and NEVER throws into the tool loop."
  artifacts:
    - path: "src/lib/medusa/agent-sig.ts"
      provides: "signAgentBody(secret, ts, rawBody) → bare lowercase hex"
      exports: ["signAgentBody"]
    - path: "src/lib/medusa/client.ts"
      provides: "medusaAgentFetch<T>(creds, path, orgId, body) signed POST to /agent/*"
      contains: "export async function medusaAgentFetch"
    - path: "src/lib/medusa/wishlist-owner.ts"
      provides: "resolveWishlistOwner(commerce) → exactly one owner key or null"
      exports: ["resolveWishlistOwner"]
    - path: "src/lib/medusa/actions/wishlist-add.ts"
      provides: "addWishlistItem(params, creds, ctx) → friendly string"
      exports: ["addWishlistItem"]
    - path: "src/lib/medusa/actions/wishlist-remove.ts"
      provides: "removeWishlistItem(params, creds, ctx) → friendly string"
      exports: ["removeWishlistItem"]
    - path: "src/lib/medusa/actions/wishlist-list.ts"
      provides: "listWishlist(creds, ctx) → rendered list string"
      exports: ["listWishlist"]
    - path: "tests/medusa-agent-fetch.test.ts"
      provides: "signing vector + medusaAgentFetch header/message assertions"
    - path: "tests/medusa-wishlist.test.ts"
      provides: "owner resolution + 3 executor behaviors (mock medusaAgentFetch + pinned ctx)"
  key_links:
    - from: "src/lib/medusa/actions/wishlist-add.ts"
      to: "medusaAgentFetch → /agent/wishlists/add"
      via: "signed POST"
      pattern: "medusaAgentFetch.*wishlists/add"
    - from: "src/lib/medusa/client.ts (medusaAgentFetch)"
      to: "src/lib/medusa/agent-sig.ts (signAgentBody)"
      via: "sign ts+'.'+rawBody, header value v1=<hex>"
      pattern: "signAgentBody\\("
    - from: "wishlist executors"
      to: "resolveWishlistOwner(commerce) from loadPinnedContext"
      via: "owner = cus else wishlist_ref else null"
      pattern: "resolveWishlistOwner"
    - from: "add/remove executors"
      to: "rateLimit com:write (R7 closed) + com:write:day (R8 closed)"
      via: "shared cart-write keys"
      pattern: "com:write:"
    - from: "list executor"
      to: "rateLimit com:read (R6 memory)"
      via: "shared cart-read key"
      pattern: "com:read:"
---

<objective>
Add the signed `/agent/*` transport (`medusaAgentFetch` + `signAgentBody`) and the three wishlist executors (`addWishlistItem`, `removeWishlistItem`, `listWishlist`) that call Stuscle's HMAC-guarded `/agent/wishlists/{add,remove,list}` surface. Owner identity comes exclusively from the pinned conversation context (anti-IDOR core rule); no owner parameter exists in any tool schema. This plan delivers the transport + executors + their unit tests. Wiring them into the dispatcher/registries is plan 135-02.

Purpose: WSL-01 (executors via signed `/agent/wishlists/*`, owner from pinned context only) and WSL-02 (HMAC signing helper unit-tested against a reference vector; add/remove side-effecting under R7/R8, list read-budgeted under R6).
Output: 5 source files + 2 new test files. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/medusa-commerce/STATE.md
@.planning/workstreams/medusa-commerce/ROADMAP.md
@.planning/workstreams/medusa-commerce/REQUIREMENTS.md
@.planning/workstreams/medusa-commerce/phases/135-wishlist-tools/135-CONTEXT.md
@.planning/workstreams/medusa-commerce/phases/135-wishlist-tools/135-RESEARCH.md
@.planning/workstreams/medusa-commerce/phases/135-wishlist-tools/135-VALIDATION.md
@.planning/research/INTEGRATION-CONTRACT.md

# Reuse verbatim — do NOT re-derive:
@src/lib/medusa/cart-sig.ts          # Phase 134 signing convention: raw-UTF8 key, ['sign'] usage, lowercase hex. signAgentBody copies this key handling; ONLY the message differs.
@src/lib/medusa/client.ts            # medusaStoreFetch is the template for medusaAgentFetch (R11 BEFORE fetch, 8s AbortSignal, MedusaApiError on non-2xx). MedusaCredentials/MedusaExecCtx/MedusaApiError/MedusaRateLimitError live here.
@src/lib/medusa/pinned-context.ts    # loadPinnedContext(ctx) → { sessionKey, commerce } in ONE conversations lookup.
@src/lib/medusa/actions/get-cart.ts  # executor shape template: (creds, ctx), R6 read, never-throw friendly strings, catch maps MedusaRateLimitError/TimeoutError.
@src/lib/medusa/actions/add-to-cart.ts # write-executor shape: R7/R8 fail-closed, error catch ladder.
@src/lib/rate-limit.ts               # rateLimit(key, limit, windowSeconds, { failMode }); failMode 'closed' denies, 'memory' falls back per-instance.
@tests/medusa-cart-write.test.ts     # copy buildSupabase, mockRateLimit, and the mock-transport idioms from here.

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration. -->

From src/lib/medusa/client.ts:
```typescript
export interface MedusaCredentials {
  baseUrl: string        // integrations.location_id, e.g. http://localhost:9000
  connectionToken: string // the HMAC secret for /agent/* signing
  publishableKey: string
  storefrontUrl?: string
}
export interface MedusaExecCtx {
  organizationId: string
  supabase: SupabaseClient<Database>
  conversationId?: string
  emitStructured?: (obj: Record<string, unknown>) => void
}
export class MedusaApiError extends Error { constructor(public status: number, body: string) }
export class MedusaRateLimitError extends Error {}
export async function medusaStoreFetch<T>(creds, path, orgId, init?): Promise<T>
```

From src/lib/medusa/pinned-context.ts:
```typescript
export interface PinnedContext { sessionKey: string; commerce: Record<string, unknown> }
export async function loadPinnedContext(ctx: MedusaExecCtx): Promise<PinnedContext>
```

From src/lib/medusa/cart-sig.ts (the convention to copy — key handling identical, message differs):
```typescript
// key = crypto.subtle.importKey('raw', encoder.encode(secret), {name:'HMAC',hash:'SHA-256'}, false, ['sign'])
// mac = crypto.subtle.sign('HMAC', key, encoder.encode(<message>))
// return [...new Uint8Array(mac)].map(b=>b.toString(16).padStart(2,'0')).join('')  // lowercase hex
export async function signCartSig(secret: string, cartId: string): Promise<string>
```

Stuscle contract shapes (confirmed by reading apps/backend/src/api/agent/wishlists/*/route.ts):
```jsonc
// POST /agent/wishlists/add   body: owner + { product_id, variant_id? }
//   200 { "item": { "id", "product_id", "variant_id": "..."|null, "product": { "title","handle","thumbnail" } | null } }
//   409 { "error": "wishlist_full" }   (cap MAX_WISHLIST_ITEMS = 100)
//   400 { "error": "invalid_body" }    (owner not exactly one of customer_id/guest_ref)
// POST /agent/wishlists/remove body: owner + { product_id, variant_id? }  → 200 { "removed": true }  (200 even when nothing matched)
// POST /agent/wishlists/list   body: owner (exactly one)  → 200 { "wishlist": { "id", "items": [{ "id","product_id","variant_id","product": {"title","handle","thumbnail"}|null }] } }
```

Stuscle verify-hmac.ts (the exact bytes we must match — apps/backend/src/api/agent/_utils/verify-hmac.ts:26):
```javascript
const expected = "v1=" + createHmac("sha256", secret).update(`${ts}.${rawStr}`).digest("hex")
// rawStr = req.rawBody decoded UTF-8; ts = the X-Xphere-Timestamp header value; clock skew > 300s → 401
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signAgentBody helper + medusaAgentFetch signed transport (byte-agreement vector)</name>
  <files>src/lib/medusa/agent-sig.ts, src/lib/medusa/client.ts, tests/medusa-agent-fetch.test.ts</files>
  <read_first>src/lib/medusa/cart-sig.ts, src/lib/medusa/client.ts (medusaStoreFetch), src/lib/rate-limit.ts, C:/Users/Vanildo/Dev/stuscle/apps/backend/src/api/agent/_utils/verify-hmac.ts</read_first>
  <behavior>
    RED first — write tests/medusa-agent-fetch.test.ts before any source:
    - Test "signing vector": signAgentBody('test-secret', '1750000000', '{"a":1}') === '1f11cf9a5d34d98061ca60891c660610b83d4a229b90d9c84c4f47fd5bff50c4' (BARE hex, no v1= prefix). This is the CONTEXT-mandated cross-repo vector — commit it as a permanent regression lock.
    - Test "signing vector (realistic add)": signAgentBody('xph_test_connection_token_abc123', '1750000000', '{"customer_id":"cus_01ABC","product_id":"prod_01XYZ"}') === 'f5817eb8b59e51a70825b9961d87899bfce8f74b043265d083b00cd5bdcf5474'.
    - Test "medusaAgentFetch signs the exact sent bytes": mock global fetch; call medusaAgentFetch(creds, '/agent/wishlists/add', 'org-1', {a:1}); assert (a) fetch called with method POST, (b) the fetch body option === '{"a":1}' (the identical string), (c) header 'X-Xphere-Timestamp' is a stringified integer, (d) header 'X-Xphere-Signature' === 'v1=' + signAgentBody(creds.connectionToken, thatTimestamp, thatBody) — i.e. header ts equals the signed ts and v1= appears exactly once, (e) 'Content-Type' === 'application/json'.
    - Test "R11 before fetch": mock rateLimit to return { allowed: false }; assert medusaAgentFetch rejects with MedusaRateLimitError and fetch was NOT called. Assert the rateLimit key === 'medusa:org:org-1', limit 120, window 60, failMode 'memory'.
    - Test "non-2xx throws MedusaApiError with status": mock fetch → { ok:false, status:409, text:()=>'{"error":"wishlist_full"}' }; assert rejects instanceof MedusaApiError with .status === 409.
  </behavior>
  <action>
    Create src/lib/medusa/agent-sig.ts exporting `signAgentBody(secret: string, ts: string, rawBody: string): Promise<string>`. Copy cart-sig.ts's key handling EXACTLY: `crypto.subtle.importKey('raw', new TextEncoder().encode(secret), { name:'HMAC', hash:'SHA-256' }, false, ['sign'])`; the ONLY difference is the message — sign `encoder.encode(`${ts}.${rawBody}`)` (NOT cartId). Return lowercase bare hex via `[...new Uint8Array(mac)].map(b=>b.toString(16).padStart(2,'0')).join('')`. Header comment: BARE hex — the caller (medusaAgentFetch) is the ONLY place that prepends `v1=`. Do NOT hex-decode the token, do NOT strip `xph_` (same traps as cart-sig.ts).

    In src/lib/medusa/client.ts add `medusaAgentFetch<T>(creds: MedusaCredentials, path: string, orgId: string, body: Record<string, unknown>): Promise<T>`. Import signAgentBody from './agent-sig'. Order (SECURITY CRITICAL, per 135-RESEARCH Pattern 1 + Pitfalls 1/2/4):
      1. `const rl = await rateLimit(`medusa:org:${orgId}`, 120, 60, { failMode: 'memory' })` — R11 shared with medusaStoreFetch, BEFORE fetch; `if (!rl.allowed) throw new MedusaRateLimitError()`.
      2. `const raw = JSON.stringify(body)` — stringify ONCE.
      3. `const ts = Math.floor(Date.now() / 1000).toString()` — seconds, as a STRING.
      4. `const sig = await signAgentBody(creds.connectionToken, ts, raw)`.
      5. fetch `${creds.baseUrl.replace(/\/$/, '')}${path}` POST, headers `{ 'Content-Type':'application/json', 'X-Xphere-Timestamp': ts, 'X-Xphere-Signature': `v1=${sig}` }`, `body: raw` (send the SAME string that was signed), `signal: AbortSignal.timeout(8000)`.
      6. `if (!res.ok) throw new MedusaApiError(res.status, await res.text())`; `return res.json() as Promise<T>`.
    Update the file header comment (currently "this file does NOT export an agent/HMAC fetch") to note that Phase 135 now adds medusaAgentFetch for the signed /agent/* surface.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/medusa-agent-fetch.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx vitest run tests/medusa-agent-fetch.test.ts` green.
    - `grep -n "1f11cf9a5d34d98061ca60891c660610b83d4a229b90d9c84c4f47fd5bff50c4" tests/medusa-agent-fetch.test.ts` matches (committed vector).
    - `grep -n "export async function medusaAgentFetch" src/lib/medusa/client.ts` matches.
    - `grep -n "v1=\${sig}\|v1=' + sig\|`v1=" src/lib/medusa/client.ts` shows the `v1=` prefix applied in exactly ONE place (client.ts, not agent-sig.ts).
    - `grep -c "JSON.stringify" src/lib/medusa/client.ts` shows medusaAgentFetch stringifies the body once (the raw string is reused for both signing and the fetch body).
  </acceptance_criteria>
  <done>signAgentBody matches all committed vectors; medusaAgentFetch signs-then-sends the identical body string with header ts == signed ts, enforces R11 before fetch, throws MedusaApiError on non-2xx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveWishlistOwner + addWishlistItem + removeWishlistItem executors</name>
  <files>src/lib/medusa/wishlist-owner.ts, src/lib/medusa/actions/wishlist-add.ts, src/lib/medusa/actions/wishlist-remove.ts, tests/medusa-wishlist.test.ts</files>
  <read_first>src/lib/medusa/actions/add-to-cart.ts, src/lib/medusa/actions/get-cart.ts, src/lib/medusa/pinned-context.ts, tests/medusa-cart-write.test.ts</read_first>
  <behavior>
    RED first — write tests/medusa-wishlist.test.ts (copy buildSupabase + mockRateLimit idioms from tests/medusa-cart-write.test.ts; mock '@/lib/medusa/client' medusaAgentFetch via importOriginal so MedusaApiError/MedusaRateLimitError stay real for instanceof):
    - resolveWishlistOwner: commerce {cus:'cus_9'} → { customer_id:'cus_9' }; commerce {wishlist_ref:'w-1'} → { guest_ref:'w-1' }; commerce with BOTH cus and wishlist_ref → prefers { customer_id } (cus wins); commerce {} → null; empty-string cus falls through to wishlist_ref.
    - addWishlistItem happy path: pinned commerce {cus:'cus_9'}; params {product_id:'prod_1'}; mock medusaAgentFetch → { item:{ id:'wi_1', product_id:'prod_1', variant_id:null, product:{ title:'Sweatshirt', handle:'sweatshirt', thumbnail:null } } }; assert result contains 'Sweatshirt' and idempotent-safe wording (e.g. matches /saved/i); assert the medusaAgentFetch body arg === { customer_id:'cus_9', product_id:'prod_1' } (exactly one owner key, no guest_ref, no undefined variant_id) and path === '/agent/wishlists/add'.
    - addWishlistItem with variant: params {product_id:'prod_1', variant_id:'variant_01A'} → body includes variant_id.
    - addWishlistItem no owner: commerce {} → returns the friendly "nothing saved yet" string AND medusaAgentFetch NOT called.
    - addWishlistItem missing product_id: params {} with owner present → returns a "tell me which product" string, medusaAgentFetch NOT called.
    - addWishlistItem 409: mock medusaAgentFetch rejects new MedusaApiError(409, '{"error":"wishlist_full"}') → result matches /full/i and mentions 100; never throws.
    - addWishlistItem R7 closed: mockRateLimit first call { allowed:false } → friendly string, medusaAgentFetch NOT called; assert the R7 key contains 'com:write:' and failMode 'closed'.
    - addWishlistItem R8 closed: R7 ok then R8 { allowed:false } → friendly string, no call.
    - addWishlistItem never throws: medusaAgentFetch rejects new Error('boom') → resolves to a string.
    - removeWishlistItem: pinned {wishlist_ref:'w-1'}; params {product_id:'prod_1'}; mock → { removed:true }; assert result matches /removed/i (idempotent-safe), body === { guest_ref:'w-1', product_id:'prod_1' }, path '/agent/wishlists/remove'; plus no-owner + R7/R8-closed + never-throw cases mirroring add.
  </behavior>
  <action>
    Create src/lib/medusa/wishlist-owner.ts exporting `resolveWishlistOwner(commerce: Record<string, unknown>): { customer_id: string } | { guest_ref: string } | null`. Per 135-RESEARCH Pattern 3: `if (typeof commerce.cus === 'string' && commerce.cus) return { customer_id: commerce.cus }; if (typeof commerce.wishlist_ref === 'string' && commerce.wishlist_ref) return { guest_ref: commerce.wishlist_ref }; return null`. Return EXACTLY one key (spread only the resolved key at the call site) — never both, never an undefined sibling (stuscle ownerSchema.refine(`!!customer_id !== !!guest_ref`) rejects zero or two owner keys with 400). NO owner param is ever read from `params`.

    Create src/lib/medusa/actions/wishlist-add.ts — `addWishlistItem(params: Record<string, unknown>, creds: MedusaCredentials, ctx: MedusaExecCtx): Promise<string>`. Structure (mirror add-to-cart.ts, but NO region/variant-resolution/cart-bootstrap):
      1. `try {` … `const { sessionKey, commerce } = await loadPinnedContext(ctx)`.
      2. `const owner = resolveWishlistOwner(commerce); if (!owner) return "Nothing's saved to a wishlist yet — browse the store or ask me to save an item and I'll start one for you."` (NO store call).
      3. R7: `rateLimit('com:write:' + sessionKey, 10, 60, { failMode: 'closed' })` → deny string. R8: `rateLimit('com:write:day:' + (ctx.conversationId ?? sessionKey), 60, 86400, { failMode: 'closed' })` → deny string. SAME shared keys as cart writes. Do NOT call bumpConversationWriteCount and do NOT touch COMMERCE_WRITE_ACTIONS (135-RESEARCH Open Q1: follow CONTEXT — R7/R8 + SIDE_EFFECTING only; wishlist writes stay out of the 3/turn + 25/conversation cart caps).
      4. `const productId = typeof params.product_id === 'string' ? params.product_id : undefined; if (!productId) return 'Tell me which product (name or link) you want to save.'` `const variantId = typeof params.variant_id === 'string' ? params.variant_id : undefined`.
      5. `const { item } = await medusaAgentFetch<{ item: { product?: { title?: string } } }>(creds, '/agent/wishlists/add', ctx.organizationId, { ...owner, product_id: productId, ...(variantId ? { variant_id: variantId } : {}) })`.
      6. `const title = item.product?.title ?? 'that item'; return `Saved ${title} to your wishlist.`` — idempotent-safe wording (135-RESEARCH Pitfall 5: stuscle cannot distinguish already-saved vs newly-saved; do NOT claim "already saved").
      7. catch ladder: `if (err instanceof MedusaApiError && err.status === 409) return "Your wishlist is full (100 items max) — I couldn't save that one."` → `if (err instanceof MedusaRateLimitError) return 'Too many store requests just now — try again in a moment.'` → `if (err instanceof Error && err.name === 'TimeoutError') return 'The store took too long to respond.'` → default `return "I couldn't save that to your wishlist just now."` (also covers 401 — a config/clock failure must NOT imply the user's input was bad; 135-RESEARCH Pitfall 7).

    Create src/lib/medusa/actions/wishlist-remove.ts — `removeWishlistItem(params, creds, ctx)` identical scaffold to add, but: path '/agent/wishlists/remove', response typed `{ removed: boolean }`, success string `"Removed that from your wishlist."` (idempotent-safe — stuscle returns removed:true even when nothing matched), no 409 branch needed (remove has no wishlist_full), same no-owner/R7/R8/never-throw handling.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/medusa-wishlist.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx vitest run tests/medusa-wishlist.test.ts` green.
    - `grep -n "resolveWishlistOwner" src/lib/medusa/wishlist-owner.ts src/lib/medusa/actions/wishlist-add.ts src/lib/medusa/actions/wishlist-remove.ts` shows the resolver defined once and imported by both executors.
    - `grep -n "com:write:" src/lib/medusa/actions/wishlist-add.ts src/lib/medusa/actions/wishlist-remove.ts` shows R7 + R8 with the shared cart-write keys.
    - `grep -n "product_id\|variant_id\|customer_id\|guest_ref\|cart_id\|email" src/lib/medusa/actions/wishlist-add.ts` shows product_id/variant_id read from params but customer_id/guest_ref only assembled from resolveWishlistOwner (never from params); no cart_id/email.
    - `grep -n "already saved\|existing\|created" src/lib/medusa/actions/wishlist-add.ts` returns NOTHING (no over-claiming, no inspecting non-existent fields — Pitfall 5).
    - `grep -n "bumpConversationWriteCount\|COMMERCE_WRITE_ACTIONS" src/lib/medusa/actions/wishlist-add.ts src/lib/medusa/actions/wishlist-remove.ts` returns NOTHING (wishlist writes stay out of the cart-only per-turn/per-conversation caps).
  </acceptance_criteria>
  <done>resolveWishlistOwner returns exactly one owner key or null; add/remove executors call the signed surface with exactly-one-owner bodies, enforce R7/R8 fail-closed, map 409→full, never throw, and use idempotent-safe wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: listWishlist executor (R6 read, item rendering)</name>
  <files>src/lib/medusa/actions/wishlist-list.ts, tests/medusa-wishlist.test.ts</files>
  <read_first>src/lib/medusa/actions/get-cart.ts, src/lib/medusa/wishlist-owner.ts, src/lib/medusa/actions/wishlist-add.ts</read_first>
  <behavior>
    RED first — extend tests/medusa-wishlist.test.ts with a listWishlist describe block:
    - list happy path: pinned {cus:'cus_9'}; mock medusaAgentFetch → { wishlist:{ id:'wl_1', items:[ { id:'wi_1', product_id:'p1', variant_id:null, product:{ title:'Sweatshirt', handle:'sweatshirt', thumbnail:null } }, { id:'wi_2', product_id:'p2', variant_id:null, product:{ title:'Tote', handle:'tote', thumbnail:null } } ] } }; assert result contains 'Sweatshirt' and 'Tote'; assert body === { customer_id:'cus_9' } and path === '/agent/wishlists/list'.
    - list empty: items [] → returns 'Your wishlist is empty.'
    - list no owner: commerce {} → friendly "nothing saved yet" string, medusaAgentFetch NOT called.
    - list R6 memory: assert the rateLimit key contains 'com:read:' with failMode 'memory'; when { allowed:false } → friendly limit string, no call.
    - list never throws: medusaAgentFetch rejects → resolves to a string.
  </behavior>
  <action>
    Create src/lib/medusa/actions/wishlist-list.ts — `listWishlist(creds: MedusaCredentials, ctx: MedusaExecCtx): Promise<string>` (NO params arg — mirror getMedusaCart's (creds, ctx) signature; anti-IDOR structural guarantee). Structure:
      1. `try {` `const { sessionKey, commerce } = await loadPinnedContext(ctx)`.
      2. `const owner = resolveWishlistOwner(commerce); if (!owner) return "Nothing's saved to a wishlist yet — browse the store or ask me to save an item and I'll start one for you."` (same string as add; no store call).
      3. R6: `const r6 = await rateLimit('com:read:' + sessionKey, 30, 60, { failMode: 'memory' }); if (!r6.allowed) return "You've hit the store-lookup limit for a moment — try again shortly."`.
      4. `const { wishlist } = await medusaAgentFetch<{ wishlist: { items: Array<{ product_id: string; product?: { title?: string } | null }> } }>(creds, '/agent/wishlists/list', ctx.organizationId, owner)`.
      5. `const items = wishlist.items ?? []; if (items.length === 0) return 'Your wishlist is empty.'` `const lines = items.map((i) => i.product?.title ?? i.product_id); return `Your wishlist:\n${lines.join('\n')}``.
      6. catch ladder mirroring get-cart.ts: MedusaRateLimitError → 'Too many store lookups just now — try again in a moment.'; TimeoutError → 'The store took too long to respond.'; default → "I couldn't reach the store just now." (no 409 branch — list has no wishlist_full).
    NOTE (discretion, defer): storefront-relative product URLs would need creds.storefrontUrl + commerce.country_code — do NOT add here (135-RESEARCH Open Q4; nice-to-have, out of scope).
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/medusa-agent-fetch.test.ts tests/medusa-wishlist.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx vitest run tests/medusa-agent-fetch.test.ts tests/medusa-wishlist.test.ts` all green.
    - `grep -n "com:read:" src/lib/medusa/actions/wishlist-list.ts` shows R6 with failMode 'memory'.
    - `grep -n "export async function listWishlist" src/lib/medusa/actions/wishlist-list.ts` shows the (creds, ctx) signature with NO params argument.
    - `grep -n "Your wishlist is empty" src/lib/medusa/actions/wishlist-list.ts` matches (empty-state string).
    - `npm run build` (typecheck gate) passes with no new errors in the touched files.
  </acceptance_criteria>
  <done>listWishlist renders pinned-owner wishlist items under the R6 read budget, handles empty/no-owner, and never throws.</done>
</task>

</tasks>

<verification>
- `CI=true npx vitest run tests/medusa-agent-fetch.test.ts tests/medusa-wishlist.test.ts --reporter=dot` green (signing vector + medusaAgentFetch + owner resolution + 3 executors).
- `npm run build` passes (type gate for the 5 new source files; the storefront/backend build does not gate on lint).
- Do NOT gate on full `npm test` — ~58 pre-existing unrelated failures (135-VALIDATION.md). Scope to the touched test files only.
- The live signed round trip against a running Stuscle backend is E2E-deferred (135-VALIDATION.md Manual-Only table) — unit tests mock the transport.
</verification>

<success_criteria>
- signAgentBody returns the committed bare-hex vector for `test-secret`/`1750000000`/`{"a":1}`; medusaAgentFetch signs-then-sends the identical body string with header ts == signed ts, R11 before fetch, MedusaApiError on non-2xx (WSL-02).
- Three executors call `/agent/wishlists/{add,remove,list}` with owner resolved ONLY from pinned cus/wishlist_ref (exactly one key), never from params; no owner param exists in any signature (WSL-01).
- add/remove enforce R7/R8 fail-closed on shared cart-write keys; list enforces R6 memory (WSL-02).
- Every failure path (409/401/timeout/rate-limit/no-owner) returns a friendly string; nothing throws into the tool loop; wording is idempotent-safe.
</success_criteria>

<output>
After completion, create `.planning/workstreams/medusa-commerce/phases/135-wishlist-tools/135-01-SUMMARY.md`.
</output>
