---
phase: 134-cart-write-tools
plan: 03
type: execute
wave: 3
depends_on: ["134-02"]
files_modified:
  - src/widget/index.ts
  - public/widget.js
  - public/widget-test.html
  - tests/widget.test.ts
autonomous: true
requirements: [CRT-04]
must_haves:
  truths:
    - "When a commerce SSE frame arrives, the widget dispatches a window CustomEvent 'xphere:commerce' carrying { action, cartId, itemCount, sig }"
    - "The Phase 133 cart_created cache-clear still fires (additive — re-fetch the context token on the next send)"
    - "The rebuilt public/widget.js is committed and contains the xphere:commerce dispatch; tests/widget.test.ts stays green"
  artifacts:
    - path: "src/widget/index.ts"
      provides: "commerce SSE re-dispatch branch + widened SSEEvent"
      contains: "xphere:commerce"
    - path: "public/widget.js"
      provides: "rebuilt bundle containing the re-dispatch (esbuild output, committed)"
      contains: "xphere:commerce"
    - path: "public/widget-test.html"
      provides: "CRT-04 manual re-dispatch checklist + window listener snippet"
      contains: "xphere:commerce"
  key_links:
    - from: "src/widget/index.ts consumeStream onEvent"
      to: "window (host page)"
      via: "dispatchEvent(new CustomEvent('xphere:commerce', { detail }))"
      pattern: "CustomEvent\\('xphere:commerce'"
---

<objective>
Re-dispatch the agent's `commerce` SSE events to the host storefront page as a
`window` CustomEvent, so the (frozen) stuscle commerce bridge can adopt/refresh the cart.
Extend the Phase 133 `commerce`/`cart_created` branch that already exists in the widget
stream consumer, rebuild the bundle, and keep the widget test suite green.

Purpose: This closes the loop — Wave 2 emits `commerce` events; the widget must forward
them to the page. The detail shape `{ action, cartId, itemCount, sig }` must match contract
§6 exactly (the stuscle bridge routes cart_updated -> refreshAgentCart, cart_created -> adoptAgentCart).

Output: src/widget/index.ts (re-dispatch), public/widget.js (rebuilt + committed),
public/widget-test.html (manual checklist), tests/widget.test.ts (bundle assertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/workstreams/medusa-commerce/phases/134-cart-write-tools/134-RESEARCH.md
@.planning/research/INTEGRATION-CONTRACT.md
@src/widget/index.ts
@public/widget-test.html
@tests/widget.test.ts

<interfaces>
<!-- Exact edit points in the widget (Phase 133 already wired the commerce/cart_created branch). -->

SSEEvent interface (src/widget/index.ts ~line 572):
```typescript
interface SSEEvent { event: string; sessionId?: string; text?: string; name?: string; action?: string }
// ADD: cartId?: string; itemCount?: number; sig?: string
```

onEvent handler (~line 852) — Phase 133 already has:
```typescript
} else if (evt.event === 'commerce' && evt.action === 'cart_created') {
  cachedToken = null   // force context re-fetch on next send
  cachedExp = 0
}
```
Replace/extend so ANY `evt.event === 'commerce'` re-dispatches, AND cart_created keeps the cache-clear.

Contract §6 detail shape (must match exactly — the stuscle bridge is frozen):
`window.dispatchEvent(new CustomEvent('xphere:commerce', { detail: { action, cartId, itemCount, sig } }))`

Build: `npm run build:widget` -> esbuild bundles src/widget/index.ts to public/widget.js.
tests/widget.test.ts reads public/widget.js from disk (loadWidget) — REBUILD before running it.
public/widget.js is committed (un-ignored since 133-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: widget re-dispatch branch + widened SSEEvent + widget-test.html checklist</name>
  <files>src/widget/index.ts, public/widget-test.html</files>
  <read_first>
    - src/widget/index.ts (SSEEvent ~572; onEvent commerce/cart_created branch ~852)
    - public/widget-test.html (existing manual checklist structure)
    - 134-RESEARCH.md "Widget re-dispatch (CRT-04)" code example
  </read_first>
  <action>
    1. Widen `SSEEvent` (~line 572) with `cartId?: string; itemCount?: number; sig?: string`.
    2. In the `onEvent` handler, change the existing `commerce`/`cart_created` branch to fire for ANY commerce event:
       ```
       } else if (evt.event === 'commerce') {
         window.dispatchEvent(new CustomEvent('xphere:commerce', {
           detail: { action: evt.action, cartId: evt.cartId, itemCount: evt.itemCount, sig: evt.sig },
         }))
         if (evt.action === 'cart_created') { cachedToken = null; cachedExp = 0 } // keep Phase 133 cache-clear
       }
       ```
       Keep it vanilla TS (no React/Next imports). The detail keys must be exactly action/cartId/itemCount/sig (contract §6).
    3. `public/widget-test.html`: add a CRT-04 checklist item and a listener snippet the tester pastes in the console:
       `window.addEventListener('xphere:commerce', e => console.log('xphere:commerce', e.detail))` — with a note to simulate a cart_created then cart_updated frame and confirm both re-dispatch with the right detail. (Manual host-page verification is deferred like 133-03 when no live stuscle stack is running.)
  </action>
  <verify>
    <automated>grep -n "CustomEvent('xphere:commerce'" src/widget/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cartId?: string" src/widget/index.ts` shows SSEEvent widened with cartId/itemCount/sig
    - `grep -n "CustomEvent('xphere:commerce'" src/widget/index.ts` matches
    - the branch fires for any `evt.event === 'commerce'` (not just cart_created) and still clears the cache on cart_created
    - `grep -n "xphere:commerce" public/widget-test.html` matches (listener snippet)
  </acceptance_criteria>
  <done>Widget source re-dispatches every commerce event as an xphere:commerce CustomEvent with the contract §6 detail, preserves the cart_created cache-clear, and widget-test.html documents the manual check.</done>
</task>

<task type="auto">
  <name>Task 2: rebuild + commit public/widget.js and lock the bundle assertion</name>
  <files>public/widget.js, tests/widget.test.ts</files>
  <read_first>
    - tests/widget.test.ts (loadWidget reads public/widget.js from disk; current suite count)
    - package.json (build:widget script)
  </read_first>
  <action>
    1. Run `npm run build:widget` (esbuild → public/widget.js). Commit the rebuilt bundle (public/widget.js is tracked since 133-03). Pitfall 7: the jsdom widget tests eval the BUILT file — a stale bundle silently passes old behavior.
    2. Extend `tests/widget.test.ts` with an assertion that the built `public/widget.js` (read via the existing readFileSync/loadWidget path) contains the string `xphere:commerce` — a cheap, live-stack-free proof the re-dispatch shipped in the bundle. Do NOT weaken any existing assertion; the suite must stay fully green (it was 11/11 after 133-03 — keep it green, adjust the expected count only if you add a test).
  </action>
  <verify>
    <automated>npm run build:widget && CI=true npx vitest run tests/widget.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "xphere:commerce" public/widget.js` >= 1 (bundle rebuilt with the dispatch)
    - tests/widget.test.ts asserts the built bundle contains `xphere:commerce`
    - full tests/widget.test.ts suite green (no regressions from the Phase 133 baseline)
  </acceptance_criteria>
  <done>public/widget.js is rebuilt and committed with the re-dispatch; the widget test suite proves the bundle carries the xphere:commerce dispatch and stays green.</done>
</task>

</tasks>

<verification>
- `npm run build:widget` succeeds; `CI=true npx vitest run tests/widget.test.ts` green.
- public/widget.js committed and contains `xphere:commerce`.
- Detail shape is exactly { action, cartId, itemCount, sig } (contract §6); cart_created still clears the cached context token.
- Manual host-page re-dispatch (live stuscle bridge) documented in widget-test.html; deferrable when no live stack, per 133-03 precedent.
</verification>

<success_criteria>
- Widget forwards every commerce SSE event to the host page as CustomEvent('xphere:commerce').
- Rebuilt public/widget.js committed; widget tests green with a bundle-content assertion.
- No live stack required for the automated gate.
</success_criteria>

<output>
After completion, create `.planning/workstreams/medusa-commerce/phases/134-cart-write-tools/134-03-SUMMARY.md`.
</output>
