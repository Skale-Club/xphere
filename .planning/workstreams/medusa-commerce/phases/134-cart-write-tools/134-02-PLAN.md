---
phase: 134-cart-write-tools
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - src/lib/action-engine/execute-action.ts
  - src/lib/agent-runtime/run-agent.ts
  - src/lib/workflows/spec.ts
  - tests/medusa-dispatch.test.ts
  - tests/medusa-wiring.test.ts
  - tests/medusa-spec.test.ts
autonomous: true
requirements: [CRT-01, CRT-02, CRT-03]
must_haves:
  truths:
    - "medusa_add_to_cart and medusa_update_cart_item route to the real executors (no longer the 'not available yet' stub)"
    - "ActionContext carries emitStructured and passes it through to the medusa write executors"
    - "The streaming run-agent path sets emitStructured: emit on the executeAction ActionContext; the blocking path omits it"
    - "A 4th side-effecting commerce write in one turn returns a clean denial string BEFORE executeAction, in both the blocking and streaming tool loops"
    - "The LLM can select both write tools — they are registered in ACTION_DESCRIPTIONS and workflows/spec.ts NODES with integration_required ['medusa'] and no visitor-scoped id params"
  artifacts:
    - path: "src/lib/action-engine/execute-action.ts"
      provides: "ActionContext.emitStructured + real dispatch of the 2 write cases"
      contains: "addToCartMedusa"
    - path: "src/lib/agent-runtime/run-agent.ts"
      provides: "emitStructured:emit (streaming) + per-turn commerce-write cap in both loops + ACTION_DESCRIPTIONS entries"
      contains: "checkCommerceWritesPerTurn"
    - path: "src/lib/workflows/spec.ts"
      provides: "medusa_add_to_cart + medusa_update_cart_item NODES"
      contains: "medusa_add_to_cart"
  key_links:
    - from: "src/lib/action-engine/execute-action.ts"
      to: "src/lib/medusa/actions/add-to-cart.ts"
      via: "case medusa_add_to_cart -> addToCartMedusa(params, creds, ctx)"
      pattern: "addToCartMedusa\\("
    - from: "src/lib/agent-runtime/run-agent.ts (streaming executeAction call)"
      to: "ActionContext.emitStructured"
      via: "emitStructured: emit"
      pattern: "emitStructured: emit"
    - from: "src/lib/agent-runtime/run-agent.ts"
      to: "src/lib/agent-runtime/idempotency.ts COMMERCE_WRITE_ACTIONS"
      via: "per-turn cap guard before executeAction"
      pattern: "COMMERCE_WRITE_ACTIONS"
---

<objective>
Wire the Wave 1 executors into the live agent pipeline: dispatch the two write cases in
execute-action.ts, thread the SSE emitter through ActionContext on the streaming path only,
enforce the per-turn commerce-write cap in both run-agent tool loops, and register the two
tools so the LLM (and the workflow authoring surface) can select them.

Purpose: Wave 1 built the executors in isolation; this plan makes them reachable and streamed.
No new store logic — pure composition of the primitives Plan 01 exported.

Output: execute-action.ts (dispatch + ActionContext.emitStructured), run-agent.ts
(emitStructured:emit streaming, per-turn cap, ACTION_DESCRIPTIONS), spec.ts (2 NODES), and
source-assertion tests aligned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/workstreams/medusa-commerce/phases/134-cart-write-tools/134-RESEARCH.md
@.planning/research/INTEGRATION-CONTRACT.md
@src/lib/action-engine/execute-action.ts
@src/lib/agent-runtime/run-agent.ts
@src/lib/workflows/spec.ts
@src/lib/medusa/credentials.ts
@tests/medusa-dispatch.test.ts
@tests/medusa-wiring.test.ts
@tests/medusa-spec.test.ts

<interfaces>
<!-- What Plan 01 (wave 1) exported, and the exact call sites to touch. -->

From src/lib/medusa/actions/add-to-cart.ts + update-cart-item.ts (Plan 01):
```typescript
export async function addToCartMedusa(params, creds: MedusaCredentials, ctx: MedusaExecCtx): Promise<string>
export async function updateCartItemMedusa(params, creds: MedusaCredentials, ctx: MedusaExecCtx): Promise<string>
```

From src/lib/agent-runtime/idempotency.ts (Plan 01):
```typescript
export const COMMERCE_WRITE_ACTIONS: Set<string> // medusa_add_to_cart + medusa_update_cart_item
```

From src/lib/agent-runtime/guardrails.ts (Plan 01):
```typescript
export function checkCommerceWritesPerTurn(count: number, max?: number): string | null
```

execute-action.ts current state (Plan 132-04): the 2 write types sit in a grouped stub
returning 'That commerce action is not available yet.' alongside 4 others. The read tools
already show the pattern to mirror (lines ~460-469): load creds via getMedusaCredentialsForOrg,
return friendly strings on missing ctx/creds, call the executor with (params, creds, ctx).
ActionContext (lines ~68-81) must gain `emitStructured?: (obj: Record<string, unknown>) => void`.

run-agent.ts call sites:
- Blocking executeAction: ~line 775-791 (ActionContext object WITHOUT emit — leave emitStructured OUT).
- Streaming executeAction: ~line 1242-1249; `const emit = (obj: object) => controller.enqueue(encode(obj))` is in closure scope (~line 1002). ADD `emitStructured: emit` to THIS ActionContext only.
- Both loops declare `let toolCallIndex = 0` per invocation (~657 blocking, ~1180 streaming) — add `let commerceWrites = 0` beside each.
- ACTION_DESCRIPTIONS object: ~line 208-230 (medusa read keys already present — add the 2 write keys).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: execute-action.ts — ActionContext.emitStructured + real dispatch of the 2 write cases</name>
  <files>src/lib/action-engine/execute-action.ts, tests/medusa-dispatch.test.ts</files>
  <read_first>
    - src/lib/action-engine/execute-action.ts (ActionContext ~68-81; medusa read block ~457-469; stub group ~470-481)
    - src/lib/medusa/actions/add-to-cart.ts, update-cart-item.ts (Plan 01 signatures)
    - tests/medusa-dispatch.test.ts (~line 69 read-tool dispatch pattern; ~124 stub-group assertion)
  </read_first>
  <action>
    1. Add `emitStructured?: (obj: Record<string, unknown>) => void` to the `ActionContext` interface (with a comment: set on the streaming path; executors null-check). This makes ActionContext structurally carry what MedusaExecCtx expects, so the existing `ctx` pass-through to the medusa executors delivers it.
    2. Import `addToCartMedusa` from `@/lib/medusa/actions/add-to-cart` and `updateCartItemMedusa` from `@/lib/medusa/actions/update-cart-item`.
    3. REMOVE `case 'medusa_add_to_cart':` and `case 'medusa_update_cart_item':` from the grouped stub (leave the other 4 — wishlist add/remove/list + get_order_status — stubbed). Add a real dispatch block MIRRORING the read-tools block: friendly string on missing ctx/org/supabase; `getMedusaCredentialsForOrg`; on missing creds return "No store is connected to this workspace yet."; then `if (actionType === 'medusa_add_to_cart') return addToCartMedusa(params, medusaCreds, ctx)` else `return updateCartItemMedusa(params, medusaCreds, ctx)`. Pass `ctx` straight through (it now carries emitStructured + conversationId).
    4. Update `tests/medusa-dispatch.test.ts`: (a) the stub-group test now covers FOUR remaining stubs — rename/adjust so it still asserts `case 'medusa_get_order_status':` + the "not available yet" string remain, but no longer implies the 2 cart writes are stubbed; (b) add a dispatch test that mocks `@/lib/medusa/actions/add-to-cart` + `@/lib/medusa/credentials` and asserts `executeAction('medusa_add_to_cart', ...)` calls `addToCartMedusa` (and same for update-cart-item). Follow the existing mock idiom in the file.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/medusa-dispatch.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "addToCartMedusa\|updateCartItemMedusa" src/lib/action-engine/execute-action.ts` matches both imports + both dispatch calls
    - `grep -n "emitStructured" src/lib/action-engine/execute-action.ts` matches (ActionContext field)
    - `medusa_add_to_cart` / `medusa_update_cart_item` no longer appear in the grouped stub returning "not available yet" (they route to executors)
    - tests/medusa-dispatch.test.ts green
  </acceptance_criteria>
  <done>Both write action types dispatch to the real executors; ActionContext carries emitStructured; the remaining 4 medusa actions stay stubbed; dispatch tests green.</done>
</task>

<task type="auto">
  <name>Task 2: run-agent.ts — emitStructured:emit (streaming) + per-turn commerce-write cap in both loops</name>
  <files>src/lib/agent-runtime/run-agent.ts, tests/medusa-wiring.test.ts</files>
  <read_first>
    - src/lib/agent-runtime/run-agent.ts (blocking loop ~657-810; streaming loop ~1180-1258; emit closure ~1002)
    - src/lib/agent-runtime/idempotency.ts (COMMERCE_WRITE_ACTIONS export)
    - src/lib/agent-runtime/guardrails.ts (checkCommerceWritesPerTurn)
    - 134-RESEARCH.md "run-agent wiring (streaming path — per-turn cap + emitStructured)"
  </read_first>
  <action>
    1. Import `COMMERCE_WRITE_ACTIONS` from `@/lib/agent-runtime/idempotency` and `checkCommerceWritesPerTurn` from `@/lib/agent-runtime/guardrails` (guardrails is already imported — add the named import).
    2. STREAMING call site (~1242-1249): add `emitStructured: emit` to the ActionContext object literal (the last field). `emit` (accepts `object`) is assignable to `(obj: Record<string, unknown>) => void` — no cast. Do NOT touch the blocking call site's context object (it must OMIT emitStructured so executors null-check).
    3. Per-turn cap — BOTH loops: beside each `let toolCallIndex = 0`, add `let commerceWrites = 0`. Inside each `execute()`, AFTER the resolveAgentTool/intersection checks and BEFORE `executeAction(...)`, add:
       ```
       if (COMMERCE_WRITE_ACTIONS.has(capturedActionType)) {
         const turnDenial = checkCommerceWritesPerTurn(++commerceWrites)
         if (turnDenial) {
           toolCallsLog.push({ name: capturedToolName, args: JSON.parse(JSON.stringify(toolArgs)) as Json, denied: true, denied_reason: 'commerce_turn_cap' })
           return turnDenial
         }
       }
       ```
       Return the string (never throw). Place it AFTER the idempotency cache-hit early-return so a replay short-circuits without double-incrementing commerceWrites.
    4. ACTION_DESCRIPTIONS (~208): add
       `medusa_add_to_cart: "Add a product to the visitor's cart (creates the cart if there is none). Quantity is clamped 1-10. No cart id parameter — the cart is bound to this chat."`
       and `medusa_update_cart_item: "Change the quantity of an item already in the visitor's cart, or remove it (quantity 0). Matches the item by name — no id parameters."`
    5. Extend `tests/medusa-wiring.test.ts` (source-assertion idiom already in the file): assert run-agent source contains `medusa_add_to_cart:` + `medusa_update_cart_item:` in ACTION_DESCRIPTIONS, `emitStructured: emit`, `COMMERCE_WRITE_ACTIONS`, and `checkCommerceWritesPerTurn`. Add an assertion that `commerceWrites` appears (the per-turn counter) in both loops.
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/medusa-wiring.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emitStructured: emit" src/lib/agent-runtime/run-agent.ts` matches exactly once (streaming only)
    - `grep -c "commerceWrites" src/lib/agent-runtime/run-agent.ts` >= 4 (declared + used in both loops)
    - `grep -n "checkCommerceWritesPerTurn\|COMMERCE_WRITE_ACTIONS" src/lib/agent-runtime/run-agent.ts` matches both
    - `grep -n "medusa_add_to_cart:\|medusa_update_cart_item:" src/lib/agent-runtime/run-agent.ts` matches (ACTION_DESCRIPTIONS)
    - the blocking executeAction context object does NOT contain emitStructured (only the streaming one does)
    - tests/medusa-wiring.test.ts green
  </acceptance_criteria>
  <done>Streaming path passes emitStructured:emit; per-turn cap returns a clean denial string before executeAction in both loops; both write tools are described for the LLM.</done>
</task>

<task type="auto">
  <name>Task 3: workflows/spec.ts — register the 2 write tools as NODES</name>
  <files>src/lib/workflows/spec.ts, tests/medusa-spec.test.ts</files>
  <read_first>
    - src/lib/workflows/spec.ts (medusa read NODES ~360-385; NodeSpec shape ~307)
    - tests/medusa-spec.test.ts (~line 16 MEDUSA_TYPES list + NODE assertions)
  </read_first>
  <action>
    Add two NodeSpec entries after the `medusa_get_cart` node, mirroring its shape:
    - `medusa_add_to_cart`: kind 'action', description "Add a product to the visitor's cart (creates the cart if none). Quantity clamped 1-10.", `integration_required: ['medusa']`, `params_schema: { type:'object', properties: { product_id: { type:'string' }, variant_id: { type:'string' }, quantity: { type:'number' } } }`, examples `[{ product_id: 'prod_123', quantity: 1 }]`. NO cart_id/customer_id/email params (anti-IDOR).
    - `medusa_update_cart_item`: kind 'action', description "Change an item's quantity in the visitor's cart, or remove it (quantity 0).", `integration_required: ['medusa']`, `params_schema: { type:'object', properties: { item_title_or_variant: { type:'string' }, quantity: { type:'number' } } }`, examples `[{ item_title_or_variant: 'Sweatshirt', quantity: 2 }]`.
    Update `tests/medusa-spec.test.ts`: extend MEDUSA_TYPES (or add cases) so the spec test asserts both write types are present in NODES with `integration_required: ['medusa']` and carry no visitor-scoped id params (assert `product_id`/`variant_id`/`item_title_or_variant`/`quantity` only — no `cart_id`, `customer_id`, `email`).
  </action>
  <verify>
    <automated>CI=true npx vitest run tests/medusa-spec.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "medusa_add_to_cart\|medusa_update_cart_item" src/lib/workflows/spec.ts` matches both NODES
    - neither new node's params_schema contains `cart_id`, `customer_id`, or `email`
    - tests/medusa-spec.test.ts green
  </acceptance_criteria>
  <done>Both write tools are registered NODES with medusa integration_required and id-free schemas; spec test green.</done>
</task>

</tasks>

<verification>
- `CI=true npx vitest run tests/medusa-dispatch.test.ts tests/medusa-wiring.test.ts tests/medusa-spec.test.ts` green.
- `npm run build` (typecheck) green — the ActionContext -> MedusaExecCtx pass-through and `emitStructured: emit` assignment must type-check.
- Streaming path passes the emitter; blocking path omits it; per-turn cap present in both loops.
</verification>

<success_criteria>
- The two write tools are live: dispatched to real executors, described in ACTION_DESCRIPTIONS, registered as spec NODES.
- ActionContext.emitStructured exists; streaming sets `emitStructured: emit`; blocking omits it.
- checkCommerceWritesPerTurn gates the 4th commerce write per turn in both tool loops, returning a clean string.
- No visitor-scoped id parameters in either tool's schema.
</success_criteria>

<output>
After completion, create `.planning/workstreams/medusa-commerce/phases/134-cart-write-tools/134-02-SUMMARY.md`.
</output>
