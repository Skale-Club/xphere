---
phase: 61
plan: 01
title: Remove Twilio duplicate + numbers count badge
status: complete
---

# Plan 61-01: Index cleanup

## Tasks

1. `src/components/integrations/integrations-table.tsx`: remove `twilio` from `ALL_PROVIDERS`; add header comment documenting the routing rule (single-credential → sheet table; multi-resource → dedicated page).
2. `src/app/(dashboard)/integrations/page.tsx`:
   - Compute `activeTwilioNumberCount` from `listTwilioNumbers()`
   - `hasTwilio = hasTwilioIntegration && activeTwilioNumberCount > 0` (replaces the bare "row exists" check)
   - New `twilioMeta` sub-label (e.g. "3 numbers configured" / "1 number configured" / "Credentials saved · 0 numbers")
   - `DedicatedIntegration` gains optional `meta?: string`
   - `DedicatedCard` renders `meta` as a small tertiary line beneath the description
   - Twilio card description rewritten to mention multi-number explicitly

## Success criteria

- [x] API-key providers table renders 6 rows (Vapi, GoHighLevel, Cal.com, OpenAI, Anthropic, OpenRouter), Twilio is removed
- [x] Twilio card pill flips to "Connected" only when both credentials AND ≥1 active number exist
- [x] Numbers count appears under the Twilio card description
- [x] Routing rule documented as a code comment
- [x] `tsc --noEmit` clean
