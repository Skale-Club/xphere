---
phase: 62
plan: 01
title: Extract SectionCard; migrate Google Reviews; verify dedicated pages
status: complete
---

# Plan 62-01: Visual unification

## Tasks

1. Create `src/components/integrations/section-card.tsx` — extract `SectionCard` (the canonical primitive for dedicated integration pages) from `twilio-settings.tsx`. Add optional `pillToneOverride` to support advisory sections.
2. Refactor `src/components/integrations/twilio-settings.tsx` to import from the shared path; remove the internal `SectionCard` function and unused `ExternalLink` import.
3. Refactor `src/app/(dashboard)/integrations/google-reviews/page.tsx`:
   - Switch from hand-rolled header div to `<PageContainer>` + `<PageHeader>` with `back`, eyebrow, eyebrowIcon, actions
   - Replace each `<Card>` block (5 cards) with `<SectionCard>` — same step semantics, canonical chrome
   - Drop the colored gradient top-stripe and pill-style colored icon-backgrounds (visual noise vs the unified language)
   - Replace the "Almost there." inline card with `<EmptyState>`
   - Keep `font-serif` only on the two large metrics (3xl average rating + 3xl total reviews) — intentional display-text accent
4. Audit Meta / Evolution / ManyChat / Google Contacts pages — all 6 dedicated pages already use `<PageHeader>`/`<PageContainer>` per grep. No structural refactor needed for those.

## Success criteria

- [x] `<SectionCard>` lives at `@/components/integrations/section-card`
- [x] `twilio-settings.tsx` and `google-reviews/page.tsx` both import it from the shared path
- [x] Google Reviews page renders all original functionality (keys, business search, status grid, recent reviews, embed snippet, plus the not-yet-configured empty state)
- [x] No remaining `font-serif` on dedicated-page H1s (Google Reviews H1 is via `<PageHeader>`)
- [x] `tsc --noEmit` clean
