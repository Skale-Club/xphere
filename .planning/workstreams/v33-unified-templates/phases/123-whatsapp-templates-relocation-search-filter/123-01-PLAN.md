---
phase: 123-whatsapp-templates-relocation-search-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/settings/whatsapp-templates/page.tsx
  - src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx
  - src/app/(dashboard)/integrations/whatsapp/templates/page.tsx
  - src/app/(dashboard)/integrations/whatsapp/templates/create-template-button.tsx
  - src/app/(dashboard)/integrations/whatsapp/templates/sync-templates-button.tsx
  - src/app/(dashboard)/integrations/whatsapp/templates/sync-zernio-templates-button.tsx
  - src/app/(dashboard)/integrations/whatsapp/actions.ts
  - src/components/settings/settings-sub-nav.tsx
  - src/components/integrations/panels/whatsapp-cloud-panel.tsx
  - src/components/chat/chat-area/send-template-dialog.tsx
autonomous: true
requirements: [WAT-01, WAT-02, WAT-03, WAT-04, WAT-05]

must_haves:
  truths:
    - "Admin reaches the WhatsApp templates screen from a real Settings nav entry (Communications section), not only via a contextual 'Manage templates' button"
    - "Admin can type a name into a search box and see the WhatsApp template list filter live, client-side, with no page reload"
    - "Admin can filter the list by status, category, and language, independently or combined with each other and with the name search (AND logic)"
    - "Meta Cloud accounts still see their templates grouped by status exactly as before; Zernio accounts still see their templates grouped by status exactly as before; orgs with neither provider still see the 'connect a provider' fallback"
    - "The integration panel 'Manage templates' button and the chat template picker fallback link both land on the new /settings/whatsapp-templates route"
  artifacts:
    - path: "src/app/(dashboard)/settings/whatsapp-templates/page.tsx"
      provides: "Relocated Server Component: fetches Meta Cloud / Zernio / fallback data exactly as the old page did, renders filter bar + grouped template list"
      min_lines: 150
    - path: "src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx"
      provides: "Client component: name search input + Status/Category/Language native selects, filters an in-memory template array"
      min_lines: 60
    - path: "src/components/settings/settings-sub-nav.tsx"
      provides: "New 'WhatsApp Templates' nav item under Communications pointing to /settings/whatsapp-templates"
      contains: "/settings/whatsapp-templates"
  key_links:
    - from: "src/components/settings/settings-sub-nav.tsx"
      to: "src/app/(dashboard)/settings/whatsapp-templates/page.tsx"
      via: "Link href in SECTIONS Communications array"
      pattern: "settings/whatsapp-templates"
    - from: "src/app/(dashboard)/settings/whatsapp-templates/page.tsx"
      to: "src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx"
      via: "Server component passes fetched+normalized template array as props to client filter component"
      pattern: "WhatsAppTemplatesFilters"
    - from: "src/components/integrations/panels/whatsapp-cloud-panel.tsx"
      to: "/settings/whatsapp-templates"
      via: "Manage templates Link href"
      pattern: "href=\"/settings/whatsapp-templates\""
    - from: "src/components/chat/chat-area/send-template-dialog.tsx"
      to: "/settings/whatsapp-templates"
      via: "Create template fallback Link href"
      pattern: "href=\"/settings/whatsapp-templates\""
    - from: "src/app/(dashboard)/integrations/whatsapp/actions.ts"
      to: "/settings/whatsapp-templates"
      via: "revalidatePath calls after template mutations"
      pattern: "revalidatePath\\('/settings/whatsapp-templates'\\)"
---

<objective>
Relocate the existing WhatsApp templates screen from the nav-orphaned `/integrations/whatsapp/templates` route to a real Settings entry at `/settings/whatsapp-templates`, and add client-side search (by name) plus status/category/language filters — all while preserving the exact dual-provider (Meta Cloud / Zernio) data-fetching and grouped-by-status rendering behavior.

Purpose: WhatsApp templates today are only reachable via a contextual "Manage templates" button buried in the integration panel. This phase gives admins a first-class Settings nav entry and the search/filter tools needed to find a template among many, without touching the underlying sync/approval mechanics or provider tables (WAT-01..05).

Output:
- New route `src/app/(dashboard)/settings/whatsapp-templates/page.tsx` (Server Component, same data-fetching logic as the old page, now rendering a filter bar above the grouped sections)
- New client component `whatsapp-templates-filters.tsx` (name search + 3 native selects, client-side instant filtering)
- Old route at `src/app/(dashboard)/integrations/whatsapp/templates/page.tsx` removed (along with its 3 sibling button files, moved to the new location)
- Nav entry added to Settings → Communications
- Both external entry points (`whatsapp-cloud-panel.tsx`, `send-template-dialog.tsx`) repointed to the new route
- `revalidatePath` calls in `actions.ts` updated to the new path so template creation/sync still refreshes the right screen
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v33-unified-templates/PROJECT.md
@.planning/workstreams/v33-unified-templates/ROADMAP.md
@.planning/workstreams/v33-unified-templates/STATE.md
@.planning/workstreams/v33-unified-templates/phases/123-whatsapp-templates-relocation-search-filter/123-CONTEXT.md

<interfaces>
<!-- Verified against the LIVE files on 2026-07-02. CONTEXT.md line numbers were approximate — these are exact. -->

## Current page: src/app/(dashboard)/integrations/whatsapp/templates/page.tsx (341 lines, full file)

Exports a single default async Server Component `WhatsAppTemplatesPage`. Structure:

```typescript
interface TemplateRow {
  id: string
  meta_template_id: string
  name: string
  language: string
  category: 'MARKETING' | 'UTILITY' | 'AUTHENTICATION'
  status: 'APPROVED' | 'PENDING' | 'REJECTED' | 'PAUSED' | 'DISABLED'
  components: unknown
  body_variable_count: number
  header_variable_count: number
  synced_at: string
}

interface ZernioTemplateRow {
  id: string
  name: string
  language: string
  category: string
  status: string
  components: unknown
  created_at: string
  updated_at: string
}

interface CloudAccount {
  id: string
  display_name: string
  phone_number_e164: string | null
  last_synced_at: string | null
}

const STATUS_COLOR: Record<string, string> = {
  APPROVED: 'bg-emerald-500/20 text-emerald-300 border-emerald-500/30',
  PENDING:  'bg-amber-500/20 text-amber-300 border-amber-500/30',
  REJECTED: 'bg-rose-500/20 text-rose-300 border-rose-500/30',
  PAUSED:   'bg-orange-500/20 text-orange-300 border-orange-500/30',
  DISABLED: 'bg-zinc-500/20 text-zinc-300 border-zinc-500/30',
}
```

Three branches, in order:
1. **Meta Cloud** (`whatsapp_cloud_accounts` row exists, `is_active=true`) — fetches `whatsapp_templates` filtered by `cloud_account_id`, groups by status via `groupCloudByStatus()`, renders sections in order `['APPROVED','PENDING','PAUSED','REJECTED','DISABLED']`, header shows phone/display name + last synced, actions row has `<CreateTemplateButton provider="cloud" />` + `<SyncTemplatesButton />`.
2. **Zernio** (`integrations` row with `provider='zernio', is_active=true` exists) — resolves account via `listZernioWhatsAppAccounts(apiKey)` (decrypt via `decrypt(zernioRow.encrypted_api_key)`), fetches `zernio_whatsapp_templates` filtered by `org_id`, groups via `groupZernioByStatus()`, renders sections in order `['APPROVED','PENDING','REJECTED','DISABLED']` (no PAUSED), header shows "Zernio · {accountName}", actions row has `<CreateTemplateButton provider="zernio" accountId={firstAccountId} />` + `<SyncZernioTemplatesButton />`.
3. **Neither connected** — fallback with `Link href="/integrations"`.

Sub-components/utilities in the SAME file (not exported elsewhere): `EmptyState`, `CloudTemplateCard`, `ZernioTemplateCard`, `groupCloudByStatus`, `groupZernioByStatus`, `extractBody`, `highlightVariables`. All must move with the page verbatim (only add filtering, do not restructure the branching or the card rendering).

Imports pulled from:
- `@/lib/supabase/server` — `createClient, getUser`
- `./sync-templates-button` — `SyncTemplatesButton` (relative import — moves with the page)
- `./sync-zernio-templates-button` — `SyncZernioTemplatesButton` (relative import — moves with the page)
- `./create-template-button` — `CreateTemplateButton` (relative import — moves with the page)
- `@/lib/phone-numbers/format` — `formatPhoneDisplay`
- `@/app/(dashboard)/integrations/whatsapp/actions` — `getZernioIntegrationSummary` (imported but unused in current file — carry over as-is, do not "fix")
- `@/lib/zernio/whatsapp-templates` — `listZernioWhatsAppAccounts`
- `@/lib/crypto` — `decrypt`

## Sibling files that must move alongside page.tsx (relative imports, no hardcoded old route path inside them)

- `create-template-button.tsx` — client component, imports `TemplateComposerDialog` / `ZernioTemplateComposerDialog` from `@/components/integrations/whatsapp/...` (absolute paths — safe to move, no edits needed to these imports)
- `sync-templates-button.tsx` — client component, imports `syncCloudTemplates` from `@/app/(dashboard)/integrations/whatsapp/actions` (absolute path — safe to move as-is)
- `sync-zernio-templates-button.tsx` — client component, imports `syncZernioTemplatesAction` from `@/app/(dashboard)/integrations/whatsapp/actions` (absolute path — safe to move as-is)

None of these 3 files reference the page's own route path — they can be moved byte-for-byte into the new folder.

## src/app/(dashboard)/integrations/whatsapp/actions.ts — revalidatePath calls to update

4 occurrences of `revalidatePath('/integrations/whatsapp/templates')` at lines 220, 241, 400, 502 (verified live). Change ALL 4 to `revalidatePath('/settings/whatsapp-templates')`.

## src/components/settings/settings-sub-nav.tsx (132 lines, full file, verified live)

```typescript
interface NavItem {
  href: string
  label: string
  icon: LucideIcon
}
interface NavSection {
  heading: string
  items: NavItem[]
}
```

Current Communications section (lines 54-61):
```typescript
  {
    heading: 'Communications',
    items: [
      { href: '/calls/settings', label: 'Call Center', icon: Phone },
      { href: '/settings/email-templates', label: 'Email Templates', icon: Mail },
      { href: '/settings/widget', label: 'Chat Widget', icon: MessageSquare },
    ],
  },
```

NOTE: Phase 122 (Settings Nav Cleanup) removes the Call Center item and moves Chat Widget to Build — that is a DIFFERENT, independent phase/plan and may land before, after, or in parallel with this one. This plan must NOT remove or move those two items — only ADD the new WhatsApp Templates item to whatever the Communications `items` array looks like at execution time. If Phase 122 already ran, the array will be shorter; if not, it still has all 3 original items. Read the live file before editing (see read_first) and append/insert the new item without deleting anything else in the array.

Add `{ href: '/settings/whatsapp-templates', label: 'WhatsApp Templates', icon: MessageCircle }`. `MessageCircle` is NOT currently imported in this file (verified) and does not collide with any other icon in use here (`Phone`, `Mail`, `MessageSquare` already used for other items) — add `MessageCircle` to the `lucide-react` import list.

## src/components/integrations/panels/whatsapp-cloud-panel.tsx — line 279 (verified live)

```tsx
          <Button size="sm" variant="ghost" asChild>
            <Link href="/integrations/whatsapp/templates">
              Manage templates <ExternalLink className="ml-1 h-3 w-3" />
            </Link>
          </Button>
```
Change href to `/settings/whatsapp-templates`. Nothing else in this ~4400-line file references the old route.

## src/components/chat/chat-area/send-template-dialog.tsx — line 254 (verified live)

```tsx
            <Button
              asChild
              variant="ghost"
              className="gap-1.5 text-[12.5px] text-accent"
            >
              <Link href="/integrations/whatsapp/templates" target="_blank" rel="noopener noreferrer">
                <ExternalLink className="h-3.5 w-3.5" />
                Create template
              </Link>
            </Button>
```
Change href to `/settings/whatsapp-templates`. Keep `target="_blank" rel="noopener noreferrer"` unchanged.

## Settings page structural convention (from src/app/(dashboard)/settings/api-keys/page.tsx and .../organization-templates/page.tsx)

Plain settings pages in this codebase are simple `px-*/py-*` containers with a manual `<h1>` — they do NOT use the shared `PageHeader` title/description props (that component only renders a right-aligned `actions` slot now; title/description props are accepted for back-compat but never rendered — see `src/components/layout/page-header.tsx`). The CURRENT WhatsApp templates page already follows this simpler manual-`<h1>` pattern (`px-6 py-6 max-w-5xl` wrapper, manual `<h1 className="text-[18px] font-semibold ...">`). Keep that exact wrapper/heading style at the new location — do NOT introduce `PageContainer`/`PageHeader` (that would be an unrequested visual change).

## Native `<select>` filter styling precedent (from send-template-dialog.tsx line ~164-176, already in codebase)

```tsx
<select
  value={selectedId}
  onChange={(e) => handleSelect(e.target.value)}
  className="w-full h-9 px-3 rounded-[8px] border border-border bg-bg-secondary text-[13px] text-text-primary"
>
  <option value="">— Select —</option>
  {/* ...options... */}
</select>
```
Reuse this exact className pattern for the 3 filter `<select>` elements (Status/Category/Language) so the filter bar visually matches existing native-select usage in the codebase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate WhatsApp templates page to /settings/whatsapp-templates and repoint all entry points</name>
  <files>
    src/app/(dashboard)/settings/whatsapp-templates/page.tsx (new)
    src/app/(dashboard)/integrations/whatsapp/templates/page.tsx (delete)
    src/app/(dashboard)/integrations/whatsapp/templates/create-template-button.tsx (move → src/app/(dashboard)/settings/whatsapp-templates/create-template-button.tsx)
    src/app/(dashboard)/integrations/whatsapp/templates/sync-templates-button.tsx (move → src/app/(dashboard)/settings/whatsapp-templates/sync-templates-button.tsx)
    src/app/(dashboard)/integrations/whatsapp/templates/sync-zernio-templates-button.tsx (move → src/app/(dashboard)/settings/whatsapp-templates/sync-zernio-templates-button.tsx)
    src/app/(dashboard)/integrations/whatsapp/actions.ts
    src/components/settings/settings-sub-nav.tsx
    src/components/integrations/panels/whatsapp-cloud-panel.tsx
    src/components/chat/chat-area/send-template-dialog.tsx
  </name>
  <read_first>
    src/app/(dashboard)/integrations/whatsapp/templates/page.tsx (full 341-line current implementation — read in full, do not assume the interfaces block above is exhaustive of formatting/whitespace)
    src/app/(dashboard)/integrations/whatsapp/templates/create-template-button.tsx
    src/app/(dashboard)/integrations/whatsapp/templates/sync-templates-button.tsx
    src/app/(dashboard)/integrations/whatsapp/templates/sync-zernio-templates-button.tsx
    src/app/(dashboard)/integrations/whatsapp/actions.ts (lines 200-260 and 390-510 for the 4 revalidatePath call sites)
    src/components/settings/settings-sub-nav.tsx (full file — current state of the Communications section may differ if Phase 122 already ran)
    src/components/integrations/panels/whatsapp-cloud-panel.tsx (around line 279)
    src/components/chat/chat-area/send-template-dialog.tsx (around line 254)
  </read_first>
  <action>
    1. Create directory `src/app/(dashboard)/settings/whatsapp-templates/`.
    2. Move the 3 sibling files byte-for-byte (no import changes needed inside them, verified above) into the new folder: `create-template-button.tsx`, `sync-templates-button.tsx`, `sync-zernio-templates-button.tsx`.
    3. Create `src/app/(dashboard)/settings/whatsapp-templates/page.tsx` containing the FULL current logic of the old `page.tsx` (all 3 branches: Meta Cloud / Zernio / fallback, plus every sub-component and utility function: `EmptyState`, `CloudTemplateCard`, `ZernioTemplateCard`, `groupCloudByStatus`, `groupZernioByStatus`, `extractBody`, `highlightVariables`, `STATUS_COLOR`, `TemplateRow`/`ZernioTemplateRow`/`CloudAccount` interfaces) — copied verbatim, imports unchanged (relative imports `./sync-templates-button` etc. now resolve correctly since siblings moved with it).
    4. In the new `page.tsx`, do NOT wire filtering into the Server Component data-fetching. This task ONLY relocates — filtering is Task 2. The one addition in this task: replace `templates` / `zTemplates` in each branch by passing the already-fetched array into a client component named `WhatsAppTemplatesFilters` that Task 2 will create, but for THIS task, keep the exact original grouped-render markup working standalone (Task 2 will refactor the grouped-render body into the client component). Concretely for Task 1: leave the page's rendering exactly as the original (grouped sections + cards inline in the Server Component), so the page is fully functional and buildable after this task alone, before Task 2 adds the filter UI on top.
    5. Delete the old directory `src/app/(dashboard)/integrations/whatsapp/templates/` entirely (all 4 files) after confirming the new folder builds.
    6. In `src/app/(dashboard)/integrations/whatsapp/actions.ts`, change all 4 occurrences of `revalidatePath('/integrations/whatsapp/templates')` to `revalidatePath('/settings/whatsapp-templates')`.
    7. In `src/components/settings/settings-sub-nav.tsx`: add `MessageCircle` to the `lucide-react` import statement. In the `Communications` section's `items` array, append (do not remove any existing items) a new entry: `{ href: '/settings/whatsapp-templates', label: 'WhatsApp Templates', icon: MessageCircle }`.
    8. In `src/components/integrations/panels/whatsapp-cloud-panel.tsx` line ~279, change `<Link href="/integrations/whatsapp/templates">` to `<Link href="/settings/whatsapp-templates">`.
    9. In `src/components/chat/chat-area/send-template-dialog.tsx` line ~254, change `<Link href="/integrations/whatsapp/templates" target="_blank" rel="noopener noreferrer">` to `<Link href="/settings/whatsapp-templates" target="_blank" rel="noopener noreferrer">`.
    10. Run `npm run build` to confirm no broken imports/type errors from the move.
  </action>
  <acceptance_criteria>
    - `test -f "src/app/(dashboard)/settings/whatsapp-templates/page.tsx"` exists
    - `test -f "src/app/(dashboard)/settings/whatsapp-templates/create-template-button.tsx"` exists
    - `test -f "src/app/(dashboard)/settings/whatsapp-templates/sync-templates-button.tsx"` exists
    - `test -f "src/app/(dashboard)/settings/whatsapp-templates/sync-zernio-templates-button.tsx"` exists
    - `test ! -d "src/app/(dashboard)/integrations/whatsapp/templates"` — old directory no longer exists
    - `grep -c "revalidatePath('/settings/whatsapp-templates')" src/app/(dashboard)/integrations/whatsapp/actions.ts` returns `4`
    - `grep -c "revalidatePath('/integrations/whatsapp/templates')" src/app/(dashboard)/integrations/whatsapp/actions.ts` returns `0`
    - `grep "MessageCircle" src/components/settings/settings-sub-nav.tsx` matches both the import line and the new nav item line (at least 2 matches)
    - `grep "/settings/whatsapp-templates" src/components/settings/settings-sub-nav.tsx` matches
    - `grep "label: 'Email Templates'" src/components/settings/settings-sub-nav.tsx` and `grep "label: 'Chat Widget'" src/components/settings/settings-sub-nav.tsx` — at least one of these two OR the Call Center item still present/moved correctly per whatever Phase 122's current state is (existing items not deleted by this task — verify by reading the file, not just grepping one item)
    - `grep "/settings/whatsapp-templates" src/components/integrations/panels/whatsapp-cloud-panel.tsx` matches
    - `grep "/settings/whatsapp-templates" src/components/chat/chat-area/send-template-dialog.tsx` matches
    - `grep "integrations/whatsapp/templates" src/components/integrations/panels/whatsapp-cloud-panel.tsx` returns no matches
    - `grep "integrations/whatsapp/templates" src/components/chat/chat-area/send-template-dialog.tsx` returns no matches
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>New route builds and renders the exact same 3-branch behavior (Meta Cloud / Zernio / fallback) as the old route; old route directory removed; both external entry points and all 4 revalidatePath calls point at /settings/whatsapp-templates; nav has a new WhatsApp Templates item under Communications.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add name search + status/category/language filters to the relocated page</name>
  <files>
    src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx (new)
    src/app/(dashboard)/settings/whatsapp-templates/page.tsx
  </files>
  <read_first>
    src/app/(dashboard)/settings/whatsapp-templates/page.tsx (the file produced by Task 1 — read its actual final content/structure before refactoring, do not assume it is identical to the pre-move file's line numbers)
    src/components/chat/chat-area/send-template-dialog.tsx (lines ~160-176 for the native `<select>` styling pattern to copy)
  </read_first>
  <action>
    1. Create `src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx` as a `'use client'` component. It must accept a generic prop shape that works for BOTH the Meta Cloud row shape and the Zernio row shape (they share `name`, `language`, `category`, `status`, `id` but differ in a few other fields) — define a local minimal interface:
       ```typescript
       interface FilterableTemplate {
         id: string
         name: string
         language: string
         category: string
         status: string
       }
       ```
       Props: `{ templates: T[], statusOrder: string[], renderGroup: (status: string, items: T[]) => React.ReactNode }` where `T extends FilterableTemplate` — OR simpler: accept the already-rendered `<CloudTemplateCard>`/`<ZernioTemplateCard>` elements are NOT passed in; instead pass the raw template array plus a `renderCard: (tpl: T) => React.ReactNode` callback, and have this component own the grouping-by-status + filtering + section rendering (moving `groupCloudByStatus`-equivalent grouping logic into a local generic groupByStatus helper inside this file, parameterized by the `statusOrder` array so it works for both Cloud's 5-status order and Zernio's 4-status order).
    2. State: `searchTerm` (string, matches against `name`, case-insensitive substring), `statusFilter` (string, default `''` = all), `categoryFilter` (string, default `''` = all), `languageFilter` (string, default `''` = all).
    3. Derive filter dropdown options dynamically from the `templates` prop: unique `category` values present, unique `language` values present (sorted). Status dropdown options come from the `statusOrder` prop (fixed per-provider list), not derived from data, so all valid statuses are always selectable even if no template currently has that status.
    4. Filtering logic: an template passes if ALL active filters match (AND logic) — empty search term / empty select value ("") means that filter is inactive. Filtered results still get grouped and rendered via the existing per-status section layout (heading + count + grid), collapsing to only the sections that have matching items — sections with zero matches after filtering are omitted (already true of the base grouping logic, must remain true when filtered).
    5. Render a filter bar above the grouped list: one `<input type="text">` (placeholder "Search by name…") + 3 `<select>` elements (Status / Category / Language), each styled with the codebase's native-select classes: `"w-full h-9 px-3 rounded-[8px] border border-border bg-bg-secondary text-[13px] text-text-primary"` (search input can use the existing shadcn `Input` component from `@/components/ui/input` instead of a raw `<input>` for visual consistency with the rest of the app). Layout: flex row, wrap on small screens, e.g. `flex flex-wrap items-center gap-2 mb-4`.
    6. When no templates match the active filters (but templates exist and at least one filter is active), render a small inline "No templates match your filters." message instead of the grouped sections (do NOT reuse the `EmptyState` component, which is for the true zero-templates case — keep them visually distinct).
    7. Refactor `src/app/(dashboard)/settings/whatsapp-templates/page.tsx`: in the Meta Cloud branch, replace the inline grouped-section JSX (the `templates && templates.length > 0 ? (...) : <EmptyState>` block) with `<WhatsAppTemplatesFilters templates={templates ?? []} statusOrder={['APPROVED','PENDING','PAUSED','REJECTED','DISABLED']} renderCard={(tpl) => <CloudTemplateCard key={tpl.id} tpl={tpl} />} />` wrapped so the true-empty (`templates.length === 0`) case still renders the original `<EmptyState>` with its Meta-specific copy, unchanged. Do the equivalent in the Zernio branch with `statusOrder={['APPROVED','PENDING','REJECTED','DISABLED']}` and its own `<EmptyState>` copy. The "neither provider connected" branch is untouched (no templates to filter).
    8. Run `npm run build` to confirm types resolve (the generic `renderCard` callback must type-check against both `TemplateRow` and `ZernioTemplateRow`).
  </action>
  <acceptance_criteria>
    - `test -f "src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx"` exists
    - `grep "'use client'" src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx` matches (first line)
    - `grep -E "useState" src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx` matches (client-side filter state)
    - `grep "searchTerm" src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx` matches
    - `grep -c "<select" src/app/(dashboard)/settings/whatsapp-templates/whatsapp-templates-filters.tsx` returns at least `3` (Status, Category, Language)
    - `grep "WhatsAppTemplatesFilters" src/app/(dashboard)/settings/whatsapp-templates/page.tsx` matches at least twice (Meta Cloud branch + Zernio branch usage)
    - `grep "APPROVED.*PENDING.*PAUSED.*REJECTED.*DISABLED" src/app/(dashboard)/settings/whatsapp-templates/page.tsx` matches (Cloud statusOrder preserved)
    - `grep "EmptyState" src/app/(dashboard)/settings/whatsapp-templates/page.tsx` still matches (true-empty case preserved, not deleted)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Admin sees a search input + 3 filter selects above the grouped WhatsApp templates list; typing a name or picking a status/category/language narrows the visible cards instantly client-side with AND-combinable filters; the true zero-templates empty state is unaffected; both Meta Cloud and Zernio provider paths use the shared filter component with their own status orders.</done>
</task>

</tasks>

<verification>
Manual smoke test after both tasks (documented here for the executor to self-check via build + grep only — no interactive checkpoint needed since this is UI-only, low-risk, and the acceptance criteria above are fully grep/build-verifiable):

1. `npm run build` passes with zero type errors.
2. `grep -r "integrations/whatsapp/templates" src/` returns zero matches anywhere in the codebase (confirms no dangling references to the old route in app code — the CONTEXT.md file itself will still mention it historically, that's fine, only src/ matters).
3. Settings nav (`settings-sub-nav.tsx`) contains a WhatsApp Templates entry under Communications pointing to `/settings/whatsapp-templates`.
4. The relocated page still branches correctly on Meta Cloud vs Zernio vs neither-connected (no branch logic was altered, only the render layer gained a filter wrapper).
</verification>

<success_criteria>
- WAT-01: `/settings/whatsapp-templates` exists as a real Settings nav destination.
- WAT-02: Name search input filters the list client-side, live.
- WAT-03: Status/Category/Language selects filter the list, combinable with each other and the search box.
- WAT-04: Meta Cloud and Zernio data-fetching, sync buttons, and create-template flows are byte-identical to before the move — zero behavior regression.
- WAT-05: `whatsapp-cloud-panel.tsx` "Manage templates" button and `send-template-dialog.tsx` fallback link both resolve to the new route.
</success_criteria>

<output>
After completion, create `.planning/workstreams/v33-unified-templates/phases/123-whatsapp-templates-relocation-search-filter/123-01-SUMMARY.md`
</output>
