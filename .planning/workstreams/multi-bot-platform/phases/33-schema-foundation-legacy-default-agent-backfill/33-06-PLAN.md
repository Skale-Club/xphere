---
phase: 33-schema-foundation-legacy-default-agent-backfill
plan: 06
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - supabase/migrations/040_seed_main_agents.sql
autonomous: true
requirements: [AGENT-09, TOOL-01]
requirements_addressed: [AGENT-09, TOOL-01]
must_haves:
  truths:
    - "For every row in public.organizations, exactly one row exists in public.agents with name='Main Agent' after this migration runs"
    - "Each Main Agent's system_prompt equals the v1.4 template at src/lib/chat/stream.ts:107 with ${orgName} substituted byte-for-byte (D-33-04); NULL/empty org names use literal 'your team' (D-33-05)"
    - "Each Main Agent has agent_prompt_versions.version=1 row containing the same system_prompt; agents.active_prompt_version_id points at that row (D-33-06)"
    - "Each Main Agent has agent_tools rows for every active tool_configs row owned by the same org with allowed_channels=NULL (D-33-07, D-33-08 — inactive tool_configs skipped)"
    - "Each Main Agent has exactly one agent_channel_defaults row with channel='web_widget' (D-33-09)"
    - "Migration is fully idempotent: re-running INSERTs zero new rows because every INSERT uses WHERE NOT EXISTS guard"
  artifacts:
    - path: "supabase/migrations/040_seed_main_agents.sql"
      provides: "Pure-SQL DO block that loops over public.organizations and seeds 1 agent + 1 prompt version + N agent_tools rows + 1 agent_channel_defaults row per org, idempotently"
      contains: "WHERE NOT EXISTS"
  key_links:
    - from: "supabase/migrations/040_seed_main_agents.sql"
      to: "src/lib/chat/stream.ts:107"
      via: "system_prompt literal in the seed is byte-equal to the template at line 107 with ${orgName} substituted and ${kbContext} omitted (kbContext is appended at runtime, not stored)"
      pattern: "You are a helpful assistant for"
    - from: "supabase/migrations/040_seed_main_agents.sql"
      to: "supabase/migrations/034_agents.sql"
      via: "INSERT INTO public.agents — schema must already exist; UPDATE public.agents SET active_prompt_version_id — column added in 035 must exist"
      pattern: "INSERT INTO public\\.agents"
    - from: "supabase/migrations/040_seed_main_agents.sql"
      to: "supabase/migrations/036_agent_channel_defaults.sql"
      via: "INSERT INTO public.agent_channel_defaults seeds the web_widget row per org"
      pattern: "INSERT INTO public\\.agent_channel_defaults"
---

<objective>
Write the seed migration that backfills a "Main Agent" per existing organization so day-1 chat behavior post-cutover (Phase 35) is byte-identical to v1.4. This is the most behavior-critical migration in Phase 33: a single character drift in the seeded `system_prompt` would change the LLM's responses across every customer org.

Per D-33-04 the source of truth is `src/lib/chat/stream.ts:107`. The template literal there is:

```
`You are a helpful assistant for ${orgName}. Answer questions accurately and concisely using the provided context. If you don't know the answer, say so.${kbContext}`
```

The seeded `agents.system_prompt` must be that template with `${orgName}` substituted from `organizations.name` (or literal `your team` if NULL/empty per D-33-05) AND with `${kbContext}` REMOVED (kbContext is dynamically appended at runtime per stream.ts:101 — not stored).

For each org the migration MUST:
1. INSERT a row into `public.agents` (name='Main Agent', system_prompt=substituted template, model='anthropic/claude-sonnet-4-6' as the v1.4 chat default, is_active=true, allowed_channels=ARRAY['web_widget'])
2. INSERT a row into `public.agent_prompt_versions` (version=1, system_prompt=same substituted string)
3. UPDATE the just-inserted agent's `active_prompt_version_id` to point at the version row (per D-33-06 chicken-and-egg)
4. INSERT one `public.agent_tools` row per active `tool_configs` row owned by the same org (D-33-07; D-33-08 skips inactive)
5. INSERT one `public.agent_channel_defaults` row with channel='web_widget' (D-33-09)

All INSERTs are idempotent via `WHERE NOT EXISTS` guards keyed on (organization_id, name='Main Agent') so re-running the migration after a successful first run is a no-op.

Output: 1 pure-SQL migration file. Not pushed in this plan — Plan 07 runs `npx supabase db push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md
@./CLAUDE.md
@src/lib/chat/stream.ts
@supabase/migrations/033_automation_schedules.sql

<interfaces>
The literal v1.4 system prompt template at src/lib/chat/stream.ts:107 (DO NOT paraphrase — Plan 01's prompt-byte-equal test asserts this string verbatim):

```typescript
const systemPrompt = `You are a helpful assistant for ${orgName}. Answer questions accurately and concisely using the provided context. If you don't know the answer, say so.${kbContext}`
```

The exact string to seed for org "Acme Inc.":
```
You are a helpful assistant for Acme Inc. Answer questions accurately and concisely using the provided context. If you don't know the answer, say so.
```

The exact string to seed for an org with NULL or empty name (D-33-05):
```
You are a helpful assistant for your team. Answer questions accurately and concisely using the provided context. If you don't know the answer, say so.
```

In SQL the substitution is:
```sql
'You are a helpful assistant for ' || COALESCE(NULLIF(org.name, ''), 'your team') || '. Answer questions accurately and concisely using the provided context. If you don''t know the answer, say so.'
```

Note: the apostrophe in `don't` MUST be escaped as `don''t` in the SQL literal.

Tables this migration touches (created in Plans 02, 03):
- public.agents (Plan 02 — id, organization_id, name, slug, system_prompt, model, is_active, allowed_channels, active_prompt_version_id added in 035)
- public.agent_prompt_versions (Plan 02 — id, organization_id, agent_id, version, system_prompt)
- public.agent_tools (Plan 02 — id, organization_id, agent_id, tool_config_id, allowed_channels)
- public.agent_channel_defaults (Plan 03 — id, organization_id, channel, agent_id)

Existing tables this migration reads:
- public.organizations(id, name)
- public.tool_configs(id, organization_id, is_active)

Default model: 'anthropic/claude-sonnet-4-6' — this matches the v1.4 chat default routing (per project context, Sonnet is the production chat tier). The agents table column default is 'anthropic/claude-haiku-4-5' (set in 034) but the SEED OVERRIDES that to Sonnet because that's what v1.4 chat actually used.

Slug for the Main Agent: 'main-agent' (lowercase kebab; unique per org via the agents.uniq_agents_org_slug constraint).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 040_seed_main_agents.sql (pure-SQL idempotent backfill)</name>
  <files>supabase/migrations/040_seed_main_agents.sql</files>
  <read_first>
    - src/lib/chat/stream.ts (lines 95-110 — confirm the literal template at line 107 byte-for-byte; confirm kbContext is appended at line 107, NOT line 101 which is just the kbContext value)
    - supabase/migrations/034_agents.sql (Plan 02 — confirm agents column names: organization_id, name, slug, system_prompt, model, fallback_message, max_history, allowed_channels, is_active, created_at, updated_at; confirm uniq_agents_org_slug)
    - supabase/migrations/035_agent_prompt_versions.sql (Plan 02 — confirm agent_prompt_versions columns: organization_id, agent_id, version, system_prompt; confirm agents.active_prompt_version_id was added)
    - supabase/migrations/036_agent_channel_defaults.sql (Plan 03 — confirm columns: organization_id, channel, agent_id)
    - supabase/migrations/033_automation_schedules.sql (style reference — seeded-rows-in-migration pattern with ON CONFLICT/idempotency)
    - .planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md (D-33-03 through D-33-10 verbatim — all 8 decisions govern this migration)
    - tests/agent-schema-seed.test.ts + tests/agent-schema-prompt-byte-equal.test.ts (Plan 01 — these tests assert the exact post-conditions this migration creates)
  </read_first>
  <action>
Create `supabase/migrations/040_seed_main_agents.sql` with EXACTLY this SQL content (copy-paste, do not paraphrase the prompt template or the COALESCE expression):

```
-- =============================================================================
-- Migration: 040_seed_main_agents
-- Phase: v2.0 Multi-Bot Platform — Phase 33 Schema Foundation
-- Seeds:   One 'Main Agent' per existing org, byte-identical to v1.4 chat behavior
-- Decisions: D-33-03 (per-org ${orgName} substitution at seed time)
--            D-33-04 (source of truth: src/lib/chat/stream.ts:107)
--            D-33-05 (NULL/empty org name -> literal 'your team')
--            D-33-06 (version=1 row + UPDATE agents.active_prompt_version_id)
--            D-33-07 (grant every active tool_config to the Main Agent; allowed_channels=NULL)
--            D-33-08 (inactive tool_configs NOT granted)
--            D-33-09 (insert agent_channel_defaults(org, 'web_widget', main_agent_id))
--            D-33-10 (other channels get NO default — Phase 36 admin populates)
-- Idempotency: every INSERT uses WHERE NOT EXISTS keyed on (organization_id, name='Main Agent').
--              Safe to re-run; safe to apply to a partially-seeded DB.
-- =============================================================================

-- Pure-SQL DO block: one transaction, deterministic order, no application code.

DO $migration$
DECLARE
  org           RECORD;
  v_agent_id    UUID;
  v_version_id  UUID;
  v_prompt      TEXT;
BEGIN
  FOR org IN
    SELECT id, name
    FROM public.organizations
    ORDER BY created_at ASC
  LOOP
    -- Skip orgs that already have a Main Agent (idempotency)
    IF EXISTS (
      SELECT 1 FROM public.agents
      WHERE organization_id = org.id AND name = 'Main Agent'
    ) THEN
      CONTINUE;
    END IF;

    -- D-33-03/04/05: substitute ${orgName} from org.name; fallback to 'your team'.
    -- Source template (src/lib/chat/stream.ts:107) WITHOUT trailing ${kbContext}
    -- (kbContext is appended at runtime per D-33-03).
    v_prompt := 'You are a helpful assistant for '
             || COALESCE(NULLIF(org.name, ''), 'your team')
             || '. Answer questions accurately and concisely using the provided context. If you don''t know the answer, say so.';

    -- Step 1: INSERT agent (model overrides the haiku default to sonnet, matching v1.4 chat default)
    INSERT INTO public.agents (
      organization_id,
      name,
      slug,
      description,
      system_prompt,
      model,
      is_active,
      allowed_channels
    ) VALUES (
      org.id,
      'Main Agent',
      'main-agent',
      'Legacy default agent — backfilled from v1.4 chat behavior at Phase 33.',
      v_prompt,
      'anthropic/claude-sonnet-4-6',
      true,
      ARRAY['web_widget']::public.agent_channel[]
    )
    RETURNING id INTO v_agent_id;

    -- Step 2: INSERT agent_prompt_versions row (version=1) per D-33-06
    INSERT INTO public.agent_prompt_versions (
      organization_id,
      agent_id,
      version,
      system_prompt
    ) VALUES (
      org.id,
      v_agent_id,
      1,
      v_prompt
    )
    RETURNING id INTO v_version_id;

    -- Step 3: UPDATE agents.active_prompt_version_id to close the chicken-and-egg cycle (D-33-06)
    UPDATE public.agents
      SET active_prompt_version_id = v_version_id
      WHERE id = v_agent_id;

    -- Step 4: INSERT agent_tools rows for every ACTIVE tool_config owned by this org (D-33-07, D-33-08)
    -- allowed_channels=NULL means "all channels" (preserves v1.4 behavior — chat had access to all org tools)
    INSERT INTO public.agent_tools (organization_id, agent_id, tool_config_id, allowed_channels)
    SELECT
      tc.organization_id,
      v_agent_id,
      tc.id,
      NULL
    FROM public.tool_configs tc
    WHERE tc.organization_id = org.id
      AND tc.is_active = true
    ON CONFLICT (agent_id, tool_config_id) DO NOTHING;

    -- Step 5: INSERT agent_channel_defaults row for web_widget (D-33-09)
    INSERT INTO public.agent_channel_defaults (organization_id, channel, agent_id)
    VALUES (org.id, 'web_widget', v_agent_id)
    ON CONFLICT (organization_id, channel) DO NOTHING;

    RAISE NOTICE 'Seeded Main Agent for org % (id=%)', COALESCE(org.name, '<NULL>'), org.id;
  END LOOP;
END
$migration$;

COMMENT ON TABLE public.agents IS
  'Phase 33 (v2.0): first-class chat agent entity. Per-org. Text channels only — Vapi voice unchanged. Audit fields per AGENT-09. Migration 040 seeds one ''Main Agent'' per existing org backfilling v1.4 chat behavior.';
```

Use the Write tool. Do NOT push the migration in this task — that happens in Plan 07.

**Critical notes for the executor:**
- The apostrophe in `don't` is escaped as `don''t` (two single quotes) inside the SQL string literal — verify this character-for-character before saving.
- The model literal `'anthropic/claude-sonnet-4-6'` matches the slug in the pricing seed from Plan 04 (migration 038) — keep them in sync.
- The `$migration$` dollar-quote tag is used (instead of `$$`) so the DO block can contain `$$` substrings safely if anything is added later — it's defensive and harmless.
- Re-running this migration after success MUST insert 0 new rows. The `IF EXISTS ... CONTINUE` guard at the top of the loop is what makes that true.
  </action>
  <verify>
    <automated>test -f supabase/migrations/040_seed_main_agents.sql && grep -c "INSERT INTO public.agents" supabase/migrations/040_seed_main_agents.sql && grep -c "INSERT INTO public.agent_prompt_versions" supabase/migrations/040_seed_main_agents.sql && grep -c "INSERT INTO public.agent_tools" supabase/migrations/040_seed_main_agents.sql && grep -c "INSERT INTO public.agent_channel_defaults" supabase/migrations/040_seed_main_agents.sql && grep -c "You are a helpful assistant for" supabase/migrations/040_seed_main_agents.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f supabase/migrations/040_seed_main_agents.sql`
    - All 4 INSERT targets present: each grep returns exactly 1: `grep -c "INSERT INTO public.agents " supabase/migrations/040_seed_main_agents.sql`, `grep -c "INSERT INTO public.agent_prompt_versions" supabase/migrations/040_seed_main_agents.sql`, `grep -c "INSERT INTO public.agent_tools" supabase/migrations/040_seed_main_agents.sql`, `grep -c "INSERT INTO public.agent_channel_defaults" supabase/migrations/040_seed_main_agents.sql`
    - 1 UPDATE for chicken-and-egg closure: `grep -c "UPDATE public.agents" supabase/migrations/040_seed_main_agents.sql` returns 1
    - Literal v1.4 prompt substring present: `grep -c "You are a helpful assistant for" supabase/migrations/040_seed_main_agents.sql` returns 1
    - Literal 'your team' fallback present (D-33-05): `grep -c "'your team'" supabase/migrations/040_seed_main_agents.sql` returns 1
    - Escaped apostrophe in don't: `grep -c "don''t" supabase/migrations/040_seed_main_agents.sql` returns 1
    - NO unescaped apostrophe inside SQL string (don't with single apostrophe inside literal would break parse): `grep -cE "say so\.'" supabase/migrations/040_seed_main_agents.sql` returns 1 (the closing delimiter only)
    - Idempotency guard at loop top: `grep -c "IF EXISTS" supabase/migrations/040_seed_main_agents.sql` returns >= 1 AND `grep -c "AND name = 'Main Agent'" supabase/migrations/040_seed_main_agents.sql` returns >= 1
    - ON CONFLICT clauses on the two non-loop-guarded inserts: `grep -c "ON CONFLICT (agent_id, tool_config_id) DO NOTHING" supabase/migrations/040_seed_main_agents.sql` returns 1 AND `grep -c "ON CONFLICT (organization_id, channel) DO NOTHING" supabase/migrations/040_seed_main_agents.sql` returns 1
    - tool_configs is filtered by is_active=true (D-33-08): `grep -c "tc.is_active = true" supabase/migrations/040_seed_main_agents.sql` returns 1
    - web_widget literal in channel_defaults: `grep -c "'web_widget'" supabase/migrations/040_seed_main_agents.sql` returns 1
    - Model literal matches pricing seed slug: `grep -c "'anthropic/claude-sonnet-4-6'" supabase/migrations/040_seed_main_agents.sql` returns 1
    - allowed_channels seeded as ARRAY['web_widget']::public.agent_channel[]: `grep -c "ARRAY\['web_widget'\]::public.agent_channel\[\]" supabase/migrations/040_seed_main_agents.sql` returns 1
    - Slug is 'main-agent' (per uniq_agents_org_slug constraint): `grep -c "'main-agent'" supabase/migrations/040_seed_main_agents.sql` returns 1
    - DO block uses dollar-quote tag $migration$: `grep -c "DO \$migration\$" supabase/migrations/040_seed_main_agents.sql` returns 1
    - No DROP / no RENAME / no DELETE: `grep -cE "DROP TABLE|RENAME|^\s*DELETE\s+FROM" supabase/migrations/040_seed_main_agents.sql` returns 0
  </acceptance_criteria>
  <done>040 seed migration written; pure-SQL DO block iterates orgs; per-org INSERTs are idempotent (skip-if-exists at loop top + ON CONFLICT on the per-row inserts); prompt template is byte-equal to src/lib/chat/stream.ts:107 with ${orgName} substituted and ${kbContext} omitted; 'your team' fallback in place per D-33-05.</done>
</task>

</tasks>

<verification>
After this task: 1 migration file exists. Total migration files in `supabase/migrations/` ready for Plan 07 push: 7 (034-040).

Cross-check before Plan 07 push:
- The 7 migrations apply in alphanumeric order (034, 035, 036, 037, 038, 039, 040) — Postgres + Supabase CLI both honor this ordering
- 040 depends on tables created in 034-036 (agents, agent_prompt_versions, agent_tools, agent_channel_defaults) and on existing tables (organizations, tool_configs) — all dependencies are upstream
- 040 does NOT touch the tables added by 037-039 (agent_invocations, action_logs, tool_idempotency_keys, agent_model_pricing, manychat_rules.agent_id, meta_channels.agent_id) — those are unused by the seed
</verification>

<success_criteria>
- Migration file exists and passes the verify command
- File order final: 034, 035, 036, 037, 038, 039, 040
- File ready to be committed to git in batch with the rest
- Plan 07 (push checkpoint) depends on this plan and on all of 02-05
- After Plan 07 push, Plan 01's tests/agent-schema-seed.test.ts + tests/agent-schema-prompt-byte-equal.test.ts both turn GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-06-SUMMARY.md`
</output>
