---
phase: 33-schema-foundation-legacy-default-agent-backfill
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/036_agent_channel_defaults.sql
  - supabase/migrations/037_agent_invocations.sql
autonomous: true
requirements: [OBS-01, OBS-02]
requirements_addressed: [OBS-01, OBS-02]
must_haves:
  truths:
    - "Migration 036 creates public.agent_channel_defaults with UNIQUE (organization_id, channel) so resolveAgentForChannel (Phase 34) can be a sub-ms lookup"
    - "Migration 037 creates public.agent_invocations (OBS-01) with all 4 mandatory indexes per D-33-13 and the agent_invocation_status + agent_invocation_mode enums per D-33-17/18"
    - "Migration 037 also adds the additive nullable agent_invocation_id (UUID FK) and trace_id (UUID NULL) columns to public.action_logs (OBS-02), plus an index on action_logs.trace_id per D-33-14"
    - "All RLS policies use the canonical (organization_id = (SELECT public.get_current_org_id())) pattern; agent_invocations has SELECT-only policy for authenticated users (writes are service-role only — runtime path)"
    - "All migrations idempotent (CREATE TABLE IF NOT EXISTS; ADD COLUMN IF NOT EXISTS; CREATE TYPE inside DO block)"
  artifacts:
    - path: "supabase/migrations/036_agent_channel_defaults.sql"
      provides: "public.agent_channel_defaults(id, organization_id, channel agent_channel, agent_id FK, created_at, updated_at) with UNIQUE (organization_id, channel); RLS; updated_at trigger"
      contains: "CREATE TABLE IF NOT EXISTS public.agent_channel_defaults"
    - path: "supabase/migrations/037_agent_invocations.sql"
      provides: "agent_invocation_status enum (success/error/aborted/skipped/denied); agent_invocation_mode enum (production/playground); public.agent_invocations table; 4 indexes per D-33-13; ALTER public.action_logs ADD agent_invocation_id + trace_id; index on action_logs.trace_id"
      contains: "CREATE TABLE IF NOT EXISTS public.agent_invocations"
  key_links:
    - from: "supabase/migrations/036_agent_channel_defaults.sql"
      to: "supabase/migrations/034_agents.sql"
      via: "FK from agent_channel_defaults.agent_id to agents.id; uses public.agent_channel enum from 034"
      pattern: "REFERENCES public\\.agents\\(id\\)"
    - from: "supabase/migrations/037_agent_invocations.sql"
      to: "supabase/migrations/034_agents.sql"
      via: "FK from agent_invocations.agent_id to agents.id; FK from parent_invocation_id self-ref"
      pattern: "REFERENCES public\\.agents\\(id\\)"
    - from: "supabase/migrations/037_agent_invocations.sql"
      to: "supabase/migrations/002_action_engine.sql"
      via: "ALTER TABLE public.action_logs ADD COLUMN agent_invocation_id + trace_id (additive — OBS-02)"
      pattern: "ALTER TABLE public\\.action_logs"
---

<objective>
Write migrations 036 + 037, completing the agent-resolver mapping (`agent_channel_defaults`) and the observability table (`agent_invocations`) plus the additive back-reference columns on `action_logs`. These are independent of migrations 034/035 in terms of file order — they all need `public.agents` to exist (which 034 creates), but 036 and 037 do not depend on each other.

Purpose: `agent_channel_defaults` is THE resolver Phase 35's web-widget cutover queries first (`resolveAgentForChannel(orgId, 'web_widget')`). `agent_invocations` is the day-1 observability sink Phase 34's runtime writes to on every `runAgent()` call. Both must exist before Phase 34 starts. The additive `action_logs` columns let Phase 34's tool executors back-link executions to the LLM call that triggered them — no breaking change to Vapi's existing `action_logs` consumers.

Output: 2 migration files. Not pushed in this plan — Plan 06 runs `npx supabase db push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md
@./CLAUDE.md
@supabase/migrations/001_foundation.sql
@supabase/migrations/002_action_engine.sql
@supabase/migrations/032_ghl_reengagement_sent.sql

<interfaces>
Reusable building blocks from earlier migrations:

From supabase/migrations/001_foundation.sql:
- public.organizations(id UUID PK, name TEXT, ...)
- public.get_current_org_id() RETURNS UUID
- public.update_updated_at() RETURNS TRIGGER

From supabase/migrations/002_action_engine.sql (existing — being ALTERed by migration 037):
- public.action_logs(id UUID PK, organization_id UUID, tool_config_id UUID NULL, vapi_call_id TEXT, tool_name TEXT, status TEXT CHECK in success/error/timeout, execution_ms INTEGER, request_payload JSONB, response_payload JSONB, error_detail TEXT, created_at TIMESTAMPTZ)
  Existing indexes: idx_action_logs_org_id, idx_action_logs_created, idx_action_logs_tool_config

From supabase/migrations/034_agents.sql (Plan 02):
- public.agents(id UUID PK, organization_id UUID, name, slug, ..., is_active)
- public.agent_channel enum (web_widget, whatsapp, messenger, instagram, manychat, telegram)

OBS-01 / OBS-02 columns (verbatim from REQUIREMENTS.md):
- agent_invocations: id, organization_id (FK + RLS), agent_id, parent_invocation_id NULL (self-FK), trace_id UUID NOT NULL, channel, conversation_id NULL, depth, status, user_message, assistant_reply, tool_calls JSONB, partner_calls JSONB, tokens_in, tokens_out, cost_usd NUMERIC(10,6), model, duration_ms, error_detail, mode (production|playground), created_at
- action_logs additive: agent_invocation_id UUID FK NULL + trace_id UUID NULL

D-33-13 mandatory indexes on agent_invocations:
1. (organization_id, created_at DESC) — dashboard list
2. (trace_id) — cross-table joins
3. (parent_invocation_id) — delegation tree
4. (agent_id, created_at DESC) — per-agent metrics

D-33-14: action_logs.trace_id gets a single index (trace_id).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 036_agent_channel_defaults.sql (per-org per-channel default agent mapping)</name>
  <files>supabase/migrations/036_agent_channel_defaults.sql</files>
  <read_first>
    - supabase/migrations/034_agents.sql (depends on `public.agents` and `public.agent_channel` enum)
    - supabase/migrations/032_ghl_reengagement_sent.sql (style reference)
    - .planning/research/ARCHITECTURE.md (lines 430-447 — agent_channel_defaults schema sketch)
    - .planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md (D-33-09, D-33-10)
  </read_first>
  <action>
Create `supabase/migrations/036_agent_channel_defaults.sql` with EXACTLY this SQL (copy-paste):

```
-- =============================================================================
-- Migration: 036_agent_channel_defaults
-- Phase: v2.0 Multi-Bot Platform — Phase 33 Schema Foundation
-- Creates: public.agent_channel_defaults — (org_id, channel) -> agent_id resolver mapping
-- RLS:     org-scoped via (SELECT public.get_current_org_id())
-- Decisions: D-33-01 (migration 3 of 6)
--            D-33-09 (Phase 33 only seeds 'web_widget'; other channels populated by Phase 36 admin)
-- Source: .planning/research/ARCHITECTURE.md Migration 036
-- =============================================================================

-- agent_channel_defaults: which agent owns inbound for (org, channel)

CREATE TABLE IF NOT EXISTS public.agent_channel_defaults (
  id              UUID                  PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id UUID                  NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE,
  channel         public.agent_channel  NOT NULL,
  agent_id        UUID                  NOT NULL REFERENCES public.agents(id) ON DELETE CASCADE,
  created_at      TIMESTAMPTZ           NOT NULL DEFAULT now(),
  updated_at      TIMESTAMPTZ           NOT NULL DEFAULT now(),
  CONSTRAINT uniq_agent_channel_defaults_org_channel UNIQUE (organization_id, channel)
);

ALTER TABLE public.agent_channel_defaults ENABLE ROW LEVEL SECURITY;

CREATE INDEX IF NOT EXISTS idx_agent_channel_defaults_org_channel
  ON public.agent_channel_defaults(organization_id, channel);

DROP POLICY IF EXISTS "agent_channel_defaults_all" ON public.agent_channel_defaults;
CREATE POLICY "agent_channel_defaults_all" ON public.agent_channel_defaults
  FOR ALL TO authenticated
  USING      (organization_id = (SELECT public.get_current_org_id()))
  WITH CHECK (organization_id = (SELECT public.get_current_org_id()));

DROP TRIGGER IF EXISTS trg_agent_channel_defaults_updated_at ON public.agent_channel_defaults;
CREATE TRIGGER trg_agent_channel_defaults_updated_at
  BEFORE UPDATE ON public.agent_channel_defaults
  FOR EACH ROW EXECUTE FUNCTION public.update_updated_at();

COMMENT ON TABLE public.agent_channel_defaults IS
  'Phase 33 (v2.0): resolves (org, channel) -> default agent for inbound dispatch. Phase 33 seeds only web_widget per D-33-09; other channels populated via Phase 36 admin UI.';
```

Use the Write tool. Do NOT push the migration in this task.
  </action>
  <verify>
    <automated>test -f supabase/migrations/036_agent_channel_defaults.sql && grep -c "CREATE TABLE IF NOT EXISTS public.agent_channel_defaults" supabase/migrations/036_agent_channel_defaults.sql && grep -c "UNIQUE (organization_id, channel)" supabase/migrations/036_agent_channel_defaults.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f supabase/migrations/036_agent_channel_defaults.sql`
    - Creates table: `grep -c "CREATE TABLE IF NOT EXISTS public.agent_channel_defaults" supabase/migrations/036_agent_channel_defaults.sql` returns 1
    - UNIQUE on (organization_id, channel): `grep -c "UNIQUE (organization_id, channel)" supabase/migrations/036_agent_channel_defaults.sql` returns 1
    - FK to agents on CASCADE: `grep -c "REFERENCES public.agents(id) ON DELETE CASCADE" supabase/migrations/036_agent_channel_defaults.sql` returns 1
    - Uses public.agent_channel enum: `grep -c "public.agent_channel" supabase/migrations/036_agent_channel_defaults.sql` returns >= 1
    - RLS enabled: `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/036_agent_channel_defaults.sql` returns 1
    - Canonical policy: `grep -cE "USING\s+\(organization_id = \(SELECT public\.get_current_org_id\(\)\)\)" supabase/migrations/036_agent_channel_defaults.sql` returns 1
    - updated_at trigger using shared helper: `grep -c "EXECUTE FUNCTION public.update_updated_at" supabase/migrations/036_agent_channel_defaults.sql` returns 1
    - No DROP / no RENAME: `grep -cE "DROP TABLE|RENAME" supabase/migrations/036_agent_channel_defaults.sql` returns 0
  </acceptance_criteria>
  <done>036 migration file written; resolver lookup index in place; ready for Plan 05 to seed web_widget rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write migration 037_agent_invocations.sql (observability table + action_logs additive cols)</name>
  <files>supabase/migrations/037_agent_invocations.sql</files>
  <read_first>
    - supabase/migrations/034_agents.sql (depends on public.agents + public.agent_channel)
    - supabase/migrations/002_action_engine.sql (ALTERing this — confirm action_logs current columns + indexes)
    - supabase/migrations/032_ghl_reengagement_sent.sql (style reference)
    - .planning/research/ARCHITECTURE.md (lines 451-487 — agent_invocations sketch — but EXTEND per D-33-17 to add 'aborted','skipped','denied' status values + per D-33-18 mode enum)
    - .planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-CONTEXT.md (D-33-13, D-33-14, D-33-17, D-33-18)
    - .planning/REQUIREMENTS.md (OBS-01 column list — verbatim source for which columns to include)
  </read_first>
  <action>
Create `supabase/migrations/037_agent_invocations.sql` with EXACTLY this SQL (copy-paste):

```
-- =============================================================================
-- Migration: 037_agent_invocations
-- Phase: v2.0 Multi-Bot Platform — Phase 33 Schema Foundation
-- Creates: agent_invocation_status + agent_invocation_mode enums
--          public.agent_invocations (OBS-01)
--          additive nullable agent_invocation_id + trace_id on public.action_logs (OBS-02)
-- Indexes: 4 mandatory on agent_invocations per D-33-13 + 1 on action_logs.trace_id per D-33-14
-- RLS:     SELECT-only for authenticated; INSERT/UPDATE service-role only (runtime writes via service-role client)
-- Decisions: D-33-01 (migration 4 of 6)
--            D-33-13 (4 indexes), D-33-14 (action_logs.trace_id index)
--            D-33-17 (status: success | error | aborted | skipped | denied)
--            D-33-18 (mode: production | playground)
-- Source: .planning/research/ARCHITECTURE.md Migration 037 (extended per D-33-17/18)
-- =============================================================================

-- Enums

DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'agent_invocation_status') THEN
    CREATE TYPE public.agent_invocation_status AS ENUM (
      'success', 'error', 'aborted', 'skipped', 'denied'
    );
  END IF;
END $$;

DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'agent_invocation_mode') THEN
    CREATE TYPE public.agent_invocation_mode AS ENUM (
      'production', 'playground'
    );
  END IF;
END $$;

-- agent_invocations: one row per runAgent() call (OBS-01)

CREATE TABLE IF NOT EXISTS public.agent_invocations (
  id                   UUID                              PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id      UUID                              NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE,
  agent_id             UUID                              NOT NULL REFERENCES public.agents(id) ON DELETE CASCADE,
  parent_invocation_id UUID                              REFERENCES public.agent_invocations(id) ON DELETE SET NULL,
  trace_id             UUID                              NOT NULL,
  channel              public.agent_channel              NOT NULL,
  conversation_id      UUID,
  session_id           TEXT,
  depth                INTEGER                           NOT NULL DEFAULT 0,
  status               public.agent_invocation_status    NOT NULL,
  mode                 public.agent_invocation_mode      NOT NULL DEFAULT 'production',
  user_message         TEXT,
  assistant_reply      TEXT,
  tool_calls           JSONB                             NOT NULL DEFAULT '[]'::jsonb,
  partner_calls        JSONB                             NOT NULL DEFAULT '[]'::jsonb,
  tokens_in            INTEGER,
  tokens_out           INTEGER,
  cost_usd             NUMERIC(10,6),
  model                TEXT,
  duration_ms          INTEGER,
  error_detail         TEXT,
  created_at           TIMESTAMPTZ                       NOT NULL DEFAULT now()
);

ALTER TABLE public.agent_invocations ENABLE ROW LEVEL SECURITY;

-- 4 mandatory indexes per D-33-13
CREATE INDEX IF NOT EXISTS idx_agent_invocations_org_created
  ON public.agent_invocations(organization_id, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_agent_invocations_trace
  ON public.agent_invocations(trace_id);

CREATE INDEX IF NOT EXISTS idx_agent_invocations_parent
  ON public.agent_invocations(parent_invocation_id)
  WHERE parent_invocation_id IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_agent_invocations_agent_created
  ON public.agent_invocations(agent_id, created_at DESC);

-- SELECT-only RLS for authenticated users; runtime writes via service-role client (bypasses RLS)
DROP POLICY IF EXISTS "agent_invocations_select" ON public.agent_invocations;
CREATE POLICY "agent_invocations_select" ON public.agent_invocations
  FOR SELECT TO authenticated
  USING (organization_id = (SELECT public.get_current_org_id()));

COMMENT ON TABLE public.agent_invocations IS
  'Phase 33 (v2.0): OBS-01 observability sink. One row per runAgent() call. Writes are service-role only (Phase 34 runtime). RLS-isolated reads for dashboard. status enum per D-33-17; mode per D-33-18.';

-- ---------------------------------------------------------------------------
-- action_logs additive columns (OBS-02)
-- ---------------------------------------------------------------------------

ALTER TABLE public.action_logs
  ADD COLUMN IF NOT EXISTS agent_invocation_id UUID
    REFERENCES public.agent_invocations(id) ON DELETE SET NULL;

ALTER TABLE public.action_logs
  ADD COLUMN IF NOT EXISTS trace_id UUID;

CREATE INDEX IF NOT EXISTS idx_action_logs_trace
  ON public.action_logs(trace_id)
  WHERE trace_id IS NOT NULL;

COMMENT ON COLUMN public.action_logs.agent_invocation_id IS
  'Phase 33 (v2.0): OBS-02 back-reference to agent_invocations. NULL = legacy v1.x action (e.g. Vapi tool call). Additive — does not break existing consumers.';

COMMENT ON COLUMN public.action_logs.trace_id IS
  'Phase 33 (v2.0): OBS-02 cross-table trace correlation. Same trace_id appears on the parent agent_invocation row + every action_logs row spawned by that invocation.';
```

Note: `agent_invocations.conversation_id` is `UUID` with NO FK to `conversations(id)` — this is intentional because `conversations.agent_id` is added in Phase 35 (D-33-20), and we want to avoid a circular dependency. Phase 35 can add the FK separately.

Use the Write tool. Do NOT push the migration in this task.
  </action>
  <verify>
    <automated>test -f supabase/migrations/037_agent_invocations.sql && grep -c "CREATE TABLE IF NOT EXISTS public.agent_invocations" supabase/migrations/037_agent_invocations.sql && grep -c "ADD COLUMN IF NOT EXISTS agent_invocation_id" supabase/migrations/037_agent_invocations.sql && grep -c "ADD COLUMN IF NOT EXISTS trace_id" supabase/migrations/037_agent_invocations.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f supabase/migrations/037_agent_invocations.sql`
    - Creates agent_invocations: `grep -c "CREATE TABLE IF NOT EXISTS public.agent_invocations" supabase/migrations/037_agent_invocations.sql` returns 1
    - Both enums created via DO blocks: `grep -c "CREATE TYPE public.agent_invocation_status" supabase/migrations/037_agent_invocations.sql` returns 1 AND `grep -c "CREATE TYPE public.agent_invocation_mode" supabase/migrations/037_agent_invocations.sql` returns 1
    - Status enum has all 5 values per D-33-17: `grep -cE "'success'.*'error'.*'aborted'.*'skipped'.*'denied'" supabase/migrations/037_agent_invocations.sql` returns 1
    - Mode enum has both values per D-33-18: `grep -cE "'production'.*'playground'" supabase/migrations/037_agent_invocations.sql` returns 1
    - All 4 indexes per D-33-13: each grep returns 1: `grep -c "idx_agent_invocations_org_created" supabase/migrations/037_agent_invocations.sql`, `grep -c "idx_agent_invocations_trace " supabase/migrations/037_agent_invocations.sql`, `grep -c "idx_agent_invocations_parent" supabase/migrations/037_agent_invocations.sql`, `grep -c "idx_agent_invocations_agent_created" supabase/migrations/037_agent_invocations.sql`
    - trace_id NOT NULL: `grep -cE "trace_id\s+UUID\s+NOT NULL" supabase/migrations/037_agent_invocations.sql` returns 1
    - parent_invocation_id self-FK with ON DELETE SET NULL: `grep -c "parent_invocation_id UUID" supabase/migrations/037_agent_invocations.sql` returns 1 AND `grep -c "REFERENCES public.agent_invocations(id) ON DELETE SET NULL" supabase/migrations/037_agent_invocations.sql` returns 1
    - SELECT-only RLS policy (no INSERT/UPDATE/DELETE policy for authenticated): `grep -c "FOR SELECT TO authenticated" supabase/migrations/037_agent_invocations.sql` returns 1 AND `grep -c "FOR INSERT TO authenticated" supabase/migrations/037_agent_invocations.sql` returns 0
    - action_logs additive columns: `grep -c "ADD COLUMN IF NOT EXISTS agent_invocation_id" supabase/migrations/037_agent_invocations.sql` returns 1 AND `grep -c "ADD COLUMN IF NOT EXISTS trace_id" supabase/migrations/037_agent_invocations.sql` returns 1
    - action_logs.trace_id index per D-33-14: `grep -c "idx_action_logs_trace" supabase/migrations/037_agent_invocations.sql` returns 1
    - No DROP / no RENAME: `grep -cE "DROP TABLE|RENAME" supabase/migrations/037_agent_invocations.sql` returns 0
  </acceptance_criteria>
  <done>037 migration file written; agent_invocations + both enums + 4 indexes; action_logs additively extended with 2 nullable columns + 1 index. Vapi action_logs consumers untouched (additive).</done>
</task>

</tasks>

<verification>
After both tasks: 2 migration files exist. Combined with Plan 02's 034/035, 4 migration files now sit in `supabase/migrations/` ready for Plan 06 push.

Cross-check: `agent_invocations.channel` uses `public.agent_channel` enum from 034 — confirm enum reference compiles at push time.
</verification>

<success_criteria>
- Both migration files exist and pass the verify commands
- File order: 034, 035, 036, 037 (no number conflicts)
- Files committed to git in same commit as Plan 02 (or same wave)
- Plan 04 (migrations 038/039) can be written in parallel with this plan since it only depends on Plan 02's `agents` table for one of its FKs (manychat_rules.agent_id, meta_channels.agent_id)
- Plan 05 (seed) depends on this plan because the seed inserts into agent_channel_defaults
</success_criteria>

<output>
After completion, create `.planning/phases/33-schema-foundation-legacy-default-agent-backfill/33-03-SUMMARY.md`
</output>
