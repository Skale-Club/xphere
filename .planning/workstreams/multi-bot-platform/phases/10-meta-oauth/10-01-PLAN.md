---
phase: 10-meta-oauth
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/meta-oauth-actions.test.ts
  - tests/meta-callback-route.test.ts
  - tests/meta-settings.test.tsx
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - META-05
  - META-06

must_haves:
  truths:
    - "Running npx vitest run tests/meta-* exits 0 with all tests reported as todo"
    - "Coverage exists for the OAuth launch action, CSRF cookie/state contract, and canonical callback redirect URI"
    - "Coverage exists for the callback route token exchange chain: code -> short-lived user token -> long-lived user token -> page access token"
    - "Coverage exists for settings UI behaviors: connected channels list, reconnect prompt on error 190, disconnect, and per-channel automation binding"
  artifacts:
    - path: "tests/meta-oauth-actions.test.ts"
      provides: "RED stubs for connect/disconnect/bind server actions"
      min_lines: 40
    - path: "tests/meta-callback-route.test.ts"
      provides: "RED stubs for GET /api/meta/callback"
      min_lines: 70
    - path: "tests/meta-settings.test.tsx"
      provides: "RED jsdom stubs for the Meta settings UI"
      min_lines: 70
  key_links:
    - from: "tests/meta-oauth-actions.test.ts"
      to: "src/app/(dashboard)/integrations/meta/actions.ts"
      via: "future server action imports"
      pattern: "connectMeta"
    - from: "tests/meta-callback-route.test.ts"
      to: "src/app/api/meta/callback/route.ts"
      via: "future GET import"
      pattern: "GET /api/meta/callback"
    - from: "tests/meta-settings.test.tsx"
      to: "src/components/integrations/meta-settings.tsx"
      via: "future UI render contract"
      pattern: "Connect with Facebook"
---

<objective>
Create RED contracts for the Meta OAuth phase before implementation begins.

Purpose: Lock the behavior for OAuth launch, callback token exchange, and the tenant-scoped Meta settings UI before Wave 1 writes production code.
Output: Three todo-only test files covering actions, callback route behavior, and settings UI states.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED stubs for Meta OAuth launch and channel management actions</name>
  <files>tests/meta-oauth-actions.test.ts</files>
  <read_first>
    - src/app/(dashboard)/integrations/actions.ts
    - tests/integrations.test.ts
    - .planning/research/ARCHITECTURE.md (Meta OAuth callback flow)
    - .planning/REQUIREMENTS.md (META-02, META-04, META-06)
  </read_first>
  <action>
    Create `tests/meta-oauth-actions.test.ts` using `it.todo()` stubs only.

    Required describe blocks and behaviors:
    - `describe('META-02: connectMeta')`
      - generates a CSRF state cookie before redirecting
      - redirects to `https://www.facebook.com/dialog/oauth`
      - requests `pages_show_list`, `pages_messaging`, `instagram_manage_messages`, and `pages_read_engagement`
      - uses `https://operator.skale.club/api/meta/callback` as the redirect URI
    - `describe('META-04: disconnectMetaChannel')`
      - deletes exactly one channel row for the active org
      - revalidates `/integrations/meta`
    - `describe('META-06: updateMetaChannelAutomation')`
      - allows different automation ids for messenger and instagram rows on the same page
      - allows clearing the automation binding

    Keep the file todo-only; no implementation code is created in this plan.
  </action>
  <acceptance_criteria>
    - `tests/meta-oauth-actions.test.ts` exists
    - file contains the three describe blocks named above
    - file contains at least 7 `it.todo(` stubs
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/meta-oauth-actions.test.ts --reporter=verbose</automated>
  </verify>
  <done>Server-side behavior for connect, disconnect, and automation binding is captured as RED coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED stubs for the Meta OAuth callback route</name>
  <files>tests/meta-callback-route.test.ts</files>
  <read_first>
    - tests/widget-config-route.test.ts
    - src/lib/crypto.ts
    - .planning/research/ARCHITECTURE.md (section 3: Meta OAuth Callback Flow)
    - .planning/research/PITFALLS.md (Meta OAuth + token storage warnings)
  </read_first>
  <action>
    Create `tests/meta-callback-route.test.ts` using `it.todo()` stubs only.

    Required describe blocks and behaviors:
    - `describe('META-01: successful callback')`
      - rejects unauthenticated requests and redirects to `/login`
      - rejects a CSRF state mismatch and redirects to `/integrations/meta?error=csrf`
      - exchanges the authorization code before any database write occurs
      - upserts one messenger row per page and one instagram row when `instagram_business_account` exists
    - `describe('META-02: token exchange chain')`
      - exchanges short-lived user token for long-lived user token
      - fetches page access tokens from `/me/accounts`
      - encrypts the page access token before writing `meta_channels`
      - never stores the short-lived or long-lived user token in the database
    - `describe('META-05: callback failure handling')`
      - redirects to `/integrations/meta?error=missing_code` when code is absent
      - redirects to `/integrations/meta?error=oauth_exchange` when Meta returns an error

    Keep the test file todo-only and aligned with the mocking style used in `tests/widget-config-route.test.ts`.
  </action>
  <acceptance_criteria>
    - `tests/meta-callback-route.test.ts` exists
    - file contains the three describe blocks named above
    - file contains at least 9 `it.todo(` stubs
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/meta-callback-route.test.ts --reporter=verbose</automated>
  </verify>
  <done>Critical token exchange and encryption behavior is contractually defined before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED jsdom stubs for the Meta settings UI</name>
  <files>tests/meta-settings.test.tsx</files>
  <read_first>
    - src/components/integrations/integrations-table.tsx
    - src/app/(dashboard)/widget/page.tsx
    - .planning/ROADMAP.md (Phase 10 success criteria)
    - .planning/REQUIREMENTS.md (META-03, META-05, META-06)
  </read_first>
  <action>
    Create `tests/meta-settings.test.tsx` using `it.todo()` stubs only and include `@vitest-environment jsdom`.

    Required describe blocks and behaviors:
    - `describe('META-03: connected channels list')`
      - shows connected page name and linked Instagram username
      - shows active status, last sync, and capability badges per channel
    - `describe('META-05: reconnect prompt')`
      - shows reconnect UI when `connection_error` contains Meta error 190
      - does not show the channel as healthy when `is_active` is false
    - `describe('META-04 and META-06: channel controls')`
      - renders a disconnect action per channel row
      - renders independent automation selectors for messenger and instagram
      - allows saving different automation ids for the two rows on the same page
    - `describe('META-01: connect entry point')`
      - renders a `Connect with Facebook` primary CTA when no channels exist

    Keep the file todo-only; no component implementation is needed in this plan.
  </action>
  <acceptance_criteria>
    - `tests/meta-settings.test.tsx` exists
    - file contains `@vitest-environment jsdom`
    - file contains all four describe blocks named above
    - file contains at least 8 `it.todo(` stubs
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/meta-settings.test.tsx --reporter=verbose</automated>
  </verify>
  <done>UI behavior for channel status, reconnect prompts, and automation binding is captured before buildout.</done>
</task>

</tasks>

<verification>
Todo-only Phase 10 RED tests run cleanly:
```bash
npx vitest run tests/meta-* --reporter=verbose
```
</verification>

<success_criteria>
- `tests/meta-oauth-actions.test.ts` exists with server action stubs
- `tests/meta-callback-route.test.ts` exists with callback route stubs
- `tests/meta-settings.test.tsx` exists with settings UI stubs
- No implementation code is created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/10-meta-oauth/10-01-SUMMARY.md`.
</output>
