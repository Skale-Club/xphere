---
phase: 09-reviews-widget
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/reviews-widget-route.test.ts
  - tests/reviews-widget.test.ts
  - tests/reviews-widget-asset.test.ts
autonomous: true
requirements:
  - GWDGT-02
  - GWDGT-04
  - GWDGT-05
  - GWDGT-06

must_haves:
  truths:
    - "Running npx vitest run tests/reviews-widget* exits 0 with all tests reported as todo"
    - "Public route coverage exists for token validation, CORS, stale-cache refusal, and response shape"
    - "Widget coverage exists for 4 layouts, Shadow DOM render, attribution, and silent failure behavior"
    - "Asset smoke test exists for public/reviews-widget.js after build:reviews-widget"
  artifacts:
    - path: "tests/reviews-widget-route.test.ts"
      provides: "RED stubs for GET /api/reviews/[token]"
      min_lines: 45
    - path: "tests/reviews-widget.test.ts"
      provides: "RED jsdom stubs for the reviews widget IIFE"
      min_lines: 70
    - path: "tests/reviews-widget-asset.test.ts"
      provides: "RED asset existence stub for public/reviews-widget.js"
      min_lines: 15
  key_links:
    - from: "tests/reviews-widget-route.test.ts"
      to: "src/app/api/reviews/[token]/route.ts"
      via: "future GET import"
      pattern: "GET /api/reviews/[token]"
    - from: "tests/reviews-widget.test.ts"
      to: "src/reviews-widget/index.ts"
      via: "future esbuild output evaluation"
      pattern: "reviews-widget.js"
---

<objective>
Create RED test stubs for the public reviews endpoint and the embeddable reviews widget before implementation begins.

Purpose: Lock the Phase 9 behavior contract before Wave 1 ships the public route and browser bundle.
Output: Three todo-only test files covering endpoint behavior, widget layouts, and built asset presence.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED stubs for the public reviews token route</name>
  <files>tests/reviews-widget-route.test.ts</files>
  <read_first>
    - tests/widget-config-route.test.ts
    - src/app/api/widget/[token]/config/route.ts
    - .planning/research/ARCHITECTURE.md (public reviews route section)
    - .planning/REQUIREMENTS.md (GWDGT-04, GWDGT-05, GWDGT-06)
  </read_first>
  <action>
    Create `tests/reviews-widget-route.test.ts` using `it.todo()` stubs only.

    Required describe blocks and behaviors:
    - `describe('GWDGT-04: GET /api/reviews/[token]')`
      - returns CORS headers for GET and OPTIONS requests
      - returns cached location + reviews payload for a valid `review_token`
      - orders reviews by `display_order` ascending
      - includes author name, author uri, author photo, relative date, maps links, and fetched_at in JSON
    - `describe('GWDGT-06: token invalid or data unavailable')`
      - returns non-200 for an invalid token
      - returns non-200 when no reviews exist for the location
      - returns non-200 when `fetched_at` is older than 30 days
    - `describe('GWDGT-05: Google attribution payload')`
      - includes place name and maps url needed for attribution footer/button
      - never calls Google Places API live from the route

    Keep the file aligned with the mock style used in `tests/widget-config-route.test.ts`.
  </action>
  <acceptance_criteria>
    - `tests/reviews-widget-route.test.ts` exists
    - file contains `describe('GWDGT-04: GET /api/reviews/[token]')`
    - file contains `describe('GWDGT-06: token invalid or data unavailable')`
    - file contains at least 8 `it.todo(` stubs
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/reviews-widget-route.test.ts --reporter=verbose</automated>
  </verify>
  <done>Route contract is captured as todo-only Vitest coverage with no implementation code created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED jsdom stubs for the reviews widget bundle</name>
  <files>tests/reviews-widget.test.ts</files>
  <read_first>
    - tests/widget.test.ts
    - src/widget/index.ts
    - .planning/REQUIREMENTS.md (GWDGT-02, GWDGT-05, GWDGT-06)
    - .planning/research/FEATURES.md (layouts, attribution, fallback expectations)
  </read_first>
  <action>
    Create `tests/reviews-widget.test.ts` using `it.todo()` stubs only and the same `@vitest-environment jsdom` pattern used by `tests/widget.test.ts`.

    Required describe blocks and behaviors:
    - `describe('GWDGT-02: layout rendering')`
      - renders carousel layout when `data-layout="carousel"`
      - renders grid layout when `data-layout="grid"`
      - renders list layout when `data-layout="list"`
      - renders compact layout when `data-layout="compact"`
      - falls back to list when layout is unknown
    - `describe('GWDGT-05: attribution and review card content')`
      - shows `Powered by Google` in every layout
      - shows author name adjacent to review text
      - shows initials fallback when `author_photo_url` is null
    - `describe('GWDGT-06: graceful failure')`
      - does not throw when `data-token` is missing
      - removes or hides host root when fetch returns non-200
      - does not throw when payload is empty or malformed
      - does not leak visible error text into the host page
    - `describe('GWDGT-04: public fetch behavior')`
      - calls `/api/reviews/{token}` relative to the script origin
      - uses Shadow DOM and does not write review markup into `document.body` directly

    Keep the test file todo-only; no real bundle evaluation is needed in this plan.
  </action>
  <acceptance_criteria>
    - `tests/reviews-widget.test.ts` exists
    - file contains `@vitest-environment jsdom`
    - file contains all four describe blocks named above
    - file contains at least 14 `it.todo(` stubs
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/reviews-widget.test.ts --reporter=verbose</automated>
  </verify>
  <done>Widget render and failure behavior are defined as RED contracts before the browser bundle exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED asset smoke stub for the built widget file</name>
  <files>tests/reviews-widget-asset.test.ts</files>
  <read_first>
    - tests/widget-asset.test.ts
    - package.json
  </read_first>
  <action>
    Create `tests/reviews-widget-asset.test.ts` using `it.todo()` stubs only.

    Required checks:
    - `public/reviews-widget.js` exists after `npm run build:reviews-widget`
    - built file is non-empty and larger than a stub
    - package.json contains `build:reviews-widget`
  </action>
  <acceptance_criteria>
    - `tests/reviews-widget-asset.test.ts` exists
    - file contains `describe('Reviews widget asset` or equivalent clear title
    - file contains at least 3 `it.todo(` stubs
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/reviews-widget-asset.test.ts --reporter=verbose</automated>
  </verify>
  <done>Asset-level expectations for the new browser bundle are captured.</done>
</task>

</tasks>

<verification>
Todo-only Phase 9 RED tests run cleanly:
```bash
npx vitest run tests/reviews-widget* --reporter=verbose
```
</verification>

<success_criteria>
- `tests/reviews-widget-route.test.ts` exists with public route stubs
- `tests/reviews-widget.test.ts` exists with layout and graceful-failure stubs
- `tests/reviews-widget-asset.test.ts` exists with build asset stubs
- No implementation code is created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/09-reviews-widget/09-01-SUMMARY.md`.
</output>
