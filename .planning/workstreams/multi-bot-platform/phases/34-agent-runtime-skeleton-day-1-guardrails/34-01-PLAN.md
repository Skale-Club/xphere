---
phase: 34-agent-runtime-skeleton-day-1-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - 34-RESEARCH.md
autonomous: true
requirements:
  - RUNTIME-01
must_haves:
  truths:
    - "The ai@^6 adoption decision is documented and locked — no 'undecided' state"
    - "package.json reflects the decision: ai package present (adopt) or absent (reject)"
    - "34-RESEARCH.md has an appended '## ai@^6 Spike Decision' section with adopt/reject + rationale"
  artifacts:
    - path: ".planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-RESEARCH.md"
      provides: "Spike decision record"
      contains: "## ai@^6 Spike Decision"
  key_links:
    - from: "spike"
      to: "run-agent.ts (Wave 3)"
      via: "adopt path uses generateText from 'ai'; reject path uses @anthropic-ai/sdk directly"
      pattern: "adopt|reject"
---

<objective>
Spike `ai@^6` (Vercel AI SDK v6) against the Phase 34 runtime call site to lock the framework decision before Wave 2/3 plans begin.

Purpose: Waves 2 and 3 must know whether to import `generateText` from `ai` or use `@anthropic-ai/sdk` directly. This decision cannot be deferred past Phase 34 (D-34-01). The spike is intentionally isolated in Wave 1 so it does not block the migration plan (34-02) from running in parallel.

Output: Documented adopt/reject decision appended to `34-RESEARCH.md`. If adopted, `ai` and `@ai-sdk/anthropic` remain in `package.json`. If rejected, they are uninstalled and `package.json` is restored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-CONTEXT.md
@.planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-RESEARCH.md

<interfaces>
<!-- Key interfaces the spike must validate against. From 34-RESEARCH.md and 34-CONTEXT.md. -->

D-34-01 spike success criterion:
- `generateText` / `streamText` integrates cleanly at the `runAgent` call site
- Drop-in means the spike touches ≤2 files and passes the same unit tests
- Adopt only if: `abortSignal` propagation works, `maxSteps` can serve as MAX_LLM_CALLS_PER_TURN cap, no type conflicts

Existing provider packages already installed:
- `@anthropic-ai/sdk ^0.82.0` — Anthropic native SDK (streaming API)
- `openai ^6.33.0` — used for OpenRouter (baseURL override)
- `ai` package is NOT currently in package.json

Phase 34 new env vars (for spike validation):
- `AGENT_RUNTIME_ENABLED` (default: 'true')
- `AGENT_TURN_TIMEOUT_MS` (default: '8000')
- `AGENT_MAX_LLM_CALLS_PER_TURN` (default: '6')

AgentRunResult shape (D-34-02 — must survive spike):
```ts
type AgentRunResult = {
  text: string
  usage: { tokensIn: number; tokensOut: number }
  invocationId: string
  traceId: string
  status: 'success' | 'error' | 'aborted' | 'denied' | 'skipped'
  errorDetail?: string
}
```

AbortController pattern (from 34-RESEARCH.md section 6):
```ts
const controller = new AbortController()
const timeoutId = setTimeout(() => controller.abort(), AGENT_TURN_TIMEOUT_MS)
// LLM call receives controller.signal
// finally: clearTimeout(timeoutId)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ai@^6 spike and lock adoption decision</name>
  <files>package.json, .planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-RESEARCH.md</files>

  <read_first>
    - C:/Users/Vanildo/Dev/operator/package.json (current deps — verify 'ai' is absent before install)
    - C:/Users/Vanildo/Dev/operator/.planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-RESEARCH.md (spike friction points listed in section 3)
    - C:/Users/Vanildo/Dev/operator/.planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-CONTEXT.md (D-34-01 success criterion, D-34-02 AgentRunResult shape)
    - C:/Users/Vanildo/Dev/operator/src/lib/chat/stream/anthropic.ts (existing LLM call pattern to replace)
  </read_first>

  <action>
Install and probe `ai@^6` + `@ai-sdk/anthropic`:

```
npm install ai@^6 @ai-sdk/anthropic
```

Then evaluate the following friction points (from 34-RESEARCH.md section 3):

**1. AbortController propagation**
Write a minimal in-memory test (no real API call needed — use a mock or check TypeScript types):
- Confirm `generateText` accepts `abortSignal: AbortSignal` as a top-level option
- Confirm `@ai-sdk/anthropic` provider forwards the signal to the underlying Anthropic stream

**2. maxSteps as MAX_LLM_CALLS_PER_TURN**
- Check if `generateText({ maxSteps: 6 })` prevents more than 6 LLM+tool round-trips
- This must be equivalent to the Phase 34 RUNTIME-05 cap

**3. Tool wrapping complexity**
- Count how many files must change to wrap `executeAction` as ai SDK tools
- The criterion is ≤2 files touched. If wrapping requires defining `execute()` functions across many action types, it exceeds the threshold

**4. OpenRouter compat**
- Check if `@ai-sdk/openai` with a custom `baseURL: 'https://openrouter.ai/api/v1'` works alongside the existing `openai ^6.33.0` package (no version conflict)

**5. usage shape**
- Confirm `generateText` returns `usage.promptTokens` and `usage.completionTokens` (maps to `tokens_in`/`tokens_out`)

**Adopt criterion (ALL must be true):**
- abortSignal propagates to underlying stream (checked via types or docs)
- maxSteps≤6 is a supported parameter
- Tool wrapping touches ≤2 files (types.ts + run-agent.ts only)
- No package version conflict with existing `openai ^6.33.0`
- usage shape maps cleanly to AgentRunResult

**Reject criterion (ANY of these):**
- abortSignal is not forwarded by `@ai-sdk/anthropic` to the underlying Anthropic stream
- Multi-provider dynamic selection (Anthropic key OR OpenRouter key per org) requires installing both `@ai-sdk/anthropic` + `@ai-sdk/openai` AND restructuring the provider-key resolution beyond 2 file changes
- Tool wrapping with `executeAction` requires touching more than run-agent.ts + types.ts
- TypeScript errors when building with both `ai` + `openai` packages

**If REJECT:** Run `npm uninstall ai @ai-sdk/anthropic` to restore package.json.

**Append decision to 34-RESEARCH.md** under a new section `## ai@^6 Spike Decision` with this exact structure:
```
## ai@^6 Spike Decision

**Date:** YYYY-MM-DD
**Result:** ADOPT | REJECT
**Rationale:** [2-4 sentences explaining the decisive friction point(s)]
**Impact on Wave 2/3 plans:**
- run-agent.ts LLM call: uses `generateText` from 'ai' | uses `@anthropic-ai/sdk` directly
- Provider selection: [how multi-provider routing is handled]
**Version pinned:** ai@X.Y.Z + @ai-sdk/anthropic@X.Y.Z | N/A (rejected)
```

Run `npm run build` after the decision to confirm no TypeScript errors are introduced.
  </action>

  <verify>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
    - 34-RESEARCH.md contains the exact heading `## ai@^6 Spike Decision`
    - 34-RESEARCH.md contains either `Result: ADOPT` or `Result: REJECT` (not both, not neither)
    - If ADOPT: package.json contains `"ai":` under dependencies
    - If REJECT: package.json does NOT contain `"ai":` under dependencies; `npm uninstall` was run
    - `npm run build` exits 0 (no TypeScript errors from package state)
  </acceptance_criteria>

  <done>
The ai@^6 adoption decision is irrevocably documented in 34-RESEARCH.md. Wave 2/3 plan executors can read the decision record to know which LLM call pattern to implement in run-agent.ts. Package.json reflects the decision (ai present or absent).
  </done>
</task>

</tasks>

<verification>
- grep "ai@\^6 Spike Decision" .planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-RESEARCH.md
- grep -E "Result: (ADOPT|REJECT)" .planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-RESEARCH.md
- npm run build (exits 0)
</verification>

<success_criteria>
- 34-RESEARCH.md has `## ai@^6 Spike Decision` appended with adopt/reject + rationale
- package.json is consistent with the decision
- Build passes
- Wave 2/3 executors can determine the LLM call pattern by reading the RESEARCH.md decision record alone
</success_criteria>

<output>
After completion, create `.planning/phases/34-agent-runtime-skeleton-day-1-guardrails/34-01-SUMMARY.md`
</output>
