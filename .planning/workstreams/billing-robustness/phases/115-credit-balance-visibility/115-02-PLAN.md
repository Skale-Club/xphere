---
phase: 115-credit-balance-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1226_copilot_credits_realtime.sql
  - src/types/database.ts
autonomous: true
requirements: [CRB-02]

must_haves:
  truths:
    - "The copilot_credit_balances table is included in the supabase_realtime publication after this migration is applied"
    - "Re-running the migration (or re-applying an equivalent ALTER PUBLICATION) does not error if the table is already in the publication"
  artifacts:
    - path: "supabase/migrations/1226_copilot_credits_realtime.sql"
      provides: "Idempotent ALTER PUBLICATION supabase_realtime ADD TABLE public.copilot_credit_balances"
  key_links:
    - from: "supabase/migrations/1226_copilot_credits_realtime.sql"
      to: "supabase_realtime publication"
      via: "ALTER PUBLICATION ... ADD TABLE"
      pattern: "ALTER PUBLICATION supabase_realtime ADD TABLE public\\.copilot_credit_balances"
---

<objective>
Enable Supabase Realtime on `copilot_credit_balances` so the `CreditsIndicator` component (built in Plan 03) can subscribe to `postgres_changes` UPDATE events and reflect balance changes without a page reload (CRB-02). This is the exact idempotent pattern already used twice in this codebase (migrations 024 and 1206) — no new mechanism, just the same `ALTER PUBLICATION` wrapped in a duplicate-safe `DO` block, applied to a new table.

Purpose: Without this migration, `.subscribe()` on the client silently receives no events — there is no error, no console warning, just a permanently stale indicator after a debit/top-up (RESEARCH.md Pitfall 4). This migration is what makes CRB-02 actually function; the component code in Plan 03 is inert without it.
Output: One new migration file applied to the remote DB via `npx supabase db push`, and `src/types/database.ts` confirmed unchanged (this migration adds no new columns/tables, only alters publication membership — regeneration is not required, but the task double-checks this explicitly since it's a stated project convention after every migration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/billing-robustness/phases/115-credit-balance-visibility/115-CONTEXT.md
@.planning/workstreams/billing-robustness/phases/115-credit-balance-visibility/115-RESEARCH.md
@supabase/migrations/024_chat_realtime_publication.sql
@supabase/migrations/1206_call_logs_realtime.sql
@supabase/migrations/1208_copilot_credits.sql
@supabase/migrations/1225_metering_reason.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and apply the Realtime publication migration</name>
  <files>supabase/migrations/1226_copilot_credits_realtime.sql</files>
  <read_first>
    - supabase/migrations/024_chat_realtime_publication.sql (exact idempotent `DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$` wrapper structure — copy this verbatim, only changing the table name and comment)
    - supabase/migrations/1206_call_logs_realtime.sql (second precedent confirming the same one-table variant of the pattern, most structurally similar to what this task needs)
    - supabase/migrations/1208_copilot_credits.sql (confirms `copilot_credit_balances` has `org_id uuid PRIMARY KEY` — a valid Realtime filter target, no schema change needed here)
    - supabase/migrations/1225_metering_reason.sql (confirms 1225 is the current highest migration number before this task; re-verify via `ls` in case a competing branch claimed 1226 first)
  </read_first>
  <action>
    First, re-verify the next free migration number (do not trust the planning-time number blindly, per RESEARCH.md Open Question 3):
    ```bash
    ls supabase/migrations | tail -5
    ```
    If `1226_*` already exists, use `1227` (or the next free number) instead and rename the file accordingly throughout this task.

    Create supabase/migrations/1226_copilot_credits_realtime.sql with exactly:
    ```sql
    -- Migration 1226: Enable Realtime for the Copilot credit balance indicator
    -- Adds copilot_credit_balances to the supabase_realtime publication so the
    -- TopBar CreditsIndicator can subscribe via postgres_changes and reflect
    -- balance changes (debits, top-ups, monthly resets) live, without a page
    -- reload (CRB-02). Idempotent: wraps the ALTER in a DO block that swallows
    -- duplicate_object errors, matching migrations 024 and 1206.

    DO $$
    BEGIN
      ALTER PUBLICATION supabase_realtime ADD TABLE public.copilot_credit_balances;
    EXCEPTION
      WHEN duplicate_object THEN NULL;
    END $$;
    ```

    Apply it:
    ```bash
    npx supabase db push
    ```

    IMPORTANT — if this command fails with a 403 / authentication error against project ref `mwklvkmggmsintqcqfvu` (a previously observed failure mode in this environment), STOP immediately. Do NOT attempt workarounds (manual SQL execution via a different client, service-role script, etc.). Report this as a blocker/checkpoint — the orchestrating session has a working alternate path (Supabase Management API) and will apply the migration itself. Do not mark this task done until the migration is confirmed applied by whichever path succeeds.

    After a successful push, this migration does not add or change any table/column, only publication membership — so `src/types/database.ts` requires NO regeneration. Confirm this by checking that no new columns exist to represent:
    ```bash
    grep -n "copilot_credit_balances" src/types/database.ts
    ```
    This should show the existing type entry unchanged (same fields as before this migration). If regeneration tooling is run out of habit and produces a diff, verify the diff is empty/whitespace-only before committing it; do not commit a database.ts change driven by an unrelated tooling version bump.
  </action>
  <verify>
    <automated>ls supabase/migrations/1226_copilot_credits_realtime.sql 2>/dev/null || ls supabase/migrations/1227_copilot_credits_realtime.sql 2>/dev/null</automated>
  </verify>
  <done>The migration file exists with the exact idempotent DO block shown above, `npx supabase db push` completed successfully against the remote project (or the orchestrator applied it via the Management API fallback), and `copilot_credit_balances` is confirmed present in the `supabase_realtime` publication (verifiable via `select * from pg_publication_tables where pubname = 'supabase_realtime' and tablename = 'copilot_credit_balances';` if DB access is available, or by the successful push output itself).</done>
</task>

</tasks>

<verification>
Confirm the migration file is committed and was successfully applied (either directly or via the orchestrator's Management API fallback). If DB access is available, run the `pg_publication_tables` query above and confirm one row is returned for `copilot_credit_balances`.
</verification>

<success_criteria>
- `supabase/migrations/1226_copilot_credits_realtime.sql` (or the next free sequential number if 1226 was claimed) exists with the idempotent ALTER PUBLICATION statement
- The migration has been applied to the remote database (directly, or flagged as a blocker for the orchestrator to apply via the Management API fallback)
- `src/types/database.ts` is unchanged (no new columns/tables from this migration)
</success_criteria>

<output>
After completion, create `.planning/phases/115-credit-balance-visibility/115-02-SUMMARY.md`
</output>
