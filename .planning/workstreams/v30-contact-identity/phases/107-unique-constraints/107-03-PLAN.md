---
phase: 107-unique-constraints
plan: 03
type: execute
wave: 3
depends_on: ["107-02"]
files_modified:
  - src/lib/whatsapp/process-message.ts
  - src/lib/evolution/process-event.ts
  - src/lib/telegram/process-update.ts
autonomous: true
requirements: [CID-07, CID-08]

must_haves:
  truths:
    - "WhatsApp Cloud contact-create path wraps INSERT in try/catch on SQLSTATE 23505 and falls back to findByPhone (D-03)"
    - "Evolution API contact-create path wraps INSERT in try/catch on SQLSTATE 23505 and falls back to findByPhone (D-03)"
    - "Telegram contact-create path wraps INSERT in try/catch on SQLSTATE 23505 and falls back to findByPhone (D-03)"
    - "All three handlers continue downstream work (conversation/message creation) with the recovered contact id"
    - "All three handlers do NOT update existing contact fields with webhook data (D-03)"
    - "All three handlers log `contact.unique_collision source=<channel>` line on 23505 collision"
    - "Existing webhook lookup queries use phone_e164 (normalized column), not raw phone"
    - "Webhook always returns HTTP 200 (CLAUDE.md webhook rule unchanged)"
    - "npm run build exits 0"
  artifacts:
    - path: "src/lib/whatsapp/process-message.ts"
      provides: "WhatsApp Cloud contact creation with unique-violation recovery"
      contains: "23505"
    - path: "src/lib/evolution/process-event.ts"
      provides: "Evolution API contact creation with unique-violation recovery"
      contains: "23505"
    - path: "src/lib/telegram/process-update.ts"
      provides: "Telegram contact creation with unique-violation recovery"
      contains: "23505"
  key_links:
    - from: "webhook insert error handler"
      to: "findByPhone helper from src/lib/contacts/server.ts"
      via: "fallback lookup on SQLSTATE 23505"
      pattern: "findByPhone"
    - from: "webhook contactId resolution"
      to: "downstream conversation/message create"
      via: "contactId now set from either created or recovered contact"
      pattern: "contactId"
---

<objective>
Add unique-violation recovery to the three webhook contact-creation sites identified in 107-RESEARCH.md (corrected from CONTEXT D-03a — the real sites are whatsapp/evolution/telegram, NOT Meta/Vapi/ManyChat which don't create contacts in this repo). Each handler catches SQLSTATE 23505 on the contact insert, looks up the winner via `findByPhone`, and continues downstream work attached to the recovered contact id. Lookups switched from raw `phone` to normalized `phone_e164` to match the partial index predicate.

Purpose: With the partial UNIQUE indexes live, blind webhook inserts will fail with 23505 on concurrent ingestion. This plan makes the three contact-creating handlers race-safe without expanding their feature set. Inbound webhooks still return HTTP 200 per CLAUDE.md.

Output: Three handler files updated. Build clean. Same logging shape as createContact (Pitfall 5 format).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v30-contact-identity/phases/107-unique-constraints/107-CONTEXT.md
@.planning/workstreams/v30-contact-identity/phases/107-unique-constraints/107-RESEARCH.md
@src/lib/contacts/server.ts
@src/lib/whatsapp/process-message.ts
@src/lib/evolution/process-event.ts
@src/lib/telegram/process-update.ts

<interfaces>
<!-- New helpers available from plan 02 -->

From src/lib/contacts/server.ts (after plan 02):
```ts
export async function findByPhone(
  supabase: SupabaseClient<Database>,
  orgId: string,
  phone: string | null | undefined,
): Promise<{ id: string } | null>

export async function findByEmail(
  supabase: SupabaseClient<Database>,
  orgId: string,
  email: string | null | undefined,
): Promise<{ id: string } | null>
```

PostgREST error code surface — verify with the project's pattern, `error.code === '23505'` is unique_violation.

CLAUDE.md webhook rule: ALL inbound webhooks return HTTP 200. Wrap recovery in try/catch; never throw out.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Harden WhatsApp Cloud contact-creation site</name>
  <files>src/lib/whatsapp/process-message.ts</files>
  <read_first>
    - src/lib/whatsapp/process-message.ts (entire file — find the contact insert around lines 68-80 per RESEARCH.md)
    - src/lib/contacts/server.ts (post-plan-02 — findByPhone helper available)
    - src/lib/contacts/zod-schemas.ts (normalisePhone signature)
    - 107-RESEARCH.md Pattern 3 (WhatsApp example shown verbatim)
  </read_first>
  <action>
    1. Add import at top of file:
       ```ts
       import { findByPhone } from '@/lib/contacts/server'
       import { normalisePhone } from '@/lib/contacts/zod-schemas'
       ```
       (Skip if either is already imported.)

    2. Locate the existing contact lookup-or-insert block (around lines 55-80, per RESEARCH.md). It currently does an `.eq('phone', fromPhone)` lookup followed by `.insert(...).select('id').single()` if not found.

    3. Replace it with the Pattern 3 shape:
       - Lookup: switch the existing `.eq('phone', fromPhone)` to `.eq('phone_e164', normalisePhone(fromPhone))` AND add `.neq('identity_status', 'archived_duplicate')`.
       - If lookup misses, attempt insert as before. On error:
         - If `error?.code === '23505'`, call `await findByPhone(supabase, orgId, fromPhone)` and use its result as `contactId`.
         - Log: `console.log(\`[whatsapp/process] contact.unique_collision source=whatsapp org_id=${orgId} contact_id=${winner.id} matched_via=phone\`)` when a winner is found.
         - If neither recovery yields a contact, `contactId` stays null — the downstream code path already handles null gracefully (verify by reading the file).
         - For non-23505 errors, preserve existing error handling (do not throw out — webhook returns 200).

    4. Do NOT modify any other logic in the file (message persistence, media, signals, etc.).

    5. Keep the existing `source: 'whatsapp'` value on the insert.
  </action>
  <acceptance_criteria>
    - `grep -q "findByPhone" src/lib/whatsapp/process-message.ts`
    - `grep -q "23505" src/lib/whatsapp/process-message.ts`
    - `grep -q "contact.unique_collision" src/lib/whatsapp/process-message.ts`
    - `grep -q "phone_e164" src/lib/whatsapp/process-message.ts` (lookup now on normalized column)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>WhatsApp contact-create site catches 23505, recovers via findByPhone, logs collision, build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Harden Evolution API contact-creation site</name>
  <files>src/lib/evolution/process-event.ts</files>
  <read_first>
    - src/lib/evolution/process-event.ts (find the contact insert around lines 217-241)
    - src/lib/whatsapp/process-message.ts (post-Task 1 — same pattern reference)
    - 107-RESEARCH.md Pattern 3 (Evolution uses identical shape)
  </read_first>
  <action>
    Apply the same shape as Task 1, with the Evolution-specific differences:
    - Source string: `'evolution'` (or whatever the current insert uses — preserve it).
    - Log prefix: `[evolution/process]` and `source=evolution` in the structured log line.
    - The lookup column was likely `.eq('phone', ...)`; switch to `.eq('phone_e164', normalisePhone(phone))` and add `.neq('identity_status', 'archived_duplicate')`.
    - Wrap the existing `.insert(...).select('id').single()` in a check for `error?.code === '23505'`; on hit call `findByPhone(supabase, orgId, phone)` and use the result as `contactId`.
    - Log `console.log(\`[evolution/process] contact.unique_collision source=evolution org_id=${orgId} contact_id=${winner.id} matched_via=phone\`)` when recovered.

    Preserve all other logic — message routing, group handling, ack work, etc.
  </action>
  <acceptance_criteria>
    - `grep -q "findByPhone" src/lib/evolution/process-event.ts`
    - `grep -q "23505" src/lib/evolution/process-event.ts`
    - `grep -q "contact.unique_collision" src/lib/evolution/process-event.ts`
    - `grep -q "phone_e164" src/lib/evolution/process-event.ts`
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Evolution contact-create site catches 23505, recovers, logs, build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Harden Telegram contact-creation site</name>
  <files>src/lib/telegram/process-update.ts</files>
  <read_first>
    - src/lib/telegram/process-update.ts (find the contact insert around lines 189-220)
    - 107-RESEARCH.md Pattern 3 (Telegram follows same shape)
  </read_first>
  <action>
    Same shape as Tasks 1 and 2:
    - Log prefix: `[telegram/process]` and `source=telegram`.
    - If Telegram identifies contacts by phone (preferred), use `findByPhone`. If by something else (Telegram numeric ID), then EITHER preserve existing lookup logic AND only add the 23505 catch (no fallback recovery is meaningful) OR if phone is available use `findByPhone` like the others.
    - Verify by reading the actual file — but per RESEARCH.md it does insert contacts with phone.
    - Lookup switches to `phone_e164` normalized column when phone is the dedup key, with `.neq('identity_status', 'archived_duplicate')`.
    - Wrap the insert call to catch 23505 → call `findByPhone(supabase, orgId, phone)` and set `contactId` from the winner.
    - Log collision in the same flat key=value shape.

    If the current handler creates contacts WITHOUT a phone (Telegram-only identity), document that in a code comment and leave the 23505 path as a defensive no-op fallback (catch but log warning that no recovery is possible — Phase 108 will add channel-identity-based recovery).
  </action>
  <acceptance_criteria>
    - `grep -q "23505" src/lib/telegram/process-update.ts`
    - `grep -q "contact.unique_collision" src/lib/telegram/process-update.ts`
    - `grep -q "findByPhone\\|phone_e164" src/lib/telegram/process-update.ts` (at least one — phone path)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Telegram contact-create site catches 23505, logs, recovers via findByPhone when phone present, build clean.</done>
</task>

</tasks>

<verification>
- All three handler files updated
- `grep -l "contact.unique_collision" src/lib/whatsapp/process-message.ts src/lib/evolution/process-event.ts src/lib/telegram/process-update.ts` returns all three files
- `npm run build` exits 0
- `npm run lint` exits 0
- Manual: webhook entry-point exports still return HTTP 200 envelope (no functions throw out)
</verification>

<success_criteria>
Three webhook contact-creation paths are race-safe with the new partial UNIQUE indexes. Lookups normalized. Collision logging consistent across handlers and with createContact (plan 02). Webhooks still return 200.
</success_criteria>

<output>
After completion, create `.planning/workstreams/v30-contact-identity/phases/107-unique-constraints/107-03-SUMMARY.md` documenting:
- Diff summary per file
- Confirmation that RESEARCH.md correction (whatsapp/evolution/telegram, not Meta/Vapi/ManyChat) was applied
- Build + lint output
</output>
