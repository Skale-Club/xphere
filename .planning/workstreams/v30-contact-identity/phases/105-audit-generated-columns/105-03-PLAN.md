---
phase: 105-audit-generated-columns
plan: 03
type: execute
wave: 3
depends_on: ["105-02"]
files_modified:
  - src/types/database.ts
autonomous: true
requirements: [CID-04]

must_haves:
  truths:
    - "Migration 1056 is applied to main DB (either via merge_branch or already via direct-apply in Plan 02)"
    - "src/types/database.ts contains ContactRow fields phone_e164, email_normalized, identity_status"
    - "src/types/database.ts contains contact_duplicate_audit table types"
    - "npm run build exits 0"
    - "npm run lint exits 0"
  artifacts:
    - path: "src/types/database.ts"
      provides: "Regenerated Supabase types including new columns and audit table"
      contains: "phone_e164"
      contains_2: "email_normalized"
      contains_3: "identity_status"
      contains_4: "contact_duplicate_audit"
  key_links:
    - from: "src/types/database.ts"
      to: "live Supabase schema"
      via: "mcp__supabase_mcp_xphere__generate_typescript_types"
      pattern: "phone_e164"
---

<objective>
Promote migration 1056 to main (if not already done in Plan 02 fallback), regenerate `src/types/database.ts` from live schema, and verify `npm run build` is green.

Purpose: Make the new columns and audit table visible to the TypeScript application layer. Satisfies ROADMAP success criterion #7 (`npm run build` exits 0; type regen handled).

Output: Updated `src/types/database.ts` committed; build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v30-contact-identity/phases/105-audit-generated-columns/105-02-BRANCH-VALIDATION.md
@CLAUDE.md
@src/types/database.ts
@supabase/migrations/1056_contact_identity_audit.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote migration to main (merge branch or confirm already-applied)</name>
  <files>(no repo file changes — MCP-only)</files>
  <read_first>
    - .planning/workstreams/v30-contact-identity/phases/105-audit-generated-columns/105-02-BRANCH-VALIDATION.md (path taken + branch ID)
  </read_first>
  <action>
1. Read `105-02-BRANCH-VALIDATION.md` to determine path taken.

2. **If "Path taken: direct-apply"** — migration is already on main. Verify by calling `mcp__supabase_mcp_xphere__list_migrations` and confirming `1056_contact_identity_audit` appears. Skip to Task 2.

3. **If "Path taken: branch"** — promote:
   - Read branch ID from validation report.
   - Confirm the validation Recommendation was `GO`. If `NO-GO`, STOP and surface to user — do not promote.
   - Call `mcp__supabase_mcp_xphere__merge_branch` with the branch ID.
   - Call `mcp__supabase_mcp_xphere__list_migrations` to confirm `1056_contact_identity_audit` now appears on main.
   - Optionally delete the branch via `mcp__supabase_mcp_xphere__delete_branch` to free resources.

4. Run a sanity probe on main:
```sql
SELECT 1 FROM information_schema.columns
WHERE table_schema='public' AND table_name='contacts'
  AND column_name IN ('phone_e164','email_normalized','identity_status');
```
Expected: 3 rows.

```sql
SELECT 1 FROM information_schema.tables
WHERE table_schema='public' AND table_name='contact_duplicate_audit';
```
Expected: 1 row.
  </action>
  <verify>
    <automated>MISSING — MCP-only; verified by Task 2 type regen requiring live schema</automated>
  </verify>
  <acceptance_criteria>
    - `list_migrations` output includes `1056_contact_identity_audit`
    - Sanity probe returns 3 rows for the new contacts columns
    - Sanity probe returns 1 row for contact_duplicate_audit table
    - If branch path: merge_branch succeeded without error
    - If NO-GO from Plan 02: this task halted before merge
  </acceptance_criteria>
  <done>
    Migration 1056 is live on main Supabase DB, confirmed via information_schema probes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate src/types/database.ts and verify build</name>
  <files>src/types/database.ts</files>
  <read_first>
    - src/types/database.ts (current ContactRow shape — note that we are REPLACING this file)
    - CLAUDE.md (`npm run build` mandatory after changes)
  </read_first>
  <action>
1. Call `mcp__supabase_mcp_xphere__generate_typescript_types`. Capture the full output.

2. Write the output to `src/types/database.ts`, fully replacing the file.

3. Verify the new file contains the expected new schema elements (these came from migration 1056):
   - Field `phone_e164` on contacts Row
   - Field `email_normalized` on contacts Row
   - Field `identity_status` on contacts Row
   - Table object `contact_duplicate_audit` with Row, Insert, Update types
   - Database['public']['Functions']['normalize_phone'] and Database['public']['Functions']['refresh_contact_duplicate_audit'] entries

4. Run `npm run lint`. Must exit 0. If it fails purely on the generated file's formatting, accept it (generated file) and run `npm run lint -- --fix` only if it stays green for hand-written code. Do NOT hand-edit the generated types.

5. Run `npm run build`. Must exit 0. This is the ROADMAP success criterion #7 gate. If errors appear in app code that consumes ContactRow, surface them — those are real type drift issues we want to catch now.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/types/database.ts` was overwritten with MCP output (not hand-edited)
    - File contains literal string `phone_e164` (grep)
    - File contains literal string `email_normalized` (grep)
    - File contains literal string `identity_status` (grep)
    - File contains literal string `contact_duplicate_audit` (grep)
    - File contains literal string `normalize_phone` (grep — function type)
    - File contains literal string `refresh_contact_duplicate_audit` (grep — function type)
    - `npm run lint` exits with code 0
    - `npm run build` exits with code 0
    - No hand-edits to the generated file (compare structure to existing pattern — purely generated)
  </acceptance_criteria>
  <done>
    Types regenerated from live schema; build green. App is type-aware of new columns.
  </done>
</task>

</tasks>

<verification>
- `mcp__supabase_mcp_xphere__list_migrations` shows 1056 on main
- src/types/database.ts contains all 5 expected literals
- `npm run build` exits 0 (ROADMAP success #7)
- `npm run lint` exits 0
</verification>

<success_criteria>
- Migration live on main
- Types regenerated, build green
- Ready for Plan 04 to snapshot the audit baseline
</success_criteria>

<output>
After completion, create `.planning/workstreams/v30-contact-identity/phases/105-audit-generated-columns/105-03-SUMMARY.md` recording:
- Promotion path (merge_branch vs already-applied)
- `list_migrations` output excerpt
- `npm run build` exit code (must be 0)
- `npm run lint` exit code (must be 0)
- Note that Plan 04 can now run audit baseline on production data
</output>
