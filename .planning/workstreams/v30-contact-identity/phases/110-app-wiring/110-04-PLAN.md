---
phase: 110-app-wiring
plan: 04
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - src/components/contacts/identity-status-badge.tsx
  - src/components/chat/contact-info-panel.tsx
  - src/app/(dashboard)/contacts/actions.ts
  - src/lib/contacts/server.ts
autonomous: true
requirements: [CID-14, CID-15]
must_haves:
  truths:
    - "IdentityStatusBadge renders 5 distinct variants per identity_status value"
    - "Badge with status='identified' and isVerified=true renders as 'Verified' (effective state derivation)"
    - "merge_conflict badge wraps Link to /admin/contacts/conflicts"
    - "archived_duplicate badge hidden unless showAdminStates=true (defaults false)"
    - "Tooltip text appears on hover for all variants"
    - "Badge renders in contact-info-panel header next to existing tags row"
    - "getContact returns is_verified boolean derived from contact_verifications EXISTS"
  artifacts:
    - path: "src/components/contacts/identity-status-badge.tsx"
      provides: "Standalone 5-state IdentityStatusBadge component with tooltip + optional link"
      min_lines: 60
      contains: "export function IdentityStatusBadge"
    - path: "src/components/chat/contact-info-panel.tsx"
      provides: "Badge rendered next to existing badges"
      contains: "IdentityStatusBadge"
  key_links:
    - from: "src/components/chat/contact-info-panel.tsx"
      to: "IdentityStatusBadge"
      via: "import + render with status + isVerified props"
      pattern: "<IdentityStatusBadge"
    - from: "src/app/(dashboard)/contacts/actions.ts"
      to: "contact_verifications"
      via: "getContact EXISTS subquery for is_verified"
      pattern: "is_verified"
---

<objective>
Ship the IdentityStatusBadge component and render it in contact-info-panel.tsx. Add is_verified derivation to the getContact query so the badge can show 'Verified' sub-state when status='identified' + at least one verification row exists. Implements D-07, D-07a (badge surface for CID-14 + CID-15).

Purpose: Visual identity surface. After Phase 110, every contact panel surfaces its identity state at a glance — channel_only / identified / verified / merge_conflict / archived_duplicate.
Output: A reusable component (future detail page reuses it unchanged) + is_verified data plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v30-contact-identity/phases/110-app-wiring/110-CONTEXT.md
@.planning/workstreams/v30-contact-identity/phases/110-app-wiring/110-RESEARCH.md
@src/components/chat/contact-info-panel.tsx
@src/components/ui/badge.tsx
@src/components/ui/tooltip.tsx
@src/app/(dashboard)/contacts/actions.ts

<interfaces>
<!-- Badge variants available: default/primary/success/warning/danger/info/outline -->
<!-- Tooltip pattern already in use at contact-info-panel.tsx:518-535 -->

ContactIdentityStatus type already exported from `@/types/database` (Phase 105):
```ts
type ContactIdentityStatus = 'channel_only' | 'identified' | 'verified' | 'merge_conflict' | 'archived_duplicate'
```

CONFIG dict per D-07:
- channel_only → variant:info, Link2 icon, tooltip "Reachable on a messaging channel but missing phone and email."
- identified → variant:success, no icon, tooltip "Has a phone or email but no manual verification yet."
- verified → variant:success, CheckCircle2 icon, tooltip "Phone or email verified by an admin or response."
- merge_conflict → variant:warning, AlertTriangle icon, tooltip "Phone matched one contact, email matched another. Resolve in /admin/contacts/conflicts.", href: '/admin/contacts/conflicts'
- archived_duplicate → variant:default, Archive icon, tooltip "Merged into another contact." (admin-only render)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IdentityStatusBadge component + add is_verified to getContact</name>
  <files>src/components/contacts/identity-status-badge.tsx, src/app/(dashboard)/contacts/actions.ts, src/lib/contacts/server.ts</files>
  <action>
**Step A — Create the component** at `src/components/contacts/identity-status-badge.tsx` using the VERBATIM implementation from RESEARCH.md §"IdentityStatusBadge Component":
- `'use client'` directive
- imports: React, next/link, lucide-react (CheckCircle2, AlertTriangle, Link2, Archive), Badge, Tooltip primitives, ContactIdentityStatus type
- CONFIG dict with all 5 variants per D-07
- `IdentityStatusBadgeProps`: `{ status: ContactIdentityStatus | null; showAdminStates?: boolean; isVerified?: boolean }`
- Defensive early return: null status → render null (Pitfall 1)
- archived_duplicate without showAdminStates → render null
- Effective state derivation: `status === 'identified' && isVerified ? 'verified' : status`
- Wrap with Link if cfg.href present (merge_conflict only)
- Wrap with TooltipProvider/Tooltip/TooltipTrigger/TooltipContent

Verify the project's `Badge` component supports the variants we use (`info`, `success`, `warning`, `default`). Check `src/components/ui/badge.tsx`. If `info`/`warning` are missing, use the closest existing variant (e.g., `primary` for info, `outline` for warning) and document the mapping in a comment.

**Step B — Add is_verified to getContact** in `src/app/(dashboard)/contacts/actions.ts`. Locate the existing `getContact(id)` function. Add `is_verified` derivation per Pitfall 7 (single-contact only — DO NOT add to list queries):

Option A (recommended): Run a second small query in `getContact` after the main contact fetch:
```ts
const { count: verCount } = await supabase
  .from('contact_verifications')
  .select('id', { count: 'exact', head: true })
  .eq('contact_id', contact.id)
  .limit(1)
const is_verified = (verCount ?? 0) > 0
return { ...contact, is_verified }
```

Update the `ContactDetail` (or equivalent) return type to include `is_verified: boolean`.

**Step C — Sanity-check the helper inventory** in `src/lib/contacts/server.ts` — if there's an obvious place for a `hasVerifications(contactId)` helper, add it there and import into getContact. Otherwise inline is fine. Do NOT refactor unrelated code.

Run `npm run build` — must exit 0.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
- src/components/contacts/identity-status-badge.tsx exists, exports IdentityStatusBadge
- 5 CONFIG entries map identity_status → variant/icon/tooltip
- Badge variants used exist in project Badge component (verified or mapped with comment)
- getContact returns is_verified boolean
- npm run build exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Render IdentityStatusBadge in contact-info-panel header</name>
  <files>src/components/chat/contact-info-panel.tsx</files>
  <action>
Insert the badge in `contact-info-panel.tsx` at line ~488 (immediately after the tags row, inside the header `<div className="min-w-0 flex-1">`). Per RESEARCH.md §"IdentityStatusBadge Insertion Point":

```tsx
{contact.tags.length > 0 && (
  <div className="mt-2 flex flex-wrap gap-1">
    {/* existing tags rendering — unchanged */}
  </div>
)}
{/* D-07a: identity status badge (Phase 110) */}
<div className="mt-2 flex items-center gap-2">
  <IdentityStatusBadge
    status={contact.identity_status}
    isVerified={contact.is_verified ?? false}
  />
  {/* Plan 03's MarkVerifiedButton (if Plan 03 landed first) renders next to badge here */}
</div>
```

If Plan 03's MarkVerifiedButton conditional was added earlier at this same location, MERGE the two flex containers into one — single `<div className="mt-2 flex items-center gap-2">` containing badge + (conditionally) button.

Imports to add:
- `import { IdentityStatusBadge } from '@/components/contacts/identity-status-badge'`

The `contact.identity_status` field already exists on ContactDetail (Phase 105 + 106). `contact.is_verified` was added in Task 1.

Pitfall 1 reminder: badge component handles null status defensively — no extra guard needed.

Run `npm run build` — must exit 0.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
- contact-info-panel.tsx imports IdentityStatusBadge
- Badge renders next to tags row in header
- If Plan 03's button already in place, layouts merged into single flex container
- npm run build exits 0
- Manual smoke deferred to Plan 07 (load contacts of varying status, verify badge variants render)
  </done>
</task>

</tasks>

<verification>
- Build green: `npm run build`
- Grep: `Grep "IdentityStatusBadge" src/` returns ≥2 hits (component file + panel usage)
- Grep: `Grep "is_verified" src/` returns ≥2 hits (badge prop + getContact return)
</verification>

<success_criteria>
- CID-14 badge layer complete
- CID-15 merge_conflict variant links to admin conflict page
- D-07 + D-07a implemented
- Component standalone — reusable for future /contacts/[id] page (NOT built in Phase 110 per D-03)
- is_verified plumbing limited to single-contact getContact (Pitfall 7 — no list-page fanout)
</success_criteria>

<output>
Create `.planning/workstreams/v30-contact-identity/phases/110-app-wiring/110-04-SUMMARY.md` with:
- Component file path + exported props shape
- Badge variant mapping (if any project-specific mapping vs RESEARCH spec)
- Insertion point line number in contact-info-panel.tsx
- is_verified derivation strategy used
- Decisions implemented: D-07, D-07a
</output>
