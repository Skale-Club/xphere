---
phase: 109-identity-trigger
plan: 03
type: execute
wave: 3
depends_on: [109-01, 109-02]
files_modified:
  - tests/contact-identity-trigger.test.ts
  - .planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md
autonomous: true
requirements: [CID-12, CID-13]

must_haves:
  truths:
    - "tests/contact-identity-trigger.test.ts exists with 6 vitest tests using raw pg.Client (BEGIN/COMMIT for deferrable tests 1 & 2)"
    - "All 6 tests pass when DATABASE_URL is set; suite soft-skips when unset (Phase 107/108 precedent)"
    - "Cleanup in afterAll deletes from contact_channel_identities BEFORE contacts to avoid firing the orphan trigger"
    - "All trigger semantics covered: deferrable-pass with channel_only-skip (test 1), deferrable-fail (test 2), orphan-block (test 3), orphan-allow (test 4), promote channel_only→identified (test 5), archived_duplicate exempt (test 6)"
    - "Phase validation report (109-VALIDATION-REPORT.md) exists covering all 7 ROADMAP criteria with explicit Option A resolution + GO recommendation"
  artifacts:
    - path: "tests/contact-identity-trigger.test.ts"
      provides: "Integration tests for triggers 1/2/3 via raw pg client"
      contains: "describeIfDb"
      min_lines: 100
    - path: ".planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md"
      provides: "Phase 109 GO/NO-GO recommendation with all 7 ROADMAP criteria mapped"
      contains: "GO"
  key_links:
    - from: "tests/contact-identity-trigger.test.ts"
      to: "pg.Client + DATABASE_URL"
      via: "new Client({ connectionString: DATABASE_URL })"
      pattern: "new Client\\(\\{ connectionString"
    - from: "afterAll cleanup"
      to: "contact_channel_identities DELETE before contacts DELETE"
      via: "explicit ordering"
      pattern: "DELETE FROM public.contact_channel_identities[\\s\\S]*DELETE FROM public.contacts"
---

<objective>
Land vitest race/invariant test file covering all 3 triggers + author the Phase 109 validation report mapping all 7 ROADMAP success criteria with explicit Option A resolution and GO recommendation.

Purpose: Closes Phase 109 with automated regression coverage (CID-12 + CID-13) and a human-readable validation document for the phase gate.

Output: One test file + one validation report. Tests green; report marks Phase 109 GO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-CONTEXT.md
@.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-RESEARCH.md
@.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION.md
@.planning/workstreams/v30-contact-identity/ROADMAP.md
@tests/contacts-unique-constraint.test.ts
@tests/contact-channel-identity.test.ts

<interfaces>
From 109-RESEARCH.md "Vitest Pattern for Deferrable Transaction Tests" — the full test file body is ready to paste verbatim with minor adjustments. Key conventions:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import pg from 'pg'
const { Client } = pg
const DATABASE_URL = process.env.DATABASE_URL
const describeIfDb = DATABASE_URL ? describe : describe.skip
```

Existing precedents in repo:
- `tests/contacts-unique-constraint.test.ts` — pg.Client soft-skip pattern
- `tests/contact-channel-identity.test.ts` — Phase 108 race test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/contact-identity-trigger.test.ts (6 tests, raw pg client)</name>
  <files>tests/contact-identity-trigger.test.ts</files>

  <read_first>
    - `109-RESEARCH.md` section "Vitest Pattern for Deferrable Transaction Tests (D-07 test 1)" — full test body to copy.
    - `tests/contacts-unique-constraint.test.ts` and `tests/contact-channel-identity.test.ts` — confirm import paths, pg helper usage, and DATABASE_URL soft-skip idiom.
  </read_first>

  <action>
    Create `tests/contact-identity-trigger.test.ts` by copying the test body verbatim from RESEARCH.md "Vitest Pattern for Deferrable Transaction Tests". The file MUST contain:

    1. **Imports:** `describe, it, expect, beforeAll, afterAll` from `vitest`; `pg` (CommonJS default) for `Client`.
    2. **Soft-skip:** `const describeIfDb = DATABASE_URL ? describe : describe.skip` per D-07a.
    3. **beforeAll:** connect pg.Client, pin an `orgId` from `SELECT id FROM public.organizations LIMIT 1`.
    4. **afterAll cleanup (CRITICAL):** delete from `public.contact_channel_identities WHERE contact_id = ANY($1)` FIRST, then delete from `public.contacts WHERE id = ANY($1)`. This ordering prevents the orphan trigger from firing during cleanup.
    5. **Six tests:**
       - **Test 1 (deferrable pass + channel_only skip):** `BEGIN; INSERT contact (identity_status='channel_only'); INSERT cci; COMMIT;` → no throw.
       - **Test 2 (strict invariant raises):** `BEGIN; INSERT contact (identity_status='identified', no phone/email/identity); COMMIT;` → throws `/identity invariant/i`.
       - **Test 3 (orphan block):** insert contact (channel_only) + 1 cci, `DELETE` cci → throws `/last channel identity/i`.
       - **Test 4 (orphan allow):** insert contact (identified, phone='+15551234567') + 1 cci, `DELETE` cci → no throw.
       - **Test 5 (promote):** insert contact (channel_only) + 1 cci, `UPDATE phone='+15559876543'`, read back → `identity_status='identified'`.
       - **Test 6 (archived exempt):** insert contact (identified, phone). `UPDATE identity_status='archived_duplicate'`. `UPDATE phone=NULL` → no throw.

    Use unique external_ids per test (`Date.now()` suffix) to avoid UNIQUE violations on cci. Push all created contact ids onto `cleanupContactIds: string[]` for afterAll.

    Do NOT add a vitest config change. The existing `vitest.config.ts` picks up `tests/*.test.ts` automatically.
  </action>

  <acceptance_criteria>
    - `tests/contact-identity-trigger.test.ts` exists.
    - `grep -c "describeIfDb" tests/contact-identity-trigger.test.ts` returns at least `1`.
    - `grep -c "test [123456]" tests/contact-identity-trigger.test.ts` returns `6`.
    - `grep -c "BEGIN" tests/contact-identity-trigger.test.ts` returns at least `2` (tests 1 & 2).
    - File contains the cleanup ordering: DELETE FROM contact_channel_identities precedes DELETE FROM contacts in afterAll (verify via multiline grep or by reading the afterAll block).
    - `npx vitest run tests/contact-identity-trigger.test.ts` exits 0 when DATABASE_URL is set (6 tests pass); when DATABASE_URL unset, suite is skipped (exit 0, no failures).
    - `npm run build` still exits 0 (test file syntactically valid).
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run tests/contact-identity-trigger.test.ts</automated>
  </verify>

  <done>
    Test file authored, all 6 tests pass (or soft-skip cleanly), cleanup ordering correct, build green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 109-VALIDATION-REPORT.md mapping all 7 ROADMAP criteria + GO recommendation</name>
  <files>.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md</files>

  <read_first>
    - `.planning/workstreams/v30-contact-identity/ROADMAP.md` (Phase 109 section — all 7 success criteria).
    - `.planning/workstreams/v30-contact-identity/phases/108-channel-identities/108-VALIDATION-REPORT.md` (use as structural template — same shape, same tone).
    - `109-RESEARCH.md` section "Mitigation Decision: Option A" — explicit resolution narrative.
    - `109-01-SUMMARY.md` and `109-02-SUMMARY.md` (Plans 01/02 outputs) for cross-references.
  </read_first>

  <action>
    Author `.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md` mirroring the structure of 108-VALIDATION-REPORT.md.

    **Required sections:**

    1. **Header** with phase id, date, recommendation = `GO`.

    2. **Option A Resolution** — explicit subsection explaining:
       - Why the original DEFERRABLE-only design was insufficient (Phase 108 webhooks use 2 separate HTTP transactions through Supabase JS; DEFERRABLE only helps within a single explicit transaction).
       - The Option A status-skip predicate (`identity_status <> 'channel_only'` in pre-flight + `IF NEW.identity_status = 'channel_only' THEN RETURN NULL;` in enforce function).
       - Why this preserves the invariant in practice (promote trigger re-engages strict check on transition to `identified`; orphan trigger covers delete path).
       - Forward compatibility note: today's webhooks always set `phone` so they'd pass the strict check anyway; the skip exists for future phone-less webhooks (e.g., Instagram DM).

    3. **ROADMAP Success Criteria — 7-row table** mapping each criterion to evidence:

       | # | Criterion | Status | Evidence |
       |---|-----------|--------|----------|
       | 1 | Trigger `enforce_contact_identity()` fires on INSERT and UPDATE of `contacts` | ✓ | Migration 1061 §3 `CREATE CONSTRAINT TRIGGER ... AFTER INSERT OR UPDATE ON public.contacts`; verified via 109-01 SQL probe `SELECT FROM pg_trigger` |
       | 2 | Trigger raises exception if no phone, email, or channel identity | ✓ | `enforce_contact_identity_at_commit_fn` body §2; test 2 in `tests/contact-identity-trigger.test.ts` |
       | 3 | Trigger runs on DELETE of `contact_channel_identities` to prevent orphan | ✓ | Trigger 2 `prevent_channel_identity_orphan` (BEFORE DELETE); test 3 |
       | 4 | Contacts from channel webhooks get `identity_status='channel_only'` until phone/email added | ⚠ partial | DB supports it (CHECK in 1056 allows the value, skip predicate honors it). **Webhook payloads still write the default `'identified'` because they always set phone (verified RESEARCH.md). No regression today; explicit `channel_only` write deferred to first phone-less webhook path (likely Phase 110 Instagram).** |
       | 5 | Promotion: `phone`/`email` populated on channel_only → status auto-bumps to `'identified'` | ✓ | Trigger 3 `promote_channel_only_on_identity` (BEFORE UPDATE WHEN OLD='channel_only'); test 5 |
       | 6 | Zod `contactSchema.refine` updated to allow phone/email-less when channel context provided | ✓ via comment | Per D-04: refine intentionally KEPT strict (forms always supply name). Comment added in 109-02 documents divergence and points to DB invariant as the looser universal enforcer. **No semantic change required to satisfy CID-12.** |
       | 7 | Existing tests pass; new tests cover invariant edge cases | ✓ | `npx vitest run` green; new `tests/contact-identity-trigger.test.ts` covers all 6 D-07 cases |

    4. **Test Coverage Matrix** — list the 6 tests, the requirement they hit (CID-12 / CID-13), and the corresponding SQL probe from 109-01.

    5. **Artifacts Manifest:**
       - `supabase/migrations/1061_contact_identity_trigger.sql` (3 functions, 3 triggers, pre-flight)
       - `.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/apply-1061.mjs`
       - `src/lib/contacts/zod-schemas.ts` (comment-only change)
       - `tests/contact-identity-trigger.test.ts`

    6. **Open Items Deferred to Phase 110:**
       - Webhook code change to set `identity_status='channel_only'` on new-insert (RESEARCH.md Open Question §1) — safe to defer because all 3 webhooks set phone today.
       - Auto-downgrade `identified → channel_only` when phone/email cleared (CONTEXT deferred).
       - Verified state, conflict UI, badges.
       - `contacts.source` column drop.

    7. **Recommendation:** `**GO** — Phase 109 deliverables complete. CID-12 + CID-13 enforced at DB layer. Option A resolution preserves Phase 108 webhook flow with zero app-code regression. Proceed to Phase 110.`
  </action>

  <acceptance_criteria>
    - File `.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md` exists.
    - `grep -c "GO" .planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md` returns at least `2` (recommendation line + section header or table).
    - `grep -c "Option A" .planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md` returns at least `1`.
    - `grep -c "CID-12" .planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md` returns at least `1`.
    - `grep -c "CID-13" .planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md` returns at least `1`.
    - 7-row criteria table is present (count rows starting with `| 1 |` through `| 7 |` — exactly 7).
    - Artifacts manifest mentions all 4 files (migration, apply script, zod-schemas, test file).
  </acceptance_criteria>

  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-VALIDATION-REPORT.md','utf8'); const rows=(t.match(/^\\| [1-7] \\|/gm)||[]).length; const need=['Option A','CID-12','CID-13','GO','1061_contact_identity_trigger.sql','apply-1061.mjs','zod-schemas.ts','contact-identity-trigger.test.ts']; for(const s of need){if(!t.includes(s)){console.error('MISSING',s);process.exit(1)}} if(rows!==7){console.error('rows',rows);process.exit(1)} console.log('OK')"</automated>
  </verify>

  <done>
    Validation report exists with all 7 ROADMAP criteria mapped, Option A resolution narrative explicit, GO recommendation, artifacts manifest complete, deferred items listed.
  </done>
</task>

</tasks>

<verification>
- Test suite passes (or soft-skips on missing DATABASE_URL).
- Validation report passes all content checks.
- Phase 109 ready for `/gsd:verify-work` and ROADMAP marking.
</verification>

<success_criteria>
- `tests/contact-identity-trigger.test.ts` exists with 6 tests, all green when DATABASE_URL set.
- `109-VALIDATION-REPORT.md` exists with explicit Option A resolution, all 7 ROADMAP criteria mapped, GO recommendation, deferred items listed.
- `npm run build` exits 0; `npx vitest run` full suite green.
</success_criteria>

<output>
After completion, create `.planning/workstreams/v30-contact-identity/phases/109-identity-trigger/109-03-SUMMARY.md` listing the test file, validation report, and the GO recommendation status.
</output>
