# Plan 112-01: Durable Lead Receipts

1. Add the RLS-protected receipt table and indexes.
2. Build contact matching and non-destructive upsert behavior.
3. Persist idempotent receipts and reject payload conflicts.
4. Expose accepted, duplicate, and conflict responses from `/api/v1/leads`.
