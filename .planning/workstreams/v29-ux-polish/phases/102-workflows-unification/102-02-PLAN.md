---
phase: 102-workflows-unification
plan: "02"
type: execute
wave: 2
depends_on:
  - "102-01"
files_modified:
  - src/components/layout/sidebar.tsx
  - src/components/flows/flow-canvas.tsx
  - src/components/flows/new-flow-form.tsx
  - src/components/command-palette.tsx
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "Sidebar has a single Workflows entry whose href is /workflows"
    - "Clicking the Workflows nav item highlights on all /workflows/** sub-routes"
    - "FlowCanvas autosave calls the correct saveWorkflowDefinition from the new _actions path"
    - "NewFlowForm router.push sends user to /workflows/flows/[id] after creation"
    - "Command palette Workflows entry navigates to /workflows"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Updated nav href"
      contains: "href: '/workflows'"
    - path: "src/components/flows/flow-canvas.tsx"
      provides: "Updated server action import"
      contains: "@/app/(dashboard)/workflows/flows/_actions/workflows"
    - path: "src/components/flows/new-flow-form.tsx"
      provides: "Updated router.push target"
      contains: "/workflows/flows/"
    - path: "src/components/command-palette.tsx"
      provides: "Updated navigation item"
      contains: "href: '/workflows'"
  key_links:
    - from: "src/components/flows/flow-canvas.tsx"
      to: "src/app/(dashboard)/workflows/flows/_actions/workflows.ts"
      via: "import { saveWorkflowDefinition } from '@/app/(dashboard)/workflows/flows/_actions/workflows'"
      pattern: "saveWorkflowDefinition.*workflows/flows/_actions/workflows"
    - from: "src/components/flows/new-flow-form.tsx"
      to: "src/app/(dashboard)/workflows/flows/_actions/workflows.ts"
      via: "import { createWorkflow } from '@/app/(dashboard)/workflows/flows/_actions/workflows'"
      pattern: "createWorkflow.*workflows/flows/_actions/workflows"
---

<objective>
Update the four files that reference /automations URLs outside the app/(dashboard)/automations/ directory: the sidebar nav item href, the FlowCanvas server action import path, the NewFlowForm router.push destination, and the command palette nav item.

Purpose: These are the cross-cutting references that would leave stale /automations links in the UI even after the new /workflows/** route tree exists. Plan 01 creates the destination; this plan makes everything point at it.
Output: 4 edited files. No new files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v29-ux-polish/phases/102-workflows-unification/102-RESEARCH.md
@.planning/workstreams/v29-ux-polish/phases/102-workflows-unification/102-01-SUMMARY.md

<interfaces>
<!-- Exact code locations for each change -->

sidebar.tsx — line 59, nav array:
```ts
// BEFORE
{ icon: Zap, label: 'Workflows', href: '/automations', group: 'build' },
// AFTER
{ icon: Zap, label: 'Workflows', href: '/workflows', group: 'build' },
```

flow-canvas.tsx — line 23:
```ts
// BEFORE
import { saveWorkflowDefinition } from '@/app/(dashboard)/automations/flows/_actions/workflows'
// AFTER
import { saveWorkflowDefinition } from '@/app/(dashboard)/workflows/flows/_actions/workflows'
```

new-flow-form.tsx — two changes:
```ts
// Import on line 11 — BEFORE
import { createWorkflow } from '@/app/(dashboard)/automations/flows/_actions/workflows'
// AFTER
import { createWorkflow } from '@/app/(dashboard)/workflows/flows/_actions/workflows'

// router.push on line 51 — BEFORE
router.push(`/automations/flows/${result.data.id}`)
// AFTER
router.push(`/workflows/flows/${result.data.id}`)
```

command-palette.tsx — line 83, items array:
```ts
// BEFORE
{ id: 'nav-tools', label: 'Automations', icon: Zap, href: '/automations', group: 'Navigation', keywords: ['actions', 'tools'] },
// AFTER
{ id: 'nav-tools', label: 'Workflows', icon: Zap, href: '/workflows', group: 'Navigation', keywords: ['automations', 'actions', 'tools', 'flows'] },
```
Note: label changes from 'Automations' to 'Workflows', href updates, and 'automations' is added to keywords so existing searches still find it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidebar.tsx nav href</name>
  <read_first>
    - src/components/layout/sidebar.tsx
  </read_first>
  <files>
    src/components/layout/sidebar.tsx
  </files>
  <action>
In src/components/layout/sidebar.tsx, find line 59 (the nav array entry for Workflows):

```ts
{ icon: Zap, label: 'Workflows', href: '/automations', group: 'build' },
```

Change only the `href` value from `'/automations'` to `'/workflows'`. Label is already 'Workflows' — do not change it. No other lines in this file need editing.
  </action>
  <verify>
    <automated>grep -n "href.*automations" src/components/layout/sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "href.*automations" src/components/layout/sidebar.tsx returns no matches
    - grep "href.*workflows" src/components/layout/sidebar.tsx shows the Zap nav item with /workflows
    - The label still reads 'Workflows' (unchanged)
  </acceptance_criteria>
  <done>Sidebar nav item href is '/workflows'. File has no remaining /automations href strings.</done>
</task>

<task type="auto">
  <name>Task 2: Update flow-canvas.tsx import and new-flow-form.tsx import + router.push</name>
  <read_first>
    - src/components/flows/flow-canvas.tsx
    - src/components/flows/new-flow-form.tsx
  </read_first>
  <files>
    src/components/flows/flow-canvas.tsx
    src/components/flows/new-flow-form.tsx
  </files>
  <action>
In src/components/flows/flow-canvas.tsx, update line 23 — the import for saveWorkflowDefinition:
- BEFORE: `import { saveWorkflowDefinition } from '@/app/(dashboard)/automations/flows/_actions/workflows'`
- AFTER:  `import { saveWorkflowDefinition } from '@/app/(dashboard)/workflows/flows/_actions/workflows'`

Only this one import changes. The function signature of saveWorkflowDefinition is identical in the new location — no call-site changes needed.

In src/components/flows/new-flow-form.tsx, make two changes:
1. Line 11 — the import: `from '@/app/(dashboard)/automations/flows/_actions/workflows'` → `from '@/app/(dashboard)/workflows/flows/_actions/workflows'`
2. Line 51 — the router.push: `router.push(\`/automations/flows/${result.data.id}\`)` → `router.push(\`/workflows/flows/${result.data.id}\`)`
  </action>
  <verify>
    <automated>grep -n "automations" src/components/flows/flow-canvas.tsx src/components/flows/new-flow-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "automations" flow-canvas.tsx returns no matches
    - grep "automations" new-flow-form.tsx returns no matches
    - flow-canvas.tsx imports saveWorkflowDefinition from @/app/(dashboard)/workflows/flows/_actions/workflows
    - new-flow-form.tsx router.push target is /workflows/flows/${result.data.id}
  </acceptance_criteria>
  <done>Both component files updated. Zero /automations references remain.</done>
</task>

<task type="auto">
  <name>Task 3: Update command-palette.tsx nav item</name>
  <read_first>
    - src/components/command-palette.tsx
  </read_first>
  <files>
    src/components/command-palette.tsx
  </files>
  <action>
In src/components/command-palette.tsx, find line 83 — the nav item for Automations:
```ts
{ id: 'nav-tools', label: 'Automations', icon: Zap, href: '/automations', group: 'Navigation', keywords: ['actions', 'tools'] },
```

Replace it with:
```ts
{ id: 'nav-tools', label: 'Workflows', icon: Zap, href: '/workflows', group: 'Navigation', keywords: ['automations', 'actions', 'tools', 'flows'] },
```

Changes: label 'Automations' → 'Workflows', href '/automations' → '/workflows', keywords extended to include 'automations' and 'flows' so users typing the old name still find it.

No other lines in this file reference /automations.
  </action>
  <verify>
    <automated>grep -n "automations" src/components/command-palette.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "href.*automations" command-palette.tsx returns no matches
    - The nav-tools item has label 'Workflows', href '/workflows'
    - keywords array includes 'automations' so users searching the old name still find it
    - grep "automations" returns only the keyword string, not any href
  </acceptance_criteria>
  <done>Command palette nav item updated to /workflows. 'automations' survives only as a search keyword.</done>
</task>

</tasks>

<verification>
After all tasks complete:
```bash
grep -rn "/automations" src/components/layout/sidebar.tsx src/components/flows/flow-canvas.tsx src/components/flows/new-flow-form.tsx src/components/command-palette.tsx
```
The only remaining /automations occurrence should be the keyword string in command-palette.tsx (`keywords: ['automations', ...]`). Zero href or import references to /automations should remain.
</verification>

<success_criteria>
- sidebar.tsx Workflows nav item href is '/workflows'
- flow-canvas.tsx imports saveWorkflowDefinition from the /workflows/_actions path
- new-flow-form.tsx imports createWorkflow from /workflows/_actions and pushes to /workflows/flows/[id]
- command-palette.tsx nav item label is 'Workflows', href is '/workflows', 'automations' exists only in keywords
</success_criteria>

<output>
After completion, create `.planning/workstreams/v29-ux-polish/phases/102-workflows-unification/102-02-SUMMARY.md`
</output>
