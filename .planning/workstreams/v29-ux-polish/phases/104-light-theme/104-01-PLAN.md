---
phase: 104-light-theme
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03

must_haves:
  truths:
    - "ThemeProvider no longer forces dark mode — forcedTheme prop is absent"
    - "System preference is used as default theme on first visit"
    - "html element does not ship a static dark class from the server"
    - "Toaster responds to system theme — not hardcoded dark"
    - "Light-mode CSS variable block exists and border-gradient works on light surfaces"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "ThemeProvider with defaultTheme=system, enableSystem=true, no forcedTheme"
      contains: "defaultTheme=\"system\""
    - path: "src/app/globals.css"
      provides: "html:not(.dark) override for border-gradient"
      contains: "html:not(.dark) .border-gradient"
  key_links:
    - from: "src/app/layout.tsx ThemeProvider"
      to: "html element class attribute"
      via: "next-themes attribute='class'"
      pattern: "attribute=\"class\""
    - from: "src/app/globals.css .dark block"
      to: "CSS custom properties"
      via: "next-themes toggling .dark on html"
      pattern: "\\.dark \\{"
---

<objective>
Unlock the theme system by removing the three ThemeProvider lock props in layout.tsx and removing the static dark class from the html element. Fix the one CSS rule in globals.css that only works on dark surfaces (.border-gradient::before uses white rgba overlay). The light CSS variable block in :root is already fully defined — no new color values need to be invented.

Purpose: This is the single config change that enables the entire theme system. Without it, no component-level dark: classes or CSS variable overrides can ever render in light mode.
Output: layout.tsx with an unlocked ThemeProvider; globals.css with a light-mode override for .border-gradient.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v29-ux-polish/ROADMAP.md
@.planning/workstreams/v29-ux-polish/phases/104-light-theme/104-CONTEXT.md
@.planning/workstreams/v29-ux-polish/phases/104-light-theme/104-RESEARCH.md

<interfaces>
<!-- Exact current state of the files being modified. Read before touching. -->

From src/app/layout.tsx (lines 50–58) — current lock state:
```tsx
<html lang="en" suppressHydrationWarning className={`dark ${inter.variable} ${mono.variable}`}>
  <body className="font-sans antialiased" suppressHydrationWarning>
    <ThemeProvider
      attribute="class"
      defaultTheme="dark"
      forcedTheme="dark"
      enableSystem={false}
      disableTransitionOnChange
    >
```

From src/app/layout.tsx (lines 62–76) — Toaster current state:
```tsx
<Toaster
  richColors
  closeButton
  position="bottom-right"
  theme="dark"
  duration={4000}
  visibleToasts={5}
  toastOptions={{
    style: {
      fontFamily: 'var(--font-sans)',
      borderRadius: '10px',
    },
    className: 'group',
  }}
/>
```

From src/app/globals.css (lines 336–344) — border-gradient current state:
```css
.border-gradient::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  margin: -1px;
  border-radius: inherit;
  background: linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0));
}
```

From src/app/globals.css (lines 276–280) — html:not(.dark) already exists:
```css
html:not(.dark) {
  color-scheme: light;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ThemeProvider dark-mode locks from layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — read the entire file before making any edit
  </read_first>
  <action>
Make exactly three targeted changes to src/app/layout.tsx:

Change 1a — Remove static "dark" class from html element (per D-02):
  BEFORE: className={`dark ${inter.variable} ${mono.variable}`}
  AFTER:  className={`${inter.variable} ${mono.variable}`}

Change 1b — Update ThemeProvider props (per D-01, D-02, D-03):
  Remove:  forcedTheme="dark"
  Change:  defaultTheme="dark"  →  defaultTheme="system"
  Change:  enableSystem={false}  →  enableSystem={true}
  Keep:    attribute="class"  (unchanged)
  Keep:    disableTransitionOnChange  (unchanged — prevents jarring full-page transition flash)

Change 1c — Update Toaster theme prop (per Claude's Discretion):
  BEFORE: theme="dark"
  AFTER:  theme="system"
  (Sonner 2.x supports "system" as a valid value — it follows the current theme)

Do NOT change: suppressHydrationWarning on html or body, font variable declarations, TooltipProvider, any other props.
Do NOT change: src/app/(auth)/layout.tsx — the auth layout's className="dark" on its wrapper div is intentional (login page uses hardcoded hex colors; removing it would break the login page appearance).
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -n "forcedTheme" src/app/layout.tsx | wc -l</automated>
    Expected output: 0 (forcedTheme must be gone)
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -n 'defaultTheme="system"' src/app/layout.tsx</automated>
    Expected output: line containing defaultTheme="system"
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -n 'enableSystem={true}' src/app/layout.tsx</automated>
    Expected output: line containing enableSystem={true}
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -n 'theme="system"' src/app/layout.tsx</automated>
    Expected output: line containing theme="system" (Toaster)
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -n 'className=.*`dark ' src/app/layout.tsx | wc -l</automated>
    Expected output: 0 (static dark class removed from html)
  </verify>
  <acceptance_criteria>
    - grep for "forcedTheme" in src/app/layout.tsx returns no matches
    - grep for 'defaultTheme="system"' in src/app/layout.tsx returns exactly 1 match
    - grep for 'enableSystem={true}' in src/app/layout.tsx returns exactly 1 match
    - grep for 'theme="system"' in src/app/layout.tsx returns exactly 1 match (on the Toaster)
    - grep for 'className=.*`dark ' in src/app/layout.tsx returns no matches
    - grep for 'disableTransitionOnChange' in src/app/layout.tsx returns exactly 1 match (still present)
    - grep for 'attribute="class"' in src/app/layout.tsx returns exactly 1 match (still present)
  </acceptance_criteria>
  <done>ThemeProvider is fully unlocked: no forcedTheme, defaultTheme=system, enableSystem=true. Toaster follows system theme. Static dark class removed from html element.</done>
</task>

<task type="auto">
  <name>Task 2: Fix border-gradient for light mode in globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — read the @layer utilities section (lines 323–463) before editing
  </read_first>
  <action>
The .border-gradient::before pseudo-element uses rgba(255,255,255,0.08) as a white overlay — this is invisible on light surfaces.

Inside @layer utilities, immediately after the existing .border-gradient::before rule block (which ends around line 344), add a light-mode override rule:

```css
html:not(.dark) .border-gradient::before {
  background: linear-gradient(180deg, rgba(0,0,0,0.04), rgba(0,0,0,0));
}
```

This uses a subtle dark-tinted gradient (4% black) that reads correctly as a border on light backgrounds, matching the visual intent of the original white overlay on dark backgrounds.

Placement: Add the new rule directly after the closing brace of the .border-gradient::before block, within the same @layer utilities block. Do not change anything else in the file.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -n "html:not(.dark) .border-gradient" src/app/globals.css</automated>
    Expected output: a line containing the selector
    <automated>cd /c/Users/Vanildo/Dev/xphere && grep -A2 "html:not(.dark) .border-gradient" src/app/globals.css</automated>
    Expected output: shows rgba(0,0,0,0.04) in the background gradient
  </verify>
  <acceptance_criteria>
    - grep for "html:not(.dark) .border-gradient" in src/app/globals.css returns exactly 1 match
    - grep -A3 for "html:not(.dark) .border-gradient" shows rgba(0,0,0,0.04) in output
    - The original .border-gradient::before rule (with white rgba overlay) is unchanged
    - The new rule appears inside the @layer utilities block (not outside it)
  </acceptance_criteria>
  <done>border-gradient renders with a visible dark-tinted overlay on light surfaces instead of an invisible white overlay.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification</name>
  <files></files>
  <read_first>
    None — this is a build check only.
  </read_first>
  <action>
Run the production build to confirm the two file changes introduce no TypeScript errors or build failures:

```bash
cd /c/Users/Vanildo/Dev/xphere && npm run build
```

If build fails, read the error output carefully. The most likely sources are:
1. A typo in the JSX attribute changes in layout.tsx (e.g., missing quote or brace)
2. A TypeScript error if "system" is not a valid type for the Toaster's theme prop

If theme="system" causes a TypeScript error on Toaster, use `theme={"system" as "dark"}` as a fallback — Sonner 2.x accepts it at runtime even if older type definitions don't include it.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/xphere && npm run build 2>&1 | tail -5</automated>
    Expected: exit code 0, output contains "Compiled successfully" or "Route (app)"
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - No TypeScript errors in stderr output
  </acceptance_criteria>
  <done>Build passes with zero TypeScript errors after all layout.tsx and globals.css changes.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. grep -c "forcedTheme" src/app/layout.tsx → must return 0
2. grep -c 'defaultTheme="system"' src/app/layout.tsx → must return 1
3. grep -c 'enableSystem={true}' src/app/layout.tsx → must return 1
4. grep -c 'theme="system"' src/app/layout.tsx → must return 1
5. grep -c "html:not(.dark) .border-gradient" src/app/globals.css → must return 1
6. npm run build → must exit 0
</verification>

<success_criteria>
- ThemeProvider has no forcedTheme prop, defaultTheme="system", enableSystem={true}
- html element className does not include the static "dark" string
- Toaster uses theme="system"
- globals.css has a light-mode border-gradient override using rgba(0,0,0,0.04)
- npm run build exits 0
</success_criteria>

<output>
After completion, create .planning/workstreams/v29-ux-polish/phases/104-light-theme/104-01-SUMMARY.md
</output>
