---
phase: 130-calendar-product-coherence
plan: 06
type: execute
wave: 3
depends_on: ["130-01", "130-02", "130-03", "130-04", "130-05"]
files_modified: []
autonomous: false
requirements: [SYNC-03, SYNC-04]

must_haves:
  truths:
    - "A human has confirmed, in a real browser, that every SYNC-03/SYNC-04 fix in this phase renders and behaves as the automated tests and code changes claim"
  artifacts: []
  key_links: []
---

<objective>
This phase's code changes (Plans 130-01 through 130-05) are verified by automated tests and `npm run build`, but this repo has no `@testing-library/react` (or equivalent) component-render test infrastructure — several of this phase's fixes are UI-rendering changes (a removed dialog step, a new checkbox group, a removed settings card, a bucketed list's visual output) that can only be proven correct by a human looking at the running app. This plan is that manual verification pass, consolidated into one checkpoint at the end rather than scattered after every plan (per this project's checkpoint-fatigue guidance).

Purpose: confirm the full SYNC-03/SYNC-04 surface actually works end-to-end in a real browser before this phase is considered done.
Output: operator confirmation (or a list of concrete issues to fix) covering all 7 verification points below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/130-calendar-product-coherence/130-CONTEXT.md
@.planning/workstreams/calendar-reliability/phases/130-calendar-product-coherence/130-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser verification of every SYNC-03/SYNC-04 fix in this phase</name>
  <files>
    src/app/(dashboard)/calendar/bookings/page.tsx, src/app/(dashboard)/calendar/calendar/page.tsx,
    src/components/calendar/calendar-view.tsx, src/components/chat/contact-info-panel.tsx,
    src/components/calendar/new-event-type-dialog.tsx, src/components/calendar/meeting-preferences.tsx,
    src/app/(dashboard)/calendar/preferences/page.tsx, src/components/calendar/event-type-form.tsx,
    src/app/(dashboard)/calendar/_actions/bookings.ts
  </files>
  <action>
    Serve the production build containing all of Plans 130-01 through 130-05,
    then manually walk through all 7 verification steps in &lt;how-to-verify&gt;
    below in a real browser session. This task makes no further code changes
    — it is a verification-only checkpoint on top of what those 5 plans
    already built.
  </action>
  <what-built>
    - Plan 130-01: bounded bookings read models (`getBookingsForList`/`getBookingsForRange`), a visible 'showed' bucket + badge on `/calendar/bookings`, and a bounded/re-fetching calendar view at `/calendar/calendar`.
    - Plan 130-02: correct event-type names in the CRM contact panel's booking list, hydrated `{{meeting.organizer.*}}` workflow variables, and a consistent 'showed' badge in the contact panel.
    - Plan 130-03: the event-type creation dialog no longer offers "Round robin"; Calendar → Preferences no longer shows the dead "Meeting location" select.
    - Plan 130-04: a new "Allowed meeting locations" checkbox group on the event type form, offering exactly 6 kinds.
    - Plan 130-05: a `google_meet`-configured event type now produces a real Meet link on booking creation.
  </what-built>
  <how-to-verify>
    This worktree's Turbopack dev server crashes on the junction-based
    `node_modules` (`TurbopackInternalError: Symlink [project]/node_modules
    is invalid, it points out of the filesystem root` — a known, pre-existing
    issue in gsd worktrees, not caused by this phase). Serve the production
    build instead:
    ```bash
    npm run build
    npx next start -p 4267
    ```

    1. **Bookings list bounding + 'showed' visibility (SYNC-03):** Visit
       `http://localhost:4267/calendar/bookings`. Confirm the page loads
       normally (no crash, no infinite spinner). If your test org has a
       booking with `status='showed'` (or create one — e.g. via
       `UPDATE bookings SET status='showed' WHERE id='<some booking id>'` in
       Supabase Studio on a non-production/test row, then revert after), it
       should appear in the **Past** section with a distinct sky-blue badge
       reading "showed" — not silently missing, and not grouped visually
       with a plain "confirmed" badge.
    2. **Calendar view bounded range + re-fetch (SYNC-03):** Visit
       `http://localhost:4267/calendar/calendar`. Confirm the current week's
       bookings render. Click the "next" (▶) arrow to navigate to next week,
       then create a test booking on the dashboard calendar for a date 2+
       weeks out, navigate to that week, and confirm it appears (proves
       re-fetch-on-navigate works, not just the initial bounded load).
    3. **Contact panel booking summary + organizer (SYNC-03):** Open a
       contact in the CRM (`/contacts`) that has at least one linked booking.
       In the side panel's Bookings section, confirm the booking shows the
       real event type name (e.g. "Discovery Call") instead of the generic
       "Booking" label, and confirm a `showed`-status booking (if present)
       shows the distinct sky badge here too.
    4. **Round-robin removed (SYNC-04):** On `/calendar` (Event Types), click
       "New event type". Confirm the dialog goes straight to the event
       type form — no "Personal booking" vs "Round robin" chooser screen.
    5. **Dead Preferences control removed (SYNC-04):** Visit
       `http://localhost:4267/calendar/preferences`. Confirm it shows a
       simple "No account-level scheduling preferences to configure yet"
       card — not a "Meeting location" select promising automatic Google
       Meet generation.
    6. **Location-kinds admin control (SYNC-04):** On the "New event type"
       form (or edit an existing event type), confirm an "Allowed meeting
       locations" checkbox group is present, offering exactly: Google Meet,
       Client's address, Custom address, Phone call, Phone call (custom
       number), Custom video link — and NOT Zoom, Whereby, or Store
       location. Save an event type with `google_meet` checked.
    7. **Google Meet completion (SYNC-04) — best-effort, requires a
       connected Google Calendar integration:** If your test org has Google
       Calendar connected (`/calendar/connections`), book the event type
       from step 6 through its public booking page
       (`/book/{slug}/{event-type-slug}`), selecting Google Meet as the
       location if prompted. Confirm the confirmation email (or the booking
       record's `meeting_url` column) contains a real `meet.google.com`
       link. If no Google Calendar integration is connected in this
       environment, this step is not verifiable here — note that instead of
       failing the checkpoint, since `createMeetingLink` degrades to `null`
       (no link, no booking failure) when no integration is connected, which
       is itself correct, tested behavior (Plan 130-05, Test 7/8 already
       cover the wiring logic without a live Google connection).
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 steps behave as described (or note step 7 as "not verifiable — no Google Calendar connected" if applicable), or describe what broke.</resume-signal>
  <verify>Manual only — see &lt;how-to-verify&gt; steps above. The automatable portions of this phase are already covered by `npx vitest run tests/calendar-bookings-list.test.ts tests/calendar-bookings-page.test.ts tests/contacts-actions-bookings.test.ts tests/calendar-scope.test.ts tests/event-types-actions.test.ts tests/calendar-bookings.test.ts` and `npm run build`, both of which should be re-run and confirmed green immediately before this checkpoint.</verify>
  <done>Human confirms all 7 verification steps in &lt;how-to-verify&gt; behave as described (step 7 may be marked not-verifiable if no Google Calendar integration is connected in this environment).</done>
</task>

</tasks>

<verification>
`npx vitest run tests/calendar-bookings-list.test.ts tests/calendar-bookings-page.test.ts tests/contacts-actions-bookings.test.ts tests/calendar-scope.test.ts tests/event-types-actions.test.ts tests/calendar-bookings.test.ts` fully green. `npm run build` succeeds. Human checkpoint approved.
</verification>

<success_criteria>
SYNC-03 and SYNC-04 are both closed: calendar read models are bounded and display all supported states correctly, and every exposed calendar configuration control either works end-to-end or has been removed — confirmed by a human in a real browser session, not just by code review.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/130-calendar-product-coherence/130-06-SUMMARY.md`
</output>
