---
phase: 128-reliable-calendar-scheduling
plan: 06
type: execute
wave: 4
depends_on: ["128-01", "128-02", "128-03", "128-04", "128-05"]
files_modified: []
autonomous: false
requirements: [SCH-01, SCH-02, SCH-03]

must_haves:
  truths:
    - "The calendar_tick_watermark migration is applied to production without disrupting the currently-running (buggy but functional) calendar-tick cron"
    - "The real-DB idempotency test passes when pointed at production after apply"
    - "The production CRON_SECRET is confirmed set (Plan 128-02's 503-on-missing behavior would otherwise take the live cron endpoint offline)"
  artifacts: []
  key_links: []
---

<objective>
Apply the migration produced by Plan 128-04 (`calendar_tick_watermark`, created but not yet applied to production) to the production Supabase project, and confirm the production `CRON_SECRET` env var is actually set (Plan 128-02 made the calendar-tick endpoint 503 when it's missing — if it were somehow unset in production Coolify despite RESEARCH.md's confirmation, this deploy would silently take the live reminder cron offline instead of just being "less secure").

`depends_on` lists every other plan in this phase (128-01 through 128-05), not just 128-04 which produces the migration file — this mirrors Plan 126-06's rationale: applying the watermark migration ahead of the route wiring that assumes it exists (128-05) or ahead of the auth fix that changes the endpoint's failure mode (128-02) would apply a production schema/behavior change ahead of the app code the rest of this phase assumes is already deployed together.

Per this project's operator constraints (CLAUDE.md, project memory), migrations are applied via Supabase MCP `apply_migration` by the operator/orchestrator — never `npx supabase db push` (the migration ledger has desynced from that before). This plan is a manual/orchestrator checkpoint, not autonomous executor work.

Purpose: the migration file alone (Plan 128-04) and the route code alone (Plan 128-05) do not close SCH-01/SCH-02/SCH-03 in production until both are live together — the route code deployed without the migration would 500 on every tick (querying a table that doesn't exist yet), and the migration applied without the route code deployed leaves the old wall-clock-window bug running against a watermark table nothing reads yet.
Output: `calendar_tick_watermark` live in production; the app code from Plans 128-01/128-02/128-05 deployed (this project auto-deploys `main` on push per `CLAUDE.md` — confirm the deploy that includes this phase's commits has actually rolled out before applying the migration, so there is no window where new code queries a table that doesn't exist yet, or old code runs against a schema it doesn't expect).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-RESEARCH.md
@supabase/migrations/1251_calendar_tick_watermark.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply the calendar_tick_watermark migration to production and confirm CRON_SECRET is set</name>
  <read_first>
    - supabase/migrations/1251_calendar_tick_watermark.sql (or whatever the actual next-free-numbered filename Plan 128-04 produced — confirm the real filename via `ls supabase/migrations/ | grep calendar_tick_watermark` before proceeding)
  </read_first>
  <files>supabase/migrations/{actual_filename}_calendar_tick_watermark.sql</files>
  <action>
    Confirm this phase's code (Plans 128-01, 128-02, 128-03, 128-05) has already been merged to `main` and deployed to production (Coolify auto-deploys on push per `CLAUDE.md` — check the Coolify app `xphere-zdt` deployment status or `git log origin/main` for this phase's commits), THEN apply the migration and verify production secret configuration. See &lt;how-to-verify&gt; for exact steps.
  </action>
  <what-built>
    One reviewed, not-yet-applied migration file: `supabase/migrations/{actual_filename}_calendar_tick_watermark.sql` — creates `calendar_tick_watermark` (durable scan-progress cursor, SCH-01/SCH-03), seeds `meeting.starts_in`/`meeting.ended` rows at `now()`, and documents the new semantics of `scheduled_workflow_ticks.fired_minute` (SCH-02) via `COMMENT ON COLUMN`. Also confirms (does not itself change) that production `CRON_SECRET` is set, since Plan 128-02's route change now returns 503 without it.
  </what-built>
  <how-to-verify>
    1. Confirm this phase's app code is already live in production (deployed build includes Plans 128-01/128-02/128-03/128-05's commits) — applying the migration BEFORE the code that reads/writes `calendar_tick_watermark` is deployed is safe (the table simply sits unused by the still-running old route code until the new code rolls out), but applying it AFTER is also safe; the ordering that is NOT safe is deploying Plan 128-05's route code (which queries `calendar_tick_watermark`) before this migration lands — verify that has not already happened by checking whether the current production `calendar-tick` invocations are erroring (Supabase MCP `get_logs` for the API/postgres logs, or Coolify application logs).
    2. Apply `supabase/migrations/{actual_filename}_calendar_tick_watermark.sql` to production via Supabase MCP `apply_migration` (never `npx supabase db push`).
    3. Confirm via Supabase MCP `list_tables` (or `SELECT event_type, scanned_to FROM public.calendar_tick_watermark`) that the table exists with 2 seeded rows (`meeting.starts_in`, `meeting.ended`).
    4. Confirm production `CRON_SECRET` is set: check the Coolify app `xphere-zdt` environment variables (per `CLAUDE.md`'s deployment section) for `CRON_SECRET`, and confirm `.github/workflows/calendar-tick.yml`'s repo secret `CRON_SECRET` matches it. If either is missing, set it now — Plan 128-02's 503 behavior means a missing `CRON_SECRET` now takes the reminder cron fully offline (previously it silently ran unauthenticated, which was the SCH-03 bug, but was at least not observably broken).
    5. Manually trigger the cron once via `.github/workflows/calendar-tick.yml`'s `workflow_dispatch` (or `curl -H "Authorization: Bearer $CRON_SECRET" https://xphere.app/api/cron/calendar-tick`) and confirm a `200` response whose body includes `starts_in_scan`/`ended_scan` fields (proves the new route code + the new table are both live and talking to each other).
    6. Re-run the real-DB test suite with production credentials in `.env.local`: `npx vitest run tests/calendar-tick-idempotency.test.ts`. Must be fully green (not soft-skipped) at this point.
  </how-to-verify>
  <resume-signal>Type "applied" once the migration is live in production, CRON_SECRET is confirmed set in both Coolify and the GitHub Actions repo secret, step 5's manual trigger returns 200 with the new response shape, and step 6's test suite is green, or describe what blocked the apply.</resume-signal>
  <verify>Manual only — see &lt;how-to-verify&gt; steps above; step 3's table check, step 5's manual trigger, and step 6's test run are the closest thing to automated confirmation, but require live production credentials and Supabase MCP access this executor does not hold outside an orchestrator/human session.</verify>
  <done>Human confirms the migration is applied, `calendar_tick_watermark` has 2 seeded rows, `CRON_SECRET` is confirmed set in both Coolify and GitHub Actions, a manual cron trigger returns 200 with the new response shape, and `tests/calendar-tick-idempotency.test.ts` is green (not soft-skipped) against production.</done>
</task>

</tasks>

<verification>
`SELECT event_type, scanned_to FROM public.calendar_tick_watermark` returns 2 rows in production. A manually triggered `calendar-tick` invocation returns 200 with `starts_in_scan`/`ended_scan` fields present. `npx vitest run tests/calendar-tick-idempotency.test.ts` is fully green against production.
</verification>

<success_criteria>
SCH-01, SCH-02, and SCH-03 are enforced in production, not just present as reviewable migration/code — the reminder scheduler is delay-tolerant, exactly-once, and secret-protected end-to-end.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-06-SUMMARY.md`
</output>
