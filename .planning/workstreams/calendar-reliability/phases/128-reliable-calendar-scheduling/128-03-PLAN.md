---
phase: 128-reliable-calendar-scheduling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/workflow-seeds-tenant-neutral.test.ts
  - supabase/seeds/workflows/booking-confirmation.yaml
  - supabase/seeds/workflows/agendamento/cleaning-service-reminders.yaml
  - supabase/seeds/workflows/agendamento/skleanings-lost-remarketing.yaml
  - supabase/seeds/workflows/agendamento/nutricao/skleanings-30d-checkin.yaml
  - supabase/seeds/workflows/agendamento/nutricao/skleanings-90d-upsell.yaml
  - supabase/seeds/workflows/agendamento/pipeline/skleanings-noshow-reengajamento.yaml
  - supabase/seeds/workflows/agendamento/pipeline/skleanings-post-service-review.yaml
  - supabase/seeds/workflows/agendamento/pipeline/skleanings-quote-followup.yaml
  - supabase/seeds/workflows/agendamento/pipeline/skleanings-quote-parado.yaml
  - .planning/workflows/examples/agendamento/cleaning-service-reminders.yaml
  - .planning/workflows/examples/agendamento/skleanings-lost-remarketing.yaml
  - .planning/workflows/examples/agendamento/nutricao/skleanings-30d-checkin.yaml
  - .planning/workflows/examples/agendamento/nutricao/skleanings-90d-upsell.yaml
  - .planning/workflows/examples/agendamento/pipeline/skleanings-noshow-reengajamento.yaml
  - .planning/workflows/examples/agendamento/pipeline/skleanings-post-service-review.yaml
  - .planning/workflows/examples/agendamento/pipeline/skleanings-quote-followup.yaml
  - .planning/workflows/examples/agendamento/pipeline/skleanings-quote-parado.yaml
autonomous: true
requirements: [SCH-04]

must_haves:
  truths:
    - "A newly created organization no longer receives a Skleanings-branded confirmation email, a 'customer' tag, or a 'Job Confirmed' opportunity from the platform-default booking-confirmation workflow"
    - "No file remaining under supabase/seeds/workflows/ (recursively — what both scripts/load-workflow-seeds.ts and src/lib/workflows/seed-org.ts can reach) contains Skleanings-specific branding, pricing, or copy"
    - "The 8 relocated Skleanings-only example workflows are no longer reachable by either seed loader, but remain available as reference examples"
    - "Existing tenant workflow rows already installed in the database (e.g. the Skleanings org's own workflows) are not touched by this change — only git-tracked seed content changes"
  artifacts:
    - path: "supabase/seeds/workflows/booking-confirmation.yaml"
      provides: "Tenant-neutral platform-default booking-confirmation workflow (no brand name, phone number, pricing, or hardcoded pipeline stage)"
    - path: ".planning/workflows/examples/agendamento/"
      provides: "The 8 relocated Skleanings-only example workflows, preserved for authoring reference, out of every seed loader's reach"
    - path: "tests/workflow-seeds-tenant-neutral.test.ts"
      provides: "Regression guard against reintroducing tenant-specific content into supabase/seeds/workflows/"
  key_links:
    - from: "scripts/load-workflow-seeds.ts (recursive loader)"
      to: "supabase/seeds/workflows/**"
      via: "listSeedFiles() walks this tree recursively — must no longer contain agendamento/"
      pattern: "readdirSync"
    - from: "src/lib/workflows/seed-org.ts (per-org loader)"
      to: "supabase/seeds/workflows/**"
      via: "listSeedFiles() (non-recursive) — was already accidentally skipping agendamento/, now the directory is gone entirely so this is by design, not accident"
      pattern: "SEEDS_DIR"
---

<objective>
Close the concrete SCH-04 offender RESEARCH.md found: `supabase/seeds/workflows/booking-confirmation.yaml` is a **currently-live, top-level platform default** (loaded for every new org via `src/lib/workflows/seed-org.ts`) that tags every confirmed-booking contact "customer", creates a "Job Confirmed" pipeline opportunity assuming a stage that doesn't exist for most tenants, and sends a fully Skleanings-branded HTML email (logo, phone number, $120 minimum charge, cleaning-specific prep instructions, `hello@skleanings.com` footer). Also relocate the 8 Skleanings-only example workflows under `supabase/seeds/workflows/agendamento/**` — currently *not* installed for new orgs only by accident (the per-org loader's non-recursive directory scan happens to skip them), but which the recursive bulk loader (`npm run seed`) WOULD install to every org if ever run against all orgs.

Purpose: new (and any other non-Skleanings) organizations must never receive a competitor's brand, pricing, or cleaning-specific business assumptions baked into "platform defaults"; per REQUIREMENTS.md's explicit Out-of-Scope note, existing tenant `workflows` rows already installed for the Skleanings org are NOT touched — only the git-tracked seed content and file layout change.
Output: neutralized `booking-confirmation.yaml`, `agendamento/` relocated in full to `.planning/workflows/examples/agendamento/` (already the documented home for reference/example workflows per `CLAUDE.md`/`WORKFLOWS.md`), and a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-RESEARCH.md
@supabase/seeds/workflows/booking-confirmation.yaml
@supabase/seeds/workflows/cancellation-acknowledgement.yaml
@src/lib/workflows/seed-org.ts
@scripts/load-workflow-seeds.ts
@scripts/validate-workflows.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing tenant-neutrality regression test</name>
  <files>tests/workflow-seeds-tenant-neutral.test.ts</files>
  <read_first>
    - supabase/seeds/workflows/booking-confirmation.yaml (current offending content — the strings this test forbids)
    - scripts/validate-workflows.ts lines 42-62 (`collectYamlFiles`/`walk` — the exact recursive-directory-walk idiom to reuse for this test's own scan, so the test covers precisely what the real loaders can reach)
  </read_first>
  <behavior>
    - Scanning every `.yaml`/`.yml` file recursively under `supabase/seeds/workflows/` and searching each file's raw text for any of: `/skleanings/i`, `/\$120 minimum/i`, `/508.?500.?6625/`, `/hello@skleanings\.com/i`, `/Job Confirmed/` — zero matches across all files.
    - `supabase/seeds/workflows/agendamento/` does not exist as a directory (relocated, not merely edited in place — proves the bulk-loader's recursive walk can no longer reach it structurally, not just that today's content happens to be clean).
    - `.planning/workflows/examples/agendamento/` exists and recursively contains exactly 8 `.yaml` files (the exact count of Skleanings-only workflows RESEARCH.md identified: `cleaning-service-reminders.yaml`, `skleanings-lost-remarketing.yaml`, `nutricao/skleanings-30d-checkin.yaml`, `nutricao/skleanings-90d-upsell.yaml`, `pipeline/skleanings-noshow-reengajamento.yaml`, `pipeline/skleanings-post-service-review.yaml`, `pipeline/skleanings-quote-followup.yaml`, `pipeline/skleanings-quote-parado.yaml`).
  </behavior>
  <action>
    Create `tests/workflow-seeds-tenant-neutral.test.ts` using Node's `fs`/`path` (no Supabase, no mocking needed — pure filesystem + string assertions, runs unconditionally, never soft-skipped). Implement a local recursive YAML collector mirroring `scripts/validate-workflows.ts`'s `walk()` (same recursion shape, scoped to the directory passed in):
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { existsSync, readdirSync, readFileSync, statSync } from 'node:fs'
    import { join, resolve } from 'node:path'

    const REPO_ROOT = resolve(__dirname, '..')
    const SEEDS_DIR = join(REPO_ROOT, 'supabase', 'seeds', 'workflows')
    const EXAMPLES_AGENDAMENTO_DIR = join(REPO_ROOT, '.planning', 'workflows', 'examples', 'agendamento')

    function collectYaml(dir: string): string[] {
      if (!existsSync(dir)) return []
      const out: string[] = []
      for (const entry of readdirSync(dir)) {
        const full = join(dir, entry)
        if (statSync(full).isDirectory()) out.push(...collectYaml(full))
        else if (entry.endsWith('.yaml') || entry.endsWith('.yml')) out.push(full)
      }
      return out
    }

    const FORBIDDEN_PATTERNS = [/skleanings/i, /\$120 minimum/i, /508.?500.?6625/, /hello@skleanings\.com/i, /Job Confirmed/]

    describe('SCH-04: platform-default workflow seeds are tenant-neutral', () => {
      it('no file under supabase/seeds/workflows/ references Skleanings-specific branding or copy', () => {
        const offenders: string[] = []
        for (const file of collectYaml(SEEDS_DIR)) {
          const content = readFileSync(file, 'utf8')
          for (const pattern of FORBIDDEN_PATTERNS) {
            if (pattern.test(content)) offenders.push(`${file} matches ${pattern}`)
          }
        }
        expect(offenders).toEqual([])
      })

      it('supabase/seeds/workflows/agendamento/ no longer exists (relocated, not just edited)', () => {
        expect(existsSync(join(SEEDS_DIR, 'agendamento'))).toBe(false)
      })

      it('all 8 relocated Skleanings example workflows exist under .planning/workflows/examples/agendamento/', () => {
        expect(collectYaml(EXAMPLES_AGENDAMENTO_DIR)).toHaveLength(8)
      })
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/workflow-seeds-tenant-neutral.test.ts</automated>
  </verify>
  <done>
    `tests/workflow-seeds-tenant-neutral.test.ts` exists with all 3 assertions; running it now FAILS (booking-confirmation.yaml still contains forbidden strings, and `agendamento/` still exists under `supabase/seeds/workflows/`) — this is the expected RED state before Task 2.
  </done>
  <acceptance_criteria>
    - `test -f tests/workflow-seeds-tenant-neutral.test.ts` succeeds
    - `grep -q "FORBIDDEN_PATTERNS" tests/workflow-seeds-tenant-neutral.test.ts` succeeds
    - `grep -c "it(" tests/workflow-seeds-tenant-neutral.test.ts` returns 3
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Neutralize booking-confirmation.yaml and relocate agendamento/** to .planning/workflows/examples/</name>
  <files>supabase/seeds/workflows/booking-confirmation.yaml, supabase/seeds/workflows/agendamento/**, .planning/workflows/examples/agendamento/**</files>
  <read_first>
    - tests/workflow-seeds-tenant-neutral.test.ts (Task 1's output — the exact contract to satisfy)
    - supabase/seeds/workflows/cancellation-acknowledgement.yaml (a genuinely neutral platform-default example already in this repo — same `send_tenant_email`/`send_sms` node shape, no brand)
  </read_first>
  <action>
    1. Relocate all 8 files (preserve subdirectory structure) via `git mv`, so history is preserved and nothing is left behind under `supabase/seeds/workflows/agendamento/`:
       - `git mv supabase/seeds/workflows/agendamento/cleaning-service-reminders.yaml .planning/workflows/examples/agendamento/cleaning-service-reminders.yaml`
       - `git mv supabase/seeds/workflows/agendamento/skleanings-lost-remarketing.yaml .planning/workflows/examples/agendamento/skleanings-lost-remarketing.yaml`
       - `git mv supabase/seeds/workflows/agendamento/nutricao/skleanings-30d-checkin.yaml .planning/workflows/examples/agendamento/nutricao/skleanings-30d-checkin.yaml`
       - `git mv supabase/seeds/workflows/agendamento/nutricao/skleanings-90d-upsell.yaml .planning/workflows/examples/agendamento/nutricao/skleanings-90d-upsell.yaml`
       - `git mv supabase/seeds/workflows/agendamento/pipeline/skleanings-noshow-reengajamento.yaml .planning/workflows/examples/agendamento/pipeline/skleanings-noshow-reengajamento.yaml`
       - `git mv supabase/seeds/workflows/agendamento/pipeline/skleanings-post-service-review.yaml .planning/workflows/examples/agendamento/pipeline/skleanings-post-service-review.yaml`
       - `git mv supabase/seeds/workflows/agendamento/pipeline/skleanings-quote-followup.yaml .planning/workflows/examples/agendamento/pipeline/skleanings-quote-followup.yaml`
       - `git mv supabase/seeds/workflows/agendamento/pipeline/skleanings-quote-parado.yaml .planning/workflows/examples/agendamento/pipeline/skleanings-quote-parado.yaml`
       (Create the destination directories first if `git mv` doesn't auto-create them; confirm `supabase/seeds/workflows/agendamento/` is fully empty and removed afterward — an empty dir has no git presence, so this is automatic once all 8 files are moved.)
       Do NOT edit the content of these 8 files — they retain their Skleanings-specific content as authoring examples (per RESEARCH.md's recommendation: "relocate rather than delete — they're realistic, well-formed examples... `.planning/workflows/examples/` is the existing, documented home for exactly that").
    2. Replace the ENTIRE contents of `supabase/seeds/workflows/booking-confirmation.yaml` with this tenant-neutral version — removes the `tag_customer` and `create_opportunity` nodes entirely (both assume business-specific platform state — a "customer" tagging convention and a "Job Confirmed" pipeline stage — that most orgs won't have; per RESEARCH.md's alternative recommendation, "those nodes removed from the platform default entirely, left to per-tenant customization"), and replaces the branded HTML email with generic copy using only variables already confirmed to exist on `MeetingScope` (`src/lib/calendar/scope.ts`: `title`, `starts_date`, `starts_time`, `location.address`, `google_calendar_url`, `attendee_contact.first_name`, `attendee_contact.email`, `id`):
       ```yaml
       # Platform-default workflow — ships to every org.
       # On booking confirmed: sends a generic confirmation email to the
       # attendee (only if the contact has an email address) with the
       # appointment details, a link to add it to Google Calendar, and
       # links to cancel or reschedule. Deliberately tenant-neutral (SCH-04)
       # — no tagging or opportunity-creation nodes, since those assume
       # business-specific platform state (a "customer" tag convention, a
       # "Job Confirmed" pipeline stage) that most orgs will not have
       # configured; tenants that want those can add them via the builder.

       name: Booking confirmation
       description: |
         Platform-default. When a booking is confirmed, sends a confirmation
         email to the attendee (only if the contact has an email address)
         with the appointment details and links to add it to Google Calendar,
         cancel, or reschedule.
       trigger:
         type: event
         event: meeting.confirmed

       nodes:
         - id: check_email
           kind: condition
           expression: "meeting.attendee_contact.email != ''"

         - id: send_confirmation_email
           kind: send_tenant_email
           to: "{{meeting.attendee_contact.email}}"
           subject: "Your {{meeting.title}} appointment is confirmed"
           body: |
             <!DOCTYPE html>
             <html lang="en">
             <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Appointment Confirmed</title></head>
             <body style="margin:0;padding:0;background:#f4f4f4;font-family:Arial,Helvetica,sans-serif;">
               <table width="100%" cellpadding="0" cellspacing="0" style="background:#f4f4f4;padding:20px 0;">
                 <tr><td align="center">
                   <table width="600" cellpadding="0" cellspacing="0" style="max-width:600px;width:100%;background:#ffffff;border-radius:8px;overflow:hidden;">
                     <tr>
                       <td style="padding:32px 40px;">
                         <p style="margin:0 0 8px;font-size:16px;color:#333333;">Hello {{meeting.attendee_contact.first_name}},</p>
                         <h1 style="margin:0 0 8px;font-size:22px;color:#333333;">Your appointment is confirmed</h1>
                         <p style="margin:0 0 24px;font-size:15px;color:#555555;">Here are your appointment details:</p>
                         <p style="margin:4px 0;font-size:15px;color:#333333;">Date: {{meeting.starts_date}}</p>
                         <p style="margin:4px 0;font-size:15px;color:#333333;">Time: {{meeting.starts_time}}</p>
                         <p style="margin:4px 0 24px;font-size:15px;color:#333333;">Location: {{meeting.location.address}}</p>
                         <p style="margin:0 0 6px;font-size:14px;color:#333333;">Need to change your appointment?</p>
                         <p style="margin:0 0 20px;font-size:14px;">
                           <a href="https://xphere.app/booking/cancel/{{meeting.id}}" style="color:#333333;">Cancel</a>
                           &nbsp;|&nbsp;
                           <a href="https://xphere.app/booking/reschedule/{{meeting.id}}" style="color:#333333;">Reschedule</a>
                         </p>
                         <p style="margin:0 0 20px;font-size:14px;">
                           <a href="{{meeting.google_calendar_url}}" style="color:#333333;">Add to Google Calendar</a>
                         </p>
                         <p style="margin:0;font-size:14px;color:#555555;">We look forward to seeing you!</p>
                       </td>
                     </tr>
                   </table>
                 </td></tr>
               </table>
             </body>
             </html>

       edges:
         - from: trigger
           to: check_email
         - from: check_email
           to: send_confirmation_email
           when: "true"
       ```
    3. Do NOT run `npm run seed`, `tsx scripts/load-workflow-seeds.ts`, or anything that writes to the `workflows`/`workflow_versions` tables — per REQUIREMENTS.md's Out-of-Scope note and RESEARCH.md's Runtime State Inventory, existing tenant workflow rows (Skleanings' already-installed automations) must NOT be touched by this plan. This task only edits git-tracked files.
  </action>
  <verify>
    <automated>npx vitest run tests/workflow-seeds-tenant-neutral.test.ts && npm run workflows:validate-all && npm run build</automated>
  </verify>
  <done>
    `tests/workflow-seeds-tenant-neutral.test.ts` passes fully. `npm run workflows:validate-all` passes (proves the relocated files still parse/validate against the node/trigger spec in their new location, and `booking-confirmation.yaml`'s edited structure is still a valid workflow definition). `npm run build` passes (per CLAUDE.md: always run build after changes, even YAML-only ones, to catch any incidental type regressions from the new test file). `git status` shows the 8 files moved (not duplicated) and `booking-confirmation.yaml` modified in place.
  </done>
  <acceptance_criteria>
    - `test -d supabase/seeds/workflows/agendamento` fails (directory gone)
    - `test -d .planning/workflows/examples/agendamento` succeeds
    - `grep -qi "skleanings" supabase/seeds/workflows/booking-confirmation.yaml` fails (no match)
    - `grep -q "check_email" supabase/seeds/workflows/booking-confirmation.yaml` succeeds
    - `npx vitest run tests/workflow-seeds-tenant-neutral.test.ts` exits 0 with zero failing tests
    - `npm run workflows:validate-all` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
`npx vitest run tests/workflow-seeds-tenant-neutral.test.ts` is fully green. `npm run workflows:validate-all` passes for every YAML under both `supabase/seeds/workflows/` and `.planning/workflows/examples/`. `npm run build` succeeds. No `workflows`/`workflow_versions` database rows were touched by this plan (git diff only).
</verification>

<success_criteria>
New (and any non-Skleanings) organizations onboarded after this change receive a tenant-neutral `booking-confirmation` platform default. The 8 Skleanings-only example workflows survive as reference material but are structurally unreachable by both seed loaders. Already-installed tenant workflow rows are untouched.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-03-SUMMARY.md`
</output>
