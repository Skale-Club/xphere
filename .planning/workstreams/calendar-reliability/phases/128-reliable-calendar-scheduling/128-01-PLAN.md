---
phase: 128-reliable-calendar-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/calendar-tick-window.test.ts
  - src/lib/calendar/tick.ts
autonomous: true
requirements: [SCH-01, SCH-02]

must_haves:
  truths:
    - "A watermark-bounded scan window can be computed that is bounded by a persisted watermark on the low end and 'now' on the high end, capped so a very old/missing watermark does not force an unbounded historical scan"
    - "The idempotency key for a meeting.starts_in reminder is derived from the booking's start_at + offset (the due-moment), not from wall-clock tick time — computing it twice for the same booking/offset at two different real times yields an identical value"
    - "A meeting.starts_in candidate whose start_at has already passed by dispatch time is flagged as stale and must not be treated as a normal due candidate"
    - "Whether a tick should advance its watermark is a pure decision based on whether any dispatch was released (retried) during that pass"
  artifacts:
    - path: "src/lib/calendar/tick.ts"
      provides: "Pure, DB-agnostic scheduling math for the calendar-tick scheduler: computeDueWindow, isDue, computeStartsInTargetMinute, computeEndedTargetMinute, isStartsInCandidateStale, shouldAdvanceWatermark, truncateToMinute, MAX_CATCHUP_LOOKBACK_MINUTES"
    - path: "tests/calendar-tick-window.test.ts"
      provides: "Unit coverage proving the SCH-01 (delay tolerance + stale-skip) and SCH-02 (stable dedup key) behaviors in isolation from Supabase/route wiring"
  key_links:
    - from: "src/app/api/cron/calendar-tick/route.ts (wired in Plan 128-05)"
      to: "src/lib/calendar/tick.ts"
      via: "import { computeDueWindow, isDue, computeStartsInTargetMinute, computeEndedTargetMinute, isStartsInCandidateStale, shouldAdvanceWatermark } from '@/lib/calendar/tick'"
      pattern: "from '@/lib/calendar/tick'"
---

<objective>
Extract the watermark-window, offset-derived-dedup-key, stale-candidate, and watermark-advance-guard logic that currently lives nowhere (it's the *missing* logic in `src/app/api/cron/calendar-tick/route.ts`) into a new, pure, unit-testable module `src/lib/calendar/tick.ts`. This is the Wave 0 prerequisite RESEARCH.md identifies for SCH-01/SCH-02: the actual scan-window and dedup-key bugs cannot be meaningfully unit-tested while inline in a Supabase-calling route handler, and this repo's established pattern for cron routes (`src/lib/obs/alerts.ts` behind `obs-alerts/route.ts`, tested via `tests/obs-alerts.test.ts`) is exactly "pure functions in `src/lib/**`, thin route".

Purpose: gives Plan 128-05 (route wiring) a tested, trustworthy building block instead of inline logic, and makes the SCH-01/SCH-02 bug fix itself directly provable without mocking Supabase.
Output: `src/lib/calendar/tick.ts` (new pure functions) + `tests/calendar-tick-window.test.ts` (new unit suite), both green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-CONTEXT.md
@.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-RESEARCH.md
@src/app/api/cron/calendar-tick/route.ts
@tests/obs-alerts.test.ts
</context>

<interfaces>
<!-- The exact function signatures this plan must produce. Plan 128-05 imports
these directly — no renaming, no different argument order. -->

```typescript
// src/lib/calendar/tick.ts

/** Hard cap on catch-up lookback (minutes). Orchestrator decision (128-CONTEXT
 *  open question 1): 24h. Protects against firing stale "starts in N minutes"
 *  content after a long outage; meeting.ended has no equivalent staleness
 *  problem and is not capped differently — only the SCAN window is capped,
 *  uniformly, for both event types. */
export const MAX_CATCHUP_LOOKBACK_MINUTES = 24 * 60

export interface DueWindow {
  /** Exclusive lower bound — the persisted watermark, capped. */
  scanStart: Date
  /** Inclusive upper bound — the tick's current wall-clock time. */
  scanEnd: Date
}

export function truncateToMinute(date: Date): Date

export function computeDueWindow(
  now: Date,
  watermark: Date | null,
  maxLookbackMinutes?: number, // defaults to MAX_CATCHUP_LOOKBACK_MINUTES
): DueWindow

export function isDue(targetMinute: Date, window: DueWindow): boolean

export function computeStartsInTargetMinute(startAt: Date, offsetMinutes: number): Date

export function computeEndedTargetMinute(endAt: Date): Date

export function isStartsInCandidateStale(startAt: Date, now: Date): boolean

export function shouldAdvanceWatermark(releasedCount: number): boolean
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing unit test suite for the pure tick-scheduling functions</name>
  <files>tests/calendar-tick-window.test.ts</files>
  <read_first>
    - tests/obs-alerts.test.ts (the exact "pure function, no mocking" test shape to copy — plain `describe`/`it`/`expect`, no Supabase, no `vi.mock`)
    - src/app/api/cron/calendar-tick/route.ts lines 1-160 (the current buggy window/dedup logic this module replaces — read to understand offset sign convention: negative offsetMinutes = "before")
  </read_first>
  <behavior>
    - `computeDueWindow(now, watermark)`: with `watermark` 10 minutes before `now` and no cap concern, returns `{ scanStart: watermark, scanEnd: now }` exactly.
    - `computeDueWindow(now, null)`: with no watermark (first run), returns `scanStart = now - MAX_CATCHUP_LOOKBACK_MINUTES minutes` (the capped floor), not an unbounded/epoch value.
    - `computeDueWindow(now, watermark)`: with `watermark` 3 days before `now` (a long-outage scenario) and default cap, `scanStart` is clamped to `now - 24h`, NOT the actual 3-day-old watermark — proves the lookback cap actually caps.
    - `computeDueWindow(now, watermark, maxLookbackMinutes)`: an explicit smaller `maxLookbackMinutes` argument (e.g. 30) is honored over the default constant.
    - `isDue(targetMinute, window)`: `targetMinute === window.scanStart` (exact equality) returns `false` — the lower bound is exclusive (prevents reprocessing the boundary target across consecutive ticks).
    - `isDue(targetMinute, window)`: `targetMinute === window.scanEnd` (exact equality) returns `true` — the upper bound is inclusive.
    - `isDue(targetMinute, window)`: `targetMinute` before `scanStart` or after `scanEnd` returns `false`.
    - `computeStartsInTargetMinute(startAt, offsetMinutes)`: `startAt = 2026-07-20T14:00:00.000Z`, `offsetMinutes = -5` → returns `2026-07-20T13:55:00.000Z` (truncated to the minute).
    - `computeStartsInTargetMinute(startAt, offsetMinutes)`: called twice with the IDENTICAL `(startAt, offsetMinutes)` pair returns bit-for-bit identical `Date` values both times — this is the SCH-02 core assertion: the key does not depend on when the function is called (simulates two ticks, hours apart, both scanning the same due-moment during catch-up).
    - `computeEndedTargetMinute(endAt)`: `endAt = 2026-07-20T15:07:32.000Z` → returns `2026-07-20T15:07:00.000Z` (seconds/ms truncated).
    - `isStartsInCandidateStale(startAt, now)`: `startAt` in the past relative to `now` (meeting already started) → `true`.
    - `isStartsInCandidateStale(startAt, now)`: `startAt` in the future relative to `now` → `false`.
    - `isStartsInCandidateStale(startAt, now)`: `startAt === now` (exact equality) → `true` (a meeting starting at this exact instant no longer needs a "starts in N minutes" reminder).
    - `shouldAdvanceWatermark(releasedCount)`: `0` → `true`.
    - `shouldAdvanceWatermark(releasedCount)`: any positive number (e.g. `1`, `3`) → `false` — if any dispatch was released (an emit failed and its claim was rolled back) during a pass, the watermark must not advance past that pass, so the ENTIRE window is retried on the next tick (already-succeeded items are cheaply no-ops via the `scheduled_workflow_ticks` PK collision).
  </behavior>
  <action>
    Create `tests/calendar-tick-window.test.ts` with plain Vitest `describe`/`it`/`expect` blocks (no `vi.mock`, no Supabase — this is pure-function testing, matching `tests/obs-alerts.test.ts`'s structure exactly). Import everything under test from `@/lib/calendar/tick`, which does not exist yet — this file is expected to fail to resolve/compile until Task 2 creates it (RED state). Group tests into `describe` blocks per function: `computeDueWindow`, `isDue`, `computeStartsInTargetMinute`, `computeEndedTargetMinute`, `isStartsInCandidateStale`, `shouldAdvanceWatermark`. Use fixed ISO date strings (not `new Date()`/`Date.now()`) throughout so the suite is fully deterministic. Compare `Date` values via `.toISOString()` equality, not `toBe`/reference equality.
  </action>
  <verify>
    <automated>npx vitest run tests/calendar-tick-window.test.ts</automated>
  </verify>
  <done>
    `tests/calendar-tick-window.test.ts` exists with all 15 behaviors above as individual `it()` blocks across 6 `describe` blocks; running it currently fails (module `@/lib/calendar/tick` does not exist) — this is the expected RED state before Task 2.
  </done>
  <acceptance_criteria>
    - `test -f tests/calendar-tick-window.test.ts` succeeds
    - `grep -c "it(" tests/calendar-tick-window.test.ts` returns 14 or more
    - `grep -q "from '@/lib/calendar/tick'" tests/calendar-tick-window.test.ts` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement src/lib/calendar/tick.ts to make the suite pass</name>
  <files>src/lib/calendar/tick.ts</files>
  <read_first>
    - tests/calendar-tick-window.test.ts (Task 1's output — the exact contract to satisfy)
    - The `<interfaces>` block above (exact exported names/signatures required)
  </read_first>
  <action>
    Create `src/lib/calendar/tick.ts` implementing exactly the exports listed in the `&lt;interfaces&gt;` block above:
    - `MAX_CATCHUP_LOOKBACK_MINUTES = 24 * 60`.
    - `truncateToMinute(date)`: returns a new `Date` with seconds and milliseconds zeroed (`copy.setSeconds(0, 0)`), matching the existing truncation idiom already used in `route.ts`'s `now.setSeconds(0, 0)`.
    - `computeDueWindow(now, watermark, maxLookbackMinutes = MAX_CATCHUP_LOOKBACK_MINUTES)`: `cappedFloor = new Date(now.getTime() - maxLookbackMinutes * 60_000)`; `scanStart = watermark && watermark.getTime() > cappedFloor.getTime() ? watermark : cappedFloor`; return `{ scanStart, scanEnd: now }`.
    - `isDue(targetMinute, window)`: `targetMinute.getTime() > window.scanStart.getTime() && targetMinute.getTime() <= window.scanEnd.getTime()`.
    - `computeStartsInTargetMinute(startAt, offsetMinutes)`: `truncateToMinute(new Date(startAt.getTime() + offsetMinutes * 60_000))` — offset is negative for "before", matching the sign convention already used in `route.ts`'s `parseOffset`/fire-moment math (`fireTargetStart = now - offsetMs`, i.e. adding a negative offset moves the fire moment earlier).
    - `computeEndedTargetMinute(endAt)`: `truncateToMinute(endAt)`.
    - `isStartsInCandidateStale(startAt, now)`: `startAt.getTime() <= now.getTime()`.
    - `shouldAdvanceWatermark(releasedCount)`: `releasedCount === 0`.
    Add a top-of-file comment explaining this module's purpose and that it is deliberately DB-agnostic (no Supabase import) so it stays unit-testable without mocking, per `tests/obs-alerts.test.ts`'s established pattern — cite SCH-01/SCH-02 by ID.
  </action>
  <verify>
    <automated>npx vitest run tests/calendar-tick-window.test.ts</automated>
  </verify>
  <done>
    All tests in `tests/calendar-tick-window.test.ts` pass. `npm run build` still succeeds (no type errors from the new module).
  </done>
  <acceptance_criteria>
    - `test -f src/lib/calendar/tick.ts` succeeds
    - `grep -q "export function computeDueWindow" src/lib/calendar/tick.ts` succeeds
    - `grep -q "export function computeStartsInTargetMinute" src/lib/calendar/tick.ts` succeeds
    - `grep -q "export function shouldAdvanceWatermark" src/lib/calendar/tick.ts` succeeds
    - `npx vitest run tests/calendar-tick-window.test.ts` exits 0 with zero failing tests
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
`npx vitest run tests/calendar-tick-window.test.ts` is fully green (not soft-skipped — this suite has no DB dependency and must always run). `npm run build` succeeds.
</verification>

<success_criteria>
The SCH-01 (delay-tolerant window + stale-skip) and SCH-02 (stable, offset-derived dedup key + watermark-advance guard) core logic exists as pure, tested functions in `src/lib/calendar/tick.ts`, ready for Plan 128-05 to wire into the route handler.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/128-reliable-calendar-scheduling/128-01-SUMMARY.md`
</output>
