---
phase: 126-booking-trust-boundary
plan: 06
type: execute
wave: 3
depends_on: ["126-01", "126-02", "126-03", "126-04", "126-05"]
files_modified: []
autonomous: false
requirements: [CAL-02, CAL-04]

must_haves:
  truths:
    - "Migrations 1249 and 1250 are applied to production without violating any pre-existing data"
    - "The overlap-constraint and RLS real-DB tests pass against the production schema after apply"
  artifacts: []
  key_links: []
---

<objective>
Apply the two migrations produced by Plans 126-03 (`1249_bookings_organizer_overlap_guard.sql`) and 126-04 (`1250_calendar_rls_least_privilege.sql`) to the production Supabase project. `depends_on` lists every other code plan in this phase (126-01, 126-02, 126-03, 126-04, 126-05), not just the two that produce the migration files — this is deliberate: applying the RLS-narrowing migration (1250) before Plan 126-05's cancel-page fix or 126-01/126-02's validation wiring land would apply a production behavior change ahead of the app code the rest of this phase assumes is already in place, even though neither migration has a literal code-level type/import dependency on those plans. Per this project's operator constraints (CLAUDE.md, project memory), migrations are applied via Supabase MCP `apply_migration` by the operator/orchestrator — never `npx supabase db push` (the migration ledger has desynced from that before). This plan is a manual/orchestrator checkpoint, not autonomous executor work: it requires MCP tool access to the live Supabase project and an explicit go/no-go decision if the pre-flight audit finds existing violations.

Purpose: the migration files alone (created in Plans 126-03/126-04) do not close CAL-02/CAL-04 in production until applied. Exclusion constraints have no `NOT VALID`/deferred-validation path — if any two existing confirmed bookings for the same organizer already overlap, applying migration 1249 fails outright, so the audit MUST run first.
Output: both migrations live in production; `tests/calendar-overlap-constraint.test.ts` and `tests/calendar-rls.test.ts` pass when pointed at production (or a branch/staging DB with the same schema).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/126-booking-trust-boundary/126-RESEARCH.md
@supabase/migrations/1249_bookings_organizer_overlap_guard.sql
@supabase/migrations/1250_calendar_rls_least_privilege.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migrations 1249 and 1250 to production via Supabase MCP</name>
  <read_first>
    - supabase/migrations/1249_bookings_organizer_overlap_guard.sql (contains the pre-flight audit SQL in its header comment)
    - supabase/migrations/1250_calendar_rls_least_privilege.sql
  </read_first>
  <files>supabase/migrations/1249_bookings_organizer_overlap_guard.sql, supabase/migrations/1250_calendar_rls_least_privilege.sql</files>
  <action>
    Run the pre-flight overlap audit against production, then apply both
    migrations to production via Supabase MCP apply_migration (never
    `npx supabase db push`), then confirm both constraints/policy drops are
    live and the real-DB test suites go green. See &lt;how-to-verify&gt; below
    for the exact steps.
  </action>
  <what-built>
    Two reviewed, not-yet-applied migration files:
    - `supabase/migrations/1249_bookings_organizer_overlap_guard.sql` — adds `bookings.organizer_user_id`, a `CHECK (start_at < end_at)` constraint, and an `EXCLUDE USING gist` constraint preventing overlapping confirmed native bookings for the same organizer across event types (CAL-02).
    - `supabase/migrations/1250_calendar_rls_least_privilege.sql` — drops the 3 anon-broad RLS policies on `bookings`, `user_availability`, `event_types` (CAL-04).
  </what-built>
  <how-to-verify>
    1. Run the pre-flight audit query from `supabase/migrations/1249_bookings_organizer_overlap_guard.sql`'s header comment against the production database (via Supabase MCP `execute_sql` or the SQL editor). If it returns any rows, STOP — do not apply migration 1249 yet. Each returned pair is two confirmed, native bookings for the same organizer that already overlap; resolve them first (cancel or reschedule one of each pair) using the existing dashboard cancel/reschedule flows, then re-run the audit until it returns zero rows.
    2. Once the audit is clean, apply `supabase/migrations/1249_bookings_organizer_overlap_guard.sql` via Supabase MCP `apply_migration`.
    3. Apply `supabase/migrations/1250_calendar_rls_least_privilege.sql` via Supabase MCP `apply_migration`.
    4. Confirm via `list_tables` / `get_advisors` (or a direct `SELECT conname FROM pg_constraint WHERE conname IN ('bookings_valid_interval', 'bookings_no_organizer_overlap')`) that both new constraints exist, and that `SELECT policyname FROM pg_policies WHERE tablename IN ('bookings','user_availability','event_types') AND policyname IN ('bookings_public_insert','user_availability_public_select','event_types_public_select')` returns zero rows (confirms the drop took effect).
    5. Re-run the real-DB test suites locally with production (or a branch/staging DB with the same applied schema) credentials in `.env.local`: `npx vitest run tests/calendar-overlap-constraint.test.ts tests/calendar-rls.test.ts`. Both suites must go fully green (not soft-skipped) at this point — soft-skip is only acceptable pre-apply.
    6. Set `NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` / `SUPABASE_DB_URL` (or `DATABASE_URL`) in `.env.local` beforehand if not already present, matching this repo's existing real-DB test convention.
  </how-to-verify>
  <resume-signal>Type "applied" once both migrations are live in production and step 5's test suites are green, or describe what the pre-flight audit found / what blocked the apply.</resume-signal>
  <verify>Manual only — see &lt;how-to-verify&gt; steps above; step 4's SQL checks and step 5's test-suite run are the closest thing to an automated confirmation, but require live production/staging credentials this executor does not hold.</verify>
  <done>Human confirms both migrations are applied, the constraint/policy checks in step 4 pass, and tests/calendar-overlap-constraint.test.ts + tests/calendar-rls.test.ts are green (not soft-skipped) in step 5.</done>
</task>

</tasks>

<verification>
`SELECT conname FROM pg_constraint WHERE conname IN ('bookings_valid_interval', 'bookings_no_organizer_overlap')` returns both. `SELECT policyname FROM pg_policies WHERE policyname IN ('bookings_public_insert','user_availability_public_select','event_types_public_select')` returns zero rows. `npx vitest run tests/calendar-overlap-constraint.test.ts tests/calendar-rls.test.ts` is fully green against production/staging.
</verification>

<success_criteria>
CAL-02 and CAL-04 are enforced in production, not just present as reviewable migration files.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/126-booking-trust-boundary/126-06-SUMMARY.md`
</output>
