---
phase: 129-provider-synchronization-integrity
plan: 06
type: execute
wave: 3
depends_on: ["129-01", "129-02", "129-03", "129-04", "129-05"]
files_modified: []
autonomous: false
requirements: [SYNC-01]

must_haves:
  truths:
    - "The bookings.google_event_id migration produced by Plan 129-02 is applied to production"
    - "The real-DB regression suites for this phase pass against the production schema after apply"
  artifacts: []
  key_links: []
---

<objective>
Apply the migration produced by Plan 129-02 (`supabase/migrations/<next>_bookings_google_event_id.sql`) to the production Supabase project. `depends_on` lists every other plan in this phase (129-01 through 129-05), not just 129-02 — mirroring Plan 126-06's precedent: applying a schema change ahead of the rest of this phase's code (which assumes the column exists once live) is undesirable even though most of those plans have no literal code-level dependency on the migration. Per this project's operator constraints (CLAUDE.md, project memory), migrations are applied via Supabase MCP `apply_migration` by the operator/orchestrator — never `npx supabase db push` (this project's migration ledger has desynced from that before).

Unlike Plan 126-06's migrations, this one is a single nullable `ADD COLUMN IF NOT EXISTS google_event_id TEXT` with no default and no constraint — there is no pre-flight audit required (a nullable column addition cannot fail against existing data, unlike Plan 126-03's exclusion constraint). This checkpoint is therefore lighter-weight: apply, confirm the column exists, re-run the phase's real-DB test suite.

Purpose: the migration file alone (created in Plan 129-02) does not close SYNC-01's Gap 2 storage foundation in production until applied.
Output: the migration is live in production; `tests/integrations-rls.test.ts` (Plan 129-03) passes against production when pointed at it (this is a regression proof unaffected by the new migration, but re-running it here confirms the environment is healthy post-apply).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/129-provider-synchronization-integrity/129-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply the bookings.google_event_id migration to production via Supabase MCP</name>
  <read_first>
    - supabase/migrations/&lt;next&gt;_bookings_google_event_id.sql (the exact filename produced by Plan 129-02 — list `supabase/migrations/` to find it if the placeholder number is unknown at this point)
  </read_first>
  <files>supabase/migrations/&lt;next&gt;_bookings_google_event_id.sql</files>
  <action>
    Apply the migration to production via Supabase MCP `apply_migration` (never `npx supabase db push`), then confirm the column is live and the phase's real-DB test suite goes green. See &lt;how-to-verify&gt; below for the exact steps.
  </action>
  <what-built>
    One reviewed, not-yet-applied migration file: `supabase/migrations/<next>_bookings_google_event_id.sql` — adds a nullable `bookings.google_event_id TEXT` column, populated going forward by Plan 129-02's changes to `createBooking`/`createBookingInternal` (SYNC-01 Gap 2 storage foundation).
  </what-built>
  <how-to-verify>
    1. Identify the actual migration filename: `ls supabase/migrations | grep google_event_id`.
    2. Apply it to production via Supabase MCP `apply_migration` (this is a simple `ADD COLUMN IF NOT EXISTS` with no default — no pre-flight audit is needed, unlike Plan 126-06's exclusion constraint).
    3. Confirm the column exists: `SELECT column_name FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'bookings' AND column_name = 'google_event_id'` should return one row (via Supabase MCP `execute_sql` or `list_tables`).
    4. Re-run the phase's real-DB test suite locally with production (or a branch/staging DB with the same applied schema) credentials in `.env.local`: `npx vitest run tests/integrations-rls.test.ts`. This suite must go fully green (not soft-skipped) at this point.
    5. Run the full mocked suite for this phase as a final sanity check: `npx vitest run tests/google-calendar-busy.test.ts tests/booking-validation.test.ts tests/calendar-bookings.test.ts tests/xkedule-webhook.test.ts tests/ghl-no-bookings-writes.test.ts`.
    6. Run `npm run build` to confirm the full production build (including the type check) is green with all of this phase's changes in place.
  </how-to-verify>
  <resume-signal>Type "applied" once the migration is live in production and steps 4-6 are green, or describe what blocked the apply.</resume-signal>
  <verify>Manual only — see &lt;how-to-verify&gt; steps above; step 3's SQL check and steps 4-6's suite/build runs are the closest thing to an automated confirmation, but require live production credentials this executor does not hold.</verify>
  <done>Human confirms the migration is applied, the column-existence check in step 3 passes, and all test suites plus `npm run build` are green in steps 4-6.</done>
</task>

</tasks>

<verification>
`SELECT column_name FROM information_schema.columns WHERE table_name = 'bookings' AND column_name = 'google_event_id'` returns one row against production. `npx vitest run tests/integrations-rls.test.ts` is fully green (not soft-skipped) against production/staging. `npm run build` is green.
</verification>

<success_criteria>
SYNC-01's storage foundation (bookings.google_event_id) is enforced in production, not just present as a reviewable migration file.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/129-provider-synchronization-integrity/129-06-SUMMARY.md`
</output>
