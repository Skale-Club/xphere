---
phase: 127-canonical-booking-lifecycle
plan: 08
type: execute
wave: 3
depends_on: ["127-01", "127-02", "127-03", "127-04", "127-05", "127-06", "127-07"]
files_modified: []
autonomous: false
requirements: [LIFE-01]

must_haves:
  truths:
    - "Migration 1251 (transition_booking_status RPC) is applied to production"
    - "The real-DB RPC test passes against the production schema after apply, not soft-skipped"
---

<objective>
Apply the migration produced by Plan 127-01 (`supabase/migrations/1251_booking_lifecycle_transition.sql`) to the production Supabase project. `depends_on` lists every other plan in this phase, not just 127-01 — this is deliberate, mirroring Plan 126-06's precedent: by the time this checkpoint runs, EVERY writer in the codebase (native, MCP, Xkedule, both workflow engines) already calls `transition.ts`'s guarded functions, which call `ctx.supabase.rpc('transition_booking_status', ...)`. Until this migration is applied to production, every one of those call sites will fail at runtime in production (the RPC does not exist there yet) — applying the migration LAST, after all the call sites already exist in code, is intentional: it means the moment this checkpoint completes, the entire canonical lifecycle contract goes live atomically across every writer, rather than some writers working and others erroring during a partial rollout window.

Per this project's operator constraints (CLAUDE.md, project memory), migrations are applied via Supabase MCP `apply_migration` by the operator/orchestrator — never `npx supabase db push` (the migration ledger has desynced from that before).

Purpose: the migration file alone (created in Plan 127-01) does not close LIFE-01 in production until applied — every writer's RPC call would 42883 (function does not exist) against production today.
Output: `transition_booking_status` lives in production; `tests/calendar-lifecycle-rpc.test.ts` passes when pointed at production (not soft-skipped); a manual smoke test of one real transition (e.g. cancel a real or disposable test booking via the dashboard) confirms end-to-end behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/calendar-reliability/phases/127-canonical-booking-lifecycle/127-RESEARCH.md
@supabase/migrations/1251_booking_lifecycle_transition.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 1251 to production via Supabase MCP</name>
  <read_first>
    - supabase/migrations/1251_booking_lifecycle_transition.sql (the RPC this task applies)
  </read_first>
  <files>supabase/migrations/1251_booking_lifecycle_transition.sql</files>
  <action>
    Apply `supabase/migrations/1251_booking_lifecycle_transition.sql` to production via Supabase MCP `apply_migration` (never `npx supabase db push`), then confirm the RPC exists with the correct grants and the real-DB test suite goes green. See &lt;how-to-verify&gt; below for the exact steps.
  </action>
  <what-built>
    One reviewed, not-yet-applied migration file: `supabase/migrations/1251_booking_lifecycle_transition.sql` — adds `public.transition_booking_status(uuid, uuid, text, text[])`, a `SECURITY DEFINER` RPC granted to `service_role` only, that every booking writer in the codebase (native dashboard/public actions, MCP tools, the Xkedule webhook indirectly via mapStatus/calendarEventFor, and both workflow-action dispatchers) now calls through `src/lib/calendar/transition.ts`'s guarded functions.
  </what-built>
  <how-to-verify>
    1. Apply `supabase/migrations/1251_booking_lifecycle_transition.sql` to production via Supabase MCP `apply_migration`.
    2. Confirm via `list_tables`/`get_advisors` (or a direct SQL check) that the function exists and is locked down correctly:
       `SELECT proname, prosecdef FROM pg_proc WHERE proname = 'transition_booking_status'` returns one row with `prosecdef = true` (SECURITY DEFINER).
       `SELECT grantee, privilege_type FROM information_schema.routine_privileges WHERE routine_name = 'transition_booking_status'` returns `service_role`/`EXECUTE` and must NOT return `anon` or `authenticated`.
    3. Set `NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_DB_URL` (or `DATABASE_URL`) in `.env.local` if not already present (matching this repo's existing real-DB test convention), then re-run `npx vitest run tests/calendar-lifecycle-rpc.test.ts` — it must go fully green (not soft-skipped) at this point.
    4. Run the full suite once more to confirm nothing regressed: `npm run build && npm test`.
    5. Optional manual smoke test: from the dashboard, cancel a disposable/test booking (or one already scheduled for cancellation) and confirm (a) the booking's status updates, (b) no error is thrown, (c) `event_dispatches` gains a new `meeting.cancelled` row for that booking (queryable via Supabase MCP `execute_sql`: `SELECT * FROM event_dispatches WHERE booking_id = '<id>' ORDER BY created_at DESC LIMIT 1`).
  </how-to-verify>
  <resume-signal>Type "applied" once the migration is live in production and step 3's test suite is green, or describe what blocked the apply.</resume-signal>
  <verify>Manual only — see &lt;how-to-verify&gt; steps above; step 2's SQL checks and step 3's test-suite run are the closest thing to an automated confirmation, but require live production credentials this executor does not hold.</verify>
  <done>Human confirms the migration is applied, the grant checks in step 2 pass, and tests/calendar-lifecycle-rpc.test.ts is green (not soft-skipped) in step 3.</done>
</task>

</tasks>

<verification>
`SELECT proname FROM pg_proc WHERE proname = 'transition_booking_status'` returns one row against production. `npx vitest run tests/calendar-lifecycle-rpc.test.ts` is fully green against production. `npm run build && npm test` succeed.
</verification>

<success_criteria>
The canonical booking lifecycle transition RPC is live in production, not just present as a reviewable migration file — every writer's guarded transition calls now succeed end-to-end.
</success_criteria>

<output>
After completion, create `.planning/workstreams/calendar-reliability/phases/127-canonical-booking-lifecycle/127-08-SUMMARY.md`
</output>
