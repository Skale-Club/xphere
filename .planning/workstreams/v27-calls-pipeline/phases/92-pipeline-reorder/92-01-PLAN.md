---
plan: 92-01
phase: 92
title: Pipeline Reorder — Verify Pre-implemented Code
objective: Verify reorderOpportunities server action and same-column kanban onDragEnd logic match PIPE-07..08 requirements.
requirements_addressed: [PIPE-07, PIPE-08]
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/pipeline/actions.ts
  - src/components/pipeline/kanban-board.tsx
autonomous: true
---

# Plan 92-01: Verify Pipeline Reorder

## Objective
Confirm that reorderOpportunities batch-updates position by array index, and onDragEnd detects same-column drops and performs optimistic reorder with rollback.

## Tasks

### Task 1: Verify implementation
Read and confirm all key files exist and implement the phase goals:
- `actions.ts` — `reorderOpportunities(stageId, orderedIds[])` iterates with position=index, returns {error} or void, calls revalidatePath
- `kanban-board.tsx` — onDragEnd detects sameStage, builds orderedIds array with splice, calls reorderOpportunities, rolls back setItems on error

### Task 2: Build gate
`npm run build` exits 0.

## Validation
- [ ] All key files verified
- [ ] npm run build exits 0
